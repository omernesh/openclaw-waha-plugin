---
phase: 49-modular-skill-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/messaging.md
  - skills/groups.md
  - skills/contacts.md
  - skills/channels.md
  - skills/chats.md
autonomous: true
requirements: [SKL-02, SKL-03, SKL-07]

must_haves:
  truths:
    - "Agent reading messaging.md can invoke all standard + rich message actions with correct parameters"
    - "Agent reading groups.md can perform any group admin operation including invite codes"
    - "Agent reading contacts.md understands both native vCard and file-based .vcf approaches"
    - "Agent reading channels.md knows the invite code vs JID gotcha and can discover/follow/unfollow channels"
    - "Agent reading chats.md can manage chats, read messages, and understands labels are WhatsApp Business only"
  artifacts:
    - path: "skills/messaging.md"
      provides: "Standard actions, rich messages, iCal approach"
      contains: "sendEvent"
    - path: "skills/groups.md"
      provides: "All group management actions"
      contains: "addParticipants"
    - path: "skills/contacts.md"
      provides: "Contact actions + vCard approaches"
      contains: "vcf"
    - path: "skills/channels.md"
      provides: "Newsletter channel actions"
      contains: "followChannel"
    - path: "skills/chats.md"
      provides: "Chat management + labels"
      contains: "archiveChat"
  key_links:
    - from: "skills/messaging.md"
      to: "SKILL.md"
      via: "Back-reference header"
      pattern: "See SKILL.md"
    - from: "skills/contacts.md"
      to: "skills/messaging.md"
      via: "Cross-reference for sendFile .vcf"
      pattern: "sendFile"
---

<objective>
Create the 5 larger per-category skill instruction files: messaging, groups, contacts, channels, chats.

Purpose: These are the highest-action-count categories. Each file gives the agent complete, self-contained documentation for invoking every action in that category — action tables, parameter details, task-oriented examples, and gotchas.

Output: 5 markdown files in skills/ directory
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-modular-skill-architecture/49-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create messaging.md, groups.md, and contacts.md</name>
  <files>skills/messaging.md, skills/groups.md, skills/contacts.md</files>
  <read_first>
    - SKILL.md (current skill file — source of truth for existing documentation)
    - src/channel.ts lines 404-484 (STANDARD_ACTIONS + UTILITY_ACTIONS — authoritative action list)
    - .planning/phases/49-modular-skill-architecture/49-RESEARCH.md (action inventory, examples, gotchas)
  </read_first>
  <action>
    Create three category files in skills/ directory. Each file follows this template:

    ```markdown
    # [Category] — WhatsApp Actions

    > Part of the WAHA OpenClaw skill. See [SKILL.md](../SKILL.md) for overview and other categories.

    ## Actions

    | Action | Parameters | Notes |
    |--------|-----------|-------|
    ...

    ## Examples

    ### [Common task]
    ```
    Action: X
    Parameters: { ... }
    ```

    ## Gotchas
    - ...
    ```

    **messaging.md** — Standard Actions + Rich Messages + iCal (per SKL-07):
    - Standard actions (target auto-resolution): send, reply, poll, react, edit, unsend, pin, unpin, read, delete
    - Rich message utility actions: sendMulti, sendPollVote, sendLocation, sendContactVcard, sendList, sendLinkPreview, sendButtonsReply, sendEvent, forwardMessage, sendPoll, starMessage, readMessages
    - Examples: send text, send poll, react, read messages, send event (native sendEvent), send iCal file (sendFile with .ics — per SKL-07)
    - Gotchas: readMessages vs read confusion (comparison table), poll needs name+options+multipleAnswers, react needs full messageId format (true_chatId_shortId), sendButtons deprecated
    - iCal section: document both `sendEvent` (native WhatsApp event) and `sendFile` with .ics for external calendar interop, with examples from RESEARCH.md

    **groups.md** — Group Management (25 actions):
    - All actions from UTILITY_ACTIONS group section: createGroup, getGroups, getGroup, getGroupsCount, deleteGroup, leaveGroup, joinGroup, setGroupSubject, setGroupDescription, setGroupPicture, deleteGroupPicture, getGroupPicture, addParticipants, removeParticipants, getParticipants, promoteToAdmin, demoteFromAdmin, demoteToMember, setInfoAdminOnly, getInfoAdminOnly, setMessagesAdminOnly, getMessagesAdminOnly, getInviteCode, revokeInviteCode, getGroupJoinInfo, refreshGroups
    - Examples: create group, add/remove participant, get invite code, join by code, promote/demote admin
    - Gotchas: joinGroup takes code NOT full URL, demoteFromAdmin/demoteToMember are aliases, participants[] needs both @c.us AND @lid JIDs for NOWEB, group picture takes file=path or URL

    **contacts.md** — Contacts + vCard (per SKL-07):
    - Actions: getContacts, getContact, checkContactExists, getContactAbout, getContactPicture, blockContact, unblockContact, createOrUpdateContact (new ACT-03)
    - vCard section (SKL-07): Three approaches — (1) native `send` with contacts[] param, (2) `sendContactVcard` utility action, (3) file-based `sendFile` with .vcf path
    - Examples from RESEARCH.md: send vCard via send action, send .vcf file, createOrUpdateContact
    - Gotchas: phoneNumber format = country code + digits, NO + prefix (e.g., "972544329000")

    CRITICAL: Use UTILITY_ACTIONS from channel.ts as authoritative source — do NOT miss actions that are in UTILITY_ACTIONS but absent from current SKILL.md. Specifically: createOrUpdateContact (ACT-03), starMessage.
  </action>
  <verify>
    <automated>test -f skills/messaging.md && test -f skills/groups.md && test -f skills/contacts.md && grep -q "sendEvent" skills/messaging.md && grep -q "ics" skills/messaging.md && grep -q "addParticipants" skills/groups.md && grep -q "vcf" skills/contacts.md && grep -q "createOrUpdateContact" skills/contacts.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - skills/messaging.md exists with "## Actions", "## Examples", "## Gotchas" sections
    - skills/messaging.md contains all 10 standard actions + all rich message utility actions
    - skills/messaging.md documents iCal/.ics file approach (SKL-07) with sendFile example
    - skills/messaging.md has readMessages vs read comparison table
    - skills/groups.md exists with all 25+ group actions in action table
    - skills/groups.md documents joinGroup invite code gotcha
    - skills/contacts.md exists with all 8 contact actions including createOrUpdateContact
    - skills/contacts.md documents vCard/.vcf approach (SKL-07) with 3 methods and examples
    - skills/contacts.md documents phoneNumber format gotcha (no + prefix)
    - Each file has back-reference: "See SKILL.md for overview"
  </acceptance_criteria>
  <done>Three category files created with complete action tables, examples, and gotchas. All actions from UTILITY_ACTIONS represented. vCard and iCal documented per SKL-07.</done>
</task>

<task type="auto">
  <name>Task 2: Create channels.md and chats.md</name>
  <files>skills/channels.md, skills/chats.md</files>
  <read_first>
    - SKILL.md (current skill file — source content for channels and chats sections)
    - src/channel.ts lines 425-470 (chat management + channels + labels in UTILITY_ACTIONS)
    - .planning/phases/49-modular-skill-architecture/49-RESEARCH.md (action inventory, gotchas)
  </read_first>
  <action>
    Create two category files in skills/ directory using the same template as Task 1.

    **channels.md** — Newsletter Channels (12+ actions):
    - Actions: getChannels, getChannel, createChannel, deleteChannel, followChannel, unfollowChannel, muteChannel, unmuteChannel, searchChannelsByText, previewChannelMessages, searchChannelsByView, getChannelSearchViews, getChannelSearchCountries, getChannelSearchCategories
    - Examples: follow channel, search channels, create channel, preview messages
    - CRITICAL gotcha: Channel invite code != newsletter JID. `whatsapp.com/channel/CODE` — CODE is NOT the newsletter JID. Must resolve via channel search or API. Document this prominently.

    **chats.md** — Chat Management + Labels (15+ actions):
    - Chat actions: getChats, getChatsOverview, getChatMessages, getChatMessage, getMessageById, deleteChat, clearChatMessages, clearMessages (alias), archiveChat, unarchiveChat, unreadChat, readChatMessages (alias for read), getChatPicture, muteChat, unmuteChat
    - Labels actions (WhatsApp Business only — add prominent caveat): getLabels, createLabel, updateLabel, deleteLabel, getChatLabels, setChatLabels, getChatsByLabel
    - Note alias pairs: clearMessages/clearChatMessages, readChatMessages/read
    - Examples: archive chat, get chat messages, mute chat, create label
    - Gotchas: Labels are WhatsApp Business only (returns empty on personal), clearMessages/clearChatMessages are aliases, muteChat duration is in seconds
  </action>
  <verify>
    <automated>test -f skills/channels.md && test -f skills/chats.md && grep -q "followChannel" skills/channels.md && grep -q "invite code" skills/channels.md && grep -q "archiveChat" skills/chats.md && grep -q "WhatsApp Business" skills/chats.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - skills/channels.md exists with "## Actions", "## Examples", "## Gotchas" sections
    - skills/channels.md contains all 14 channel actions in action table
    - skills/channels.md prominently documents invite code vs JID gotcha
    - skills/chats.md exists with all chat management + labels actions
    - skills/chats.md has "WhatsApp Business only" caveat on labels section
    - skills/chats.md documents alias pairs (clearMessages/clearChatMessages, readChatMessages/read)
    - Each file has back-reference: "See SKILL.md for overview"
  </acceptance_criteria>
  <done>Two category files created with complete action tables, examples, and gotchas. Channel invite code gotcha documented. Labels WhatsApp Business caveat included.</done>
</task>

</tasks>

<verification>
All 5 category files exist in skills/ directory with correct structure:
- `grep -l "## Actions" skills/messaging.md skills/groups.md skills/contacts.md skills/channels.md skills/chats.md` returns all 5 files
- `grep -l "## Examples" skills/messaging.md skills/groups.md skills/contacts.md skills/channels.md skills/chats.md` returns all 5 files
- `grep -l "## Gotchas" skills/messaging.md skills/groups.md skills/contacts.md skills/channels.md skills/chats.md` returns all 5 files
- vCard documented: `grep -c "vcf\|vCard" skills/contacts.md` > 0
- iCal documented: `grep -c "ics\|iCal" skills/messaging.md` > 0
</verification>

<success_criteria>
- 5 files exist: skills/messaging.md, skills/groups.md, skills/contacts.md, skills/channels.md, skills/chats.md
- Every action in UTILITY_ACTIONS (chat, group, contact, channel sections) appears in at least one file
- Each file has Actions table, Examples, and Gotchas sections
- vCard (.vcf) documented in contacts.md with 3 approaches + examples (SKL-07)
- iCal (.ics) documented in messaging.md with sendFile example (SKL-07)
- No "Sammie" written in any file (CLAUDE.md rule)
</success_criteria>

<output>
After completion, create `.planning/phases/49-modular-skill-architecture/49-01-SUMMARY.md`
</output>
