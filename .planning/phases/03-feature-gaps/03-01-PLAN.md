---
phase: 03-feature-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/send.ts
  - src/channel.ts
  - src/config-schema.ts
  - src/types.ts
  - tests/link-preview.test.ts
  - tests/chat-mute.test.ts
autonomous: true
requirements:
  - FEAT-01
  - FEAT-02
  - FEAT-03
  - FEAT-04
  - FEAT-07

must_haves:
  truths:
    - "When sendWahaText is called with text containing a URL and autoLinkPreview is not false, the WAHA API body includes linkPreview: true"
    - "When sendWahaText is called with text containing no URL, the WAHA API body does NOT include linkPreview"
    - "When autoLinkPreview config is false, linkPreview is never added regardless of URL presence"
    - "muteChat and unmuteChat utility actions are registered and call correct WAHA endpoints"
    - "sendWahaLinkPreview (FEAT-02) exists and is registered as utility action"
    - "formatActionError (FEAT-07) handles 9+ error patterns with suggestions"
  artifacts:
    - path: "src/send.ts"
      provides: "sendWahaText with auto linkPreview, muteWahaChat, unmuteWahaChat"
      contains: "linkPreview"
    - path: "src/channel.ts"
      provides: "muteChat and unmuteChat in UTILITY_ACTIONS and ACTION_HANDLERS"
      contains: "muteChat"
    - path: "src/config-schema.ts"
      provides: "autoLinkPreview config field"
      contains: "autoLinkPreview"
    - path: "tests/link-preview.test.ts"
      provides: "Unit tests for URL detection and linkPreview flag"
    - path: "tests/chat-mute.test.ts"
      provides: "Unit tests for mute/unmute API calls"
  key_links:
    - from: "src/send.ts"
      to: "src/http-client.ts"
      via: "callWahaApi for mute/unmute and sendText"
      pattern: "callWahaApi"
    - from: "src/channel.ts"
      to: "src/send.ts"
      via: "ACTION_HANDLERS calling muteWahaChat/unmuteWahaChat"
      pattern: "muteWahaChat|unmuteWahaChat"
---

<objective>
Add auto link preview to sendWahaText, implement chat mute/unmute actions, and verify two already-completed features (FEAT-02, FEAT-07).

Purpose: When Sammie sends a URL, recipients see a rich preview card automatically. Chat mute/unmute closes a basic capability gap (channel mute exists but chat mute doesn't).
Output: Modified send.ts with linkPreview auto-detection and mute/unmute functions, updated channel.ts with action registration, config schema with autoLinkPreview option, unit tests.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feature-gaps/03-RESEARCH.md

Key patterns from codebase:
- muteWahaChannel/unmuteWahaChannel at send.ts ~lines 1255-1265 are the template for chat mute/unmute
- UTILITY_ACTIONS array at channel.ts ~line 263 for action registration
- ACTION_HANDLERS map at channel.ts ~line 106 for handler wiring
- resolveWahaAccount -> assertAllowedSession -> callWahaApi is the standard call chain
- Config fields: types.ts WahaAccountConfig + config-schema.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auto link preview in sendWahaText + chat mute/unmute functions</name>
  <files>src/send.ts, src/types.ts, src/config-schema.ts, tests/link-preview.test.ts, tests/chat-mute.test.ts</files>
  <behavior>
    - link-preview.test.ts:
      - "detects URLs": URL_REGEX matches http:// and https:// URLs
      - "adds linkPreview": when text contains URL and autoLinkPreview not false, callWahaApi body has linkPreview: true
      - "no URL": when text has no URL, body does NOT have linkPreview
      - "config disables": when autoLinkPreview is false, body never has linkPreview even with URLs
    - chat-mute.test.ts:
      - "mute calls API": muteWahaChat calls callWahaApi with path ending /chats/{chatId}/mute
      - "unmute calls API": unmuteWahaChat calls callWahaApi with path ending /chats/{chatId}/unmute
      - "muteChat registered": muteChat exists in UTILITY_ACTIONS
      - "unmuteChat registered": unmuteChat exists in UTILITY_ACTIONS
  </behavior>
  <action>
    1. Add `autoLinkPreview?: boolean` to WahaAccountConfig in types.ts
    2. Add `autoLinkPreview` to config-schema.ts (boolean, optional, description: "Auto-add linkPreview on URLs")
    3. In send.ts, add URL_REGEX constant: `const URL_REGEX = /https?:\/\/\S+/i;`
    4. In sendWahaText, after building the body: read `cfg.channels?.waha?.autoLinkPreview` (default true). If true AND URL_REGEX.test(text), add `linkPreview: true` to body via conditional spread
    5. Add muteWahaChat() and unmuteWahaChat() in send.ts following exact pattern from muteWahaChannel/unmuteWahaChannel. Path: `/api/{session}/chats/${encodeURIComponent(chatId)}/mute` (and /unmute). Use callWahaApi (defaults to POST)
    6. Add optional `duration?: number` param to muteWahaChat, include in body if provided
    7. Write tests/link-preview.test.ts — mock callWahaApi, test the 4 behaviors above
    8. Write tests/chat-mute.test.ts — mock callWahaApi, test the 4 behaviors above

    DO NOT CHANGE existing sendWahaText behavior for messages without URLs. Only ADD the linkPreview conditional spread.
    DO NOT modify muteWahaChannel/unmuteWahaChannel — these are separate (newsletter) functions.
  </action>
  <verify>
    <automated>npx vitest run tests/link-preview.test.ts tests/chat-mute.test.ts --reporter=verbose</automated>
  </verify>
  <done>sendWahaText adds linkPreview:true when URL detected and config allows. muteWahaChat/unmuteWahaChat exist and call correct WAHA endpoints. All 8 tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Register mute/unmute actions + verify FEAT-02 and FEAT-07</name>
  <files>src/channel.ts</files>
  <action>
    1. Add "muteChat" and "unmuteChat" to the UTILITY_ACTIONS array in channel.ts
    2. Add handlers to ACTION_HANDLERS map:
       - `muteChat: (cfg, p) => muteWahaChat({ cfg, chatId: String(p.chatId), duration: p.duration ? Number(p.duration) : undefined })`
       - `unmuteChat: (cfg, p) => unmuteWahaChat({ cfg, chatId: String(p.chatId) })`
    3. Import muteWahaChat and unmuteWahaChat from send.ts
    4. Verify FEAT-02: confirm sendWahaLinkPreview exists in send.ts (~line 591) and "sendLinkPreview" is in UTILITY_ACTIONS and ACTION_HANDLERS. Document in commit message.
    5. Verify FEAT-07: confirm formatActionError in error-formatter.ts handles 9+ patterns and is wired into handleAction. Document in commit message.

    DO NOT modify STANDARD_ACTIONS — muteChat/unmuteChat are utility actions (mode "none").
    DO NOT remove any existing entries from UTILITY_ACTIONS or ACTION_HANDLERS.
  </action>
  <verify>
    <automated>npx vitest run tests/chat-mute.test.ts --reporter=verbose</automated>
  </verify>
  <done>muteChat and unmuteChat registered as utility actions. FEAT-02 (sendLinkPreview) and FEAT-07 (formatActionError) verified as already working. TypeScript compiles without errors.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/link-preview.test.ts tests/chat-mute.test.ts --reporter=verbose` — all tests pass
- `npx tsc --noEmit` — no type errors
- grep for "muteChat" and "unmuteChat" in channel.ts confirms registration
- grep for "linkPreview" in send.ts confirms auto-detection logic
- grep for "autoLinkPreview" in config-schema.ts confirms config field
</verification>

<success_criteria>
- sendWahaText auto-adds linkPreview:true when text contains URL (configurable)
- muteChat/unmuteChat utility actions registered and functional
- FEAT-02 and FEAT-07 verified as complete (no new code needed)
- All unit tests pass
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-gaps/03-01-SUMMARY.md`
</output>
