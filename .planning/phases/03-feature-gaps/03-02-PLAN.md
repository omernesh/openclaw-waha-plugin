---
phase: 03-feature-gaps
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/inbound.ts
  - src/types.ts
  - tests/mentions.test.ts
autonomous: true
requirements:
  - FEAT-05

must_haves:
  truths:
    - "When an inbound message contains @mentions, the mentionedJids array is populated in WahaInboundMessage"
    - "JIDs are normalized from @s.whatsapp.net to @c.us format"
    - "When _data is missing or malformed, mentionedJids is an empty array (no crash)"
    - "The ctxPayload sent to OpenClaw includes mentionedJids so Sammie knows who was tagged"
  artifacts:
    - path: "src/types.ts"
      provides: "mentionedJids field on WahaInboundMessage"
      contains: "mentionedJids"
    - path: "src/inbound.ts"
      provides: "extractMentionedJids function and wiring into handleWahaInbound"
      contains: "extractMentionedJids"
    - path: "tests/mentions.test.ts"
      provides: "Unit tests for mention extraction"
  key_links:
    - from: "src/inbound.ts"
      to: "OpenClaw ctxPayload"
      via: "mentionedJids field added to context"
      pattern: "mentionedJids"
---

<objective>
Extract @mentioned JIDs from inbound WAHA webhook payloads and include them in the message context sent to OpenClaw.

Purpose: Sammie can understand social context in group messages — knowing who was @mentioned helps respond appropriately.
Output: extractMentionedJids function in inbound.ts, updated WahaInboundMessage type, mentions in ctxPayload, unit tests.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-feature-gaps/03-RESEARCH.md

Key context:
- WAHA NOWEB engine puts mentions at: _data.message.extendedTextMessage.contextInfo.mentionedJid (array of strings)
- JIDs come as @s.whatsapp.net, must normalize to @c.us
- _data field is undocumented/engine-specific — use optional chaining at every level
- handleWahaInbound in inbound.ts processes raw webhook -> WahaInboundMessage -> ctxPayload
- CRITICAL: vCard interception lives in deliverWahaReply in inbound.ts — DO NOT MOVE it
- Only extract from direct message content, not from quoted/replied messages
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract @mentions from inbound messages</name>
  <files>src/inbound.ts, src/types.ts, tests/mentions.test.ts</files>
  <behavior>
    - "extracts mentions": extractMentionedJids returns JID array from _data.message.extendedTextMessage.contextInfo.mentionedJid
    - "normalizes JIDs": @s.whatsapp.net converted to @c.us
    - "empty on missing": returns [] when _data is undefined, or any nested level is missing
    - "empty on non-text": returns [] when message is image/video/etc (no extendedTextMessage)
    - "ctxPayload": mentionedJids array appears in the context payload sent to OpenClaw
  </behavior>
  <action>
    1. Add `mentionedJids?: string[]` to WahaInboundMessage interface in types.ts
    2. In inbound.ts, create exported function `extractMentionedJids(rawPayload: Record<string, unknown>): string[]`:
       - Use optional chaining: `(rawPayload._data as any)?.message?.extendedTextMessage?.contextInfo?.mentionedJid`
       - If result is not an array, return []
       - Filter to strings only (safety)
       - Map each JID: `.replace(/@s\.whatsapp\.net$/, "@c.us")`
       - Return the normalized array
    3. In handleWahaInbound, call extractMentionedJids on the raw webhook payload
    4. Set `msg.mentionedJids = extractMentionedJids(rawPayload)` on the WahaInboundMessage
    5. In the ctxPayload construction, add mentionedJids: if non-empty, include `mentionedJids: msg.mentionedJids` and append a human-readable line: "Mentioned: +phoneNumber1, +phoneNumber2" (extract phone from JID before @c.us)
    6. Write tests/mentions.test.ts testing the 5 behaviors above. Mock raw payloads with various _data structures.

    DO NOT touch deliverWahaReply or vCard interception code.
    DO NOT deeply nest without optional chaining — _data can be undefined at any level.
    Log a debug line when mentions are found: `log.debug("mentions extracted", { count, jids })` using existing logger.
  </action>
  <verify>
    <automated>npx vitest run tests/mentions.test.ts --reporter=verbose</automated>
  </verify>
  <done>extractMentionedJids correctly parses NOWEB _data field. JIDs normalized to @c.us. Empty array on missing/malformed data. mentionedJids included in ctxPayload. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/mentions.test.ts --reporter=verbose` — all tests pass
- `npx tsc --noEmit` — no type errors
- grep for "mentionedJids" in types.ts, inbound.ts confirms wiring
- grep for "extractMentionedJids" in inbound.ts confirms function exists and is called
</verification>

<success_criteria>
- @mentions extracted from NOWEB _data field with full optional chaining safety
- JIDs normalized from @s.whatsapp.net to @c.us
- Empty array returned on any missing/malformed data (never crashes)
- mentionedJids included in ctxPayload for Sammie's context
- All unit tests pass, TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-feature-gaps/03-02-SUMMARY.md`
</output>
