---
phase: 63-dashboard-auth
plan: 03
type: execute
wave: 3
depends_on: [63-02]
files_modified:
  - src/admin/src/components/tabs/IntegrationWizardTab.tsx
  - src/admin/src/components/AppSidebar.tsx
  - src/admin/src/App.tsx
autonomous: true
requirements: [AUTH-06]

must_haves:
  truths:
    - "Integration wizard presents MCP config snippet with the user's API key pre-filled"
    - "Integration wizard presents REST curl example with the user's API key pre-filled"
    - "Integration wizard presents SKILL.md download option"
    - "User can copy any config snippet to clipboard with one click"
    - "Wizard only appears after WhatsApp connection is confirmed"
  artifacts:
    - path: "src/admin/src/components/tabs/IntegrationWizardTab.tsx"
      provides: "Three-option integration setup: MCP, REST, SKILL.md"
      exports: ["default"]
  key_links:
    - from: "src/admin/src/components/tabs/IntegrationWizardTab.tsx"
      to: "authClient.apiKey"
      via: "reads user's API key for pre-filling snippets"
      pattern: "apiKey\\.list"
---

<objective>
Create the integration wizard tab that presents MCP config, REST curl examples, and SKILL.md download — all pre-filled with the user's API key and server URL.

Purpose: After connecting WhatsApp and creating an API key, users need copy-paste-ready integration configs. This is the last step of the self-service onboarding flow.
Output: IntegrationWizardTab with three integration options and copy-to-clipboard
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-dashboard-auth/63-RESEARCH.md
@.planning/phases/63-dashboard-auth/63-02-SUMMARY.md

@src/admin/src/App.tsx
@src/admin/src/components/AppSidebar.tsx
@src/admin/src/lib/auth-client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration wizard tab with MCP/REST/SKILL.md options</name>
  <files>src/admin/src/components/tabs/IntegrationWizardTab.tsx, src/admin/src/components/AppSidebar.tsx, src/admin/src/App.tsx</files>
  <read_first>src/admin/src/components/AppSidebar.tsx (for TabId and sidebar items), src/admin/src/App.tsx (for tab rendering), src/admin/src/lib/auth-client.ts, src/admin/src/components/tabs/ApiKeysTab.tsx (for apiKey.list pattern)</read_first>
  <action>
**A. Create `src/admin/src/components/tabs/IntegrationWizardTab.tsx`:**

On mount, fetch user's API keys via `authClient.apiKey.list()`. Use the first key's masked value for display, or prompt user to create one first if no keys exist.

Three sections in a tabbed or card-based layout:

1. **MCP Config** — Show a JSON config block for Claude Code / MCP clients:
   ```json
   {
     "mcpServers": {
       "chatlytics": {
         "url": "http://<SERVER_URL>/mcp",
         "headers": {
           "Authorization": "Bearer <YOUR_API_KEY>"
         }
       }
     }
   }
   ```
   - Replace `<SERVER_URL>` with `window.location.origin`
   - Replace `<YOUR_API_KEY>` with `ctl_...xxxx` (masked) and show a note: "Replace with your full API key from the API Keys tab"
   - Copy button using navigator.clipboard.writeText

2. **REST API (curl)** — Show example curl commands:
   ```bash
   # Send a message
   curl -X POST http://<SERVER_URL>/api/v1/send \
     -H "Authorization: Bearer <YOUR_API_KEY>" \
     -H "Content-Type: application/json" \
     -d '{"to": "John", "text": "Hello from Chatlytics!"}'

   # Search contacts
   curl http://<SERVER_URL>/api/v1/search?q=marketing \
     -H "Authorization: Bearer <YOUR_API_KEY>"
   ```
   - Same SERVER_URL and API_KEY substitution
   - Copy button per snippet

3. **SKILL.md** — Show a brief description of what SKILL.md is (agent-facing documentation for LLMs) and a download button:
   - "Download SKILL.md" button that opens `/SKILL.md` or fetches and downloads it
   - Brief explanation: "Give this file to your AI agent so it knows all available WhatsApp actions"

**B. UI design:**
- Use shadcn Tabs component for the three options (MCP | REST | SKILL.md)
- Code blocks styled with monospace font, dark background (bg-zinc-900 text-zinc-100), rounded corners
- Copy button positioned top-right of each code block (Lucide Copy icon, turns to Check on success for 2 seconds)
- "No API keys yet" state: show a link/button to navigate to the API Keys tab
- Use existing shadcn Card, Tabs, TabsList, TabsTrigger, TabsContent, Button, Badge

**C. Wire into sidebar and App.tsx:**
- Add `'integration'` to TabId type in AppSidebar
- Add sidebar item: "Integration" (icon: Plug or Rocket from Lucide) — place after API Keys
- Lazy-load in App.tsx: `const IntegrationWizardTab = lazy(() => import(...))`
- Add rendering case in tab switch

**D. Add a "Send Test Message" button in the REST tab:**
- Button calls `POST /api/v1/send` with a test payload using the user's API key
- Shows success/failure toast via sonner
- This fulfills the "send test message" part of AUTH-06 success criteria

IMPORTANT: Do NOT modify any existing tab or sidebar item. Only ADD the new tab.
  </action>
  <verify>
    <automated>cd "D:/docker/waha-oc-plugin" && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep "IntegrationWizardTab" src/admin/src/App.tsx` shows lazy import
    - `grep "integration" src/admin/src/components/AppSidebar.tsx` shows new tab ID
    - `grep "mcpServers" src/admin/src/components/tabs/IntegrationWizardTab.tsx` shows MCP config
    - `grep "curl" src/admin/src/components/tabs/IntegrationWizardTab.tsx` shows REST examples
    - `grep "SKILL.md" src/admin/src/components/tabs/IntegrationWizardTab.tsx` shows download option
    - `grep "clipboard" src/admin/src/components/tabs/IntegrationWizardTab.tsx` shows copy functionality
    - All existing tests pass
  </acceptance_criteria>
  <done>Integration wizard tab shows MCP config, REST curl, SKILL.md download — all with copy-to-clipboard and server URL pre-filled. Send test message button works.</done>
</task>

</tasks>

<verification>
- Integration tab renders three options: MCP, REST, SKILL.md
- Config snippets include window.location.origin as server URL
- Copy buttons work (navigator.clipboard)
- SKILL.md download link functions
- Test message button sends via /api/v1/send
- All existing tests pass
</verification>

<success_criteria>
- IntegrationWizardTab presents all three integration options per AUTH-06
- Code snippets pre-filled with server URL
- Copy-to-clipboard on all snippets
- Send test message button functional
- Zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/63-dashboard-auth/63-03-SUMMARY.md`
</output>
