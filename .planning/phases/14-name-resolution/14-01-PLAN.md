---
phase: 14-name-resolution
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/directory.ts
  - src/monitor.ts
autonomous: true
requirements:
  - NAME-01
  - NAME-04

must_haves:
  truths:
    - "Batch resolve endpoint returns {jid: name} map for any array of JIDs"
    - "@lid JIDs resolve to the same contact name as their @c.us equivalent"
    - "Allow From, Group Allow From, Allowed Groups tag bubbles show contact names instead of raw JIDs"
    - "Dashboard Access Control card shows contact names for @lid JIDs, merging duplicates"
  artifacts:
    - path: "src/directory.ts"
      provides: "resolveJids() batch lookup method with @lid->@c.us fallback"
      contains: "resolveJids"
    - path: "src/monitor.ts"
      provides: "GET /api/admin/directory/resolve endpoint, enhanced createTagInput with name resolution, dashboard @lid dedup"
      contains: "api/admin/directory/resolve"
  key_links:
    - from: "createTagInput (frontend)"
      to: "/api/admin/directory/resolve"
      via: "fetch call in renderTags after setValue"
      pattern: "api/admin/directory/resolve"
    - from: "resolveJids (directory.ts)"
      to: "contacts table"
      via: "SQL query with @lid->@c.us fallback"
      pattern: "resolveJids"
    - from: "Dashboard Access Control"
      to: "/api/admin/directory/resolve"
      via: "batch fetch replacing per-JID createNameResolver for dedup"
      pattern: "nr-allowFrom"
---

<objective>
Add server-side batch JID resolution with @lid->@c.us fallback, enhance tag input bubbles to show resolved names, and deduplicate @lid/@c.us entries in the dashboard Access Control card.

Purpose: Raw @lid JID strings are unreadable to admins. This plan makes every JID-displaying surface show human names.
Output: Batch resolve API endpoint, name-aware tag inputs, deduplicated dashboard Access Control.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-background-directory-sync/13-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/directory.ts (DirectoryDb class):
```typescript
getContact(jid: string): ContactRecord | null;
getContacts(search?: string, type?: string, limit?: number, offset?: number): ContactRecord[];
// ContactRecord has: jid, displayName, firstSeenAt, lastMessageAt, messageCount, isGroup, mode, mentionOnly, customKeywords, canInitiate, canInitiateOverride
```

From src/monitor.ts (embedded frontend JS):
```javascript
// createNameResolver(container, jid) — fetches /api/admin/directory/:jid, renders avatar+name+jid
// createTagInput(containerId, opts) — pill bubble input, returns { getValue(), setValue(arr) }
//   renderTags() internal — currently shows raw JID text in each pill
//   tags[] is array of raw JID strings
// _accessKvBuilt guard — prevents re-creating Name Resolver divs on 30s auto-refresh
// createNameResolver is called per-JID in the dashboard Access Control card for allowFrom, groupAllowFrom, allowedGroups
```

Key HTML IDs for dashboard Access Control:
- `#access-kv` — container for all Access Control content
- `#nr-allowFrom`, `#nr-groupAllowFrom`, `#nr-allowedGroups` — containers for name resolver entries (created dynamically)

Tag input instances:
- `tagInputAllowFrom`, `tagInputGroupAllowFrom`, `tagInputAllowedGroups` — in Settings tab
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch resolve endpoint and @lid fallback</name>
  <files>src/directory.ts, src/monitor.ts</files>
  <read_first>src/directory.ts (full file — understand ContactRecord, getContact, the contacts table schema, FTS5 setup), src/monitor.ts lines 3830-3960 (existing /api/admin/directory/:jid endpoint)</read_first>
  <action>
**In src/directory.ts**, add a `resolveJids(jids: string[])` method to the `DirectoryDb` class:
- Accepts an array of JID strings
- Returns `Map<string, string>` mapping each input JID to its resolved display_name (or null if not found)
- For each JID, first try direct lookup in contacts table
- For @lid JIDs that have no match, also try the @c.us equivalent (replace `@lid` with `@c.us` in the JID)
- If the @c.us equivalent has a display_name, map the original @lid JID to that name
- Use a single SQL query with `WHERE jid IN (...)` for efficiency (batch, not per-JID)
- Then do a second pass for unresolved @lid JIDs with their @c.us equivalents
- Place the method after `getContact()` around line 355

**In src/monitor.ts**, add a `GET /api/admin/directory/resolve` server-side route:
- Accepts query parameter `jids` — comma-separated list of JIDs (URL-encoded)
- Calls `db.resolveJids(jidArray)` on the DirectoryDb instance
- Returns JSON: `{ resolved: { "jid1": "Display Name", "jid2": "Another Name", ... } }`
- JIDs with no resolved name are omitted from the response (not included with null)
- Place the route handler BEFORE the existing `GET /api/admin/directory/:jid` handler (so the path doesn't match `:jid` = "resolve")
- Also update the existing `GET /api/admin/directory/:jid` handler to try @lid->@c.us fallback: if `db.getContact(jid)` returns null and jid ends with `@lid`, try `db.getContact(jid.replace('@lid', '@c.us'))` and return that contact's data with the original @lid JID preserved

IMPORTANT: All code in monitor.ts is inside a template literal (backtick string). Double-escape all backslashes: use `\\\\` for literal backslash, `\\'` for apostrophe in regex, etc. This is a confirmed architectural decision from Phase 12.
  </action>
  <verify>
    <automated>cd D:/docker/waha-oc-plugin && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `resolveJids` method exists in DirectoryDb class in src/directory.ts
- grep for `resolveJids` in directory.ts returns at least 1 match (the method definition)
- grep for `api/admin/directory/resolve` in monitor.ts returns at least 1 match (the route handler)
- grep for `@lid.*@c.us` or `replace.*@lid.*@c.us` in directory.ts returns at least 1 match (the fallback logic)
- The existing `/api/admin/directory/:jid` handler has @lid->@c.us fallback (grep for `@lid` near `getContact` in the route handler section)
- All existing tests pass
  </acceptance_criteria>
  <done>Batch resolve endpoint returns name map for JID arrays. Single-JID endpoint falls back @lid->@c.us. DirectoryDb.resolveJids() handles batch lookups efficiently.</done>
</task>

<task type="auto">
  <name>Task 2: Tag input name resolution and dashboard Access Control dedup</name>
  <files>src/monitor.ts</files>
  <read_first>src/monitor.ts lines 1074-1150 (createTagInput function — understand renderTags, addTag, setValue, getValue), src/monitor.ts lines 1571-1610 (dashboard Access Control card rendering with createNameResolver and _accessKvBuilt guard)</read_first>
  <action>
**Enhance `createTagInput()` to resolve names** (around line 1074 in monitor.ts):

1. Add an optional `resolveNames: true` option to `createTagInput` opts
2. When `resolveNames` is true, modify `renderTags()`:
   - After rendering pills with raw JID text (existing behavior), fire a single batch resolve call
   - Call `fetch('/api/admin/directory/resolve?jids=' + encodeURIComponent(tags.join(',')))`
   - On response, for each pill element: if the JID has a resolved name, update the pill's text content to the name, and set `title` attribute to the raw JID (tooltip on hover)
   - Keep `getValue()` returning raw JID strings (NOT names) — this is critical for config save
   - The pill text is cosmetic only; the underlying `tags[]` array always stores raw JIDs
3. To avoid N+1 fetches, debounce: use a `_resolveTimer` that batches all pending JIDs and fetches once after 50ms
4. On error or no match, leave the pill showing the raw JID (graceful fallback)

**Update tag input creation calls** in `loadConfig()` (around line 2120):
- Change the three `createTagInput` calls for allowFrom, groupAllowFrom, allowedGroups to pass `{ resolveNames: true }`:
  - `tagInputAllowFrom = createTagInput('s-allowFrom-ti', { placeholder: '972544329000@c.us', resolveNames: true });`
  - `tagInputGroupAllowFrom = createTagInput('s-groupAllowFrom-ti', { placeholder: 'Phone or JID, press Enter', resolveNames: true });`
  - `tagInputAllowedGroups = createTagInput('s-allowedGroups-ti', { placeholder: '120363421825201386@g.us', resolveNames: true });`

**Dashboard Access Control @lid dedup** (around line 1575):
- In the `_accessKvBuilt` block where `jidGroups` is iterated to create name resolver entries:
- Before iterating each `grp.arr`, deduplicate: if both a `@lid` JID and its `@c.us` equivalent appear in the same array, keep only the `@c.us` entry (preferred for display)
- Add a dedup function before the loop: `function dedupLidCus(arr) { var cusSet = {}; for (var i = 0; i < arr.length; i++) { if (arr[i].endsWith('@c.us')) cusSet[arr[i].replace('@c.us', '')] = true; } return arr.filter(function(j) { return !(j.endsWith('@lid') && cusSet[j.replace('@lid', '')]); }); }`
- Apply: `grp.arr = dedupLidCus(grp.arr);` before the `for` loop that calls `createNameResolver`

IMPORTANT: All embedded JS is inside a template literal. Double-escape backslashes. Use `\\n` not `\n` in any regex patterns. `\\'` for escaped quotes.
  </action>
  <verify>
    <automated>cd D:/docker/waha-oc-plugin && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- grep for `resolveNames` in monitor.ts returns at least 2 matches (option check + usage in createTagInput calls)
- grep for `api/admin/directory/resolve` in the renderTags area of monitor.ts returns at least 1 match (the batch fetch)
- grep for `dedupLidCus` in monitor.ts returns at least 1 match (the dedup function)
- The three createTagInput calls for allowFrom, groupAllowFrom, allowedGroups all include `resolveNames: true`
- `getValue()` still returns raw JID strings (NOT resolved names) — verify by reading the getValue function
- All existing tests pass
  </acceptance_criteria>
  <done>Tag input bubbles for Allow From, Group Allow From, Allowed Groups show resolved contact names with raw JID as tooltip. Dashboard Access Control card deduplicates @lid/@c.us entries showing same contact once.</done>
</task>

</tasks>

<verification>
1. `npx vitest run` — all tests pass
2. grep `resolveJids` in directory.ts — method exists
3. grep `api/admin/directory/resolve` in monitor.ts — endpoint exists
4. grep `resolveNames` in monitor.ts — tag inputs use name resolution
5. grep `dedupLidCus` in monitor.ts — dashboard dedup exists
</verification>

<success_criteria>
- Batch resolve endpoint accepts JID array and returns name map with @lid->@c.us fallback
- Tag input pills show resolved names with JID tooltips for allowFrom, groupAllowFrom, allowedGroups
- Dashboard Access Control merges @lid/@c.us duplicates into single entries
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-name-resolution/14-01-SUMMARY.md`
</output>
