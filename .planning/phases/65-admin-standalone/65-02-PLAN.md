---
phase: 65-admin-standalone
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - skills/messaging.md
  - skills/groups.md
  - skills/contacts.md
  - skills/channels.md
  - skills/chats.md
  - skills/status.md
  - skills/presence.md
  - skills/profile.md
  - skills/media.md
  - skills/slash-commands.md
autonomous: true
requirements: [SKILL-01]

must_haves:
  truths:
    - "SKILL.md v4 references Chatlytics API key and MCP endpoint with zero OpenClaw-specific instructions"
    - "Agent can understand how to send a WhatsApp message using only SKILL.md v4 content"
    - "MCP config snippet is copy-paste ready for Claude Desktop, Cursor, and Continue"
  artifacts:
    - path: "SKILL.md"
      provides: "Framework-agnostic agent skill documentation"
      contains: "version: 4.0.0"
    - path: "skills/messaging.md"
      provides: "Messaging skill with REST API examples"
      contains: "/api/v1/"
  key_links:
    - from: "SKILL.md"
      to: "REST API endpoints"
      via: "API examples referencing /api/v1/"
      pattern: "/api/v1/"
    - from: "SKILL.md"
      to: "MCP tools"
      via: "MCP config snippet with send_message tool"
      pattern: "send_message"
---

<objective>
Rewrite SKILL.md to v4.0.0 — framework-agnostic Chatlytics documentation.

Purpose: SKILL-01 requires SKILL.md to reference the Chatlytics API key and MCP endpoint with no OpenClaw-specific instructions. This makes the skill usable by any AI agent framework (Claude, Cursor, Continue, custom agents).
Output: SKILL.md v4.0.0 + updated skill category files under skills/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/65-admin-standalone/65-RESEARCH.md
@SKILL.md
@skills/messaging.md

<interfaces>
<!-- REST API endpoints (from Phase 60) -->
POST /api/v1/send        — Send text message { chatId, text }
POST /api/v1/send-media   — Send media { chatId, mediaUrl, type, caption }
GET  /api/v1/messages     — Read messages ?chatId=&limit=
GET  /api/v1/search       — Search contacts/groups ?query=
GET  /api/v1/directory    — List directory ?type=contact|group&search=
GET  /api/v1/sessions     — List sessions
GET  /api/v1/status       — Mimicry + health status
POST /api/v1/send-poll    — Create poll { chatId, title, options }
POST /api/v1/react        — React to message { chatId, messageId, emoji }

<!-- MCP tools (from Phase 62) -->
send_message, send_media, read_messages, search, get_directory,
manage_group, get_status, update_settings, send_poll, send_reaction

<!-- Auth pattern -->
Authorization: Bearer ctl_YOUR_API_KEY
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md to v4.0.0 framework-agnostic format</name>
  <read_first>
    - SKILL.md (full file — current v6.0.0 content to understand structure)
    - skills/messaging.md (reference for action documentation pattern)
    - src/mcp-server.ts (lines 1-80 for actual MCP tool names and descriptions)
    - src/api-routes.ts or src/monitor.ts (grep for "/api/v1/" to confirm exact endpoint paths)
  </read_first>
  <files>
    SKILL.md
    skills/messaging.md
    skills/groups.md
    skills/contacts.md
    skills/channels.md
    skills/chats.md
    skills/status.md
    skills/presence.md
    skills/profile.md
    skills/media.md
    skills/slash-commands.md
  </files>
  <action>
    **SKILL.md v4.0.0 rewrite:**

    Replace the entire SKILL.md with a Chatlytics-focused version. Version MUST be 4.0.0 (not 7.0.0 — per research Pitfall 4).

    New SKILL.md structure:
    ```
    ---
    name: chatlytics-whatsapp
    description: Use when the agent needs to send/receive WhatsApp messages via Chatlytics
    metadata:
      version: 4.0.0
    ---

    # Chatlytics WhatsApp — Skill Index

    ## Authentication
    API key: `Authorization: Bearer ctl_YOUR_API_KEY`
    Server URL: CHATLYTICS_URL env var or http://localhost:8050

    ## MCP Config (Claude Desktop / Cursor / Continue)
    ```json
    {
      "mcpServers": {
        "chatlytics": {
          "type": "http",
          "url": "http://localhost:8050/mcp",
          "headers": { "Authorization": "Bearer ctl_YOUR_API_KEY" }
        }
      }
    }
    ```

    ## MCP Tools Quick Reference
    | Tool | Description |
    |------|-------------|
    | send_message | Send text to any chat (auto-resolves names) |
    | send_media | Send image/video/file/voice |
    | read_messages | Read recent messages from a chat |
    | search | Find contacts, groups, channels by name |
    | get_directory | List contacts/groups/newsletters |
    | manage_group | Create, delete, add/remove participants |
    | get_status | Health, mimicry status, session info |
    | update_settings | Modify config (DM filter, group filter) |
    | send_poll | Create a poll in a chat |
    | send_reaction | React to a message with emoji |

    ## REST API Quick Start
    POST /api/v1/send { "chatId": "...", "text": "..." }
    GET  /api/v1/messages?chatId=...&limit=20
    GET  /api/v1/search?query=marketing
    GET  /api/v1/directory?type=contact

    ## Category Files
    (same table as before, linking to skills/*.md)
    ```

    **Category files (skills/*.md) update:**
    For each skills/*.md file:
    - Replace any OpenClaw-specific syntax (`Action: send | Target: "group" | Parameters: {...}`) with REST API equivalents (`POST /api/v1/send { "chatId": "...", "text": "..." }`)
    - Add MCP tool name at the top of each relevant section (e.g., "MCP tool: `send_message`")
    - Keep the action tables and gotchas — they document WAHA quirks that are still valid
    - Remove references to "gateway", "OpenClaw", "plugin SDK"
    - Keep chatId/JID format documentation (still needed for REST API callers)

    **Preserve:**
    - All WAHA quirk documentation (poll wrapper, reaction messageId format, etc.)
    - JID format explanations (@c.us, @g.us, @newsletter)
    - Media handling notes
    - Group/channel management details

    **Remove:**
    - `Action: X | Target: Y | Parameters: Z` syntax (replace with REST/MCP equivalents)
    - References to "OpenClaw gateway", "plugin SDK", "handleAction"
    - `listActions()` references
    - Any mention of channel.ts dispatch
  </action>
  <verify>
    <automated>cd D:\docker\waha-oc-plugin && grep -c "openclaw\|OpenClaw\|plugin-sdk\|gateway" SKILL.md skills/*.md 2>/dev/null | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "version: 4.0.0" SKILL.md returns a match
    - grep -i "openclaw" SKILL.md returns zero matches
    - grep "/api/v1/" SKILL.md returns at least 3 matches (REST examples)
    - grep "send_message" SKILL.md returns at least 1 match (MCP tool reference)
    - grep "ctl_" SKILL.md returns at least 1 match (API key format)
    - grep -ri "openclaw\|plugin.sdk\|gateway" skills/*.md returns zero matches (all category files cleaned)
  </acceptance_criteria>
  <done>
    SKILL.md v4.0.0 is framework-agnostic. References Chatlytics API key (ctl_), MCP endpoint (/mcp), REST API (/api/v1/), and MCP tools (send_message, etc.). Zero OpenClaw-specific syntax remains in SKILL.md or skills/*.md.
  </done>
</task>

</tasks>

<verification>
- `grep "version: 4.0.0" SKILL.md` confirms version
- `grep -rci "openclaw\|plugin-sdk" SKILL.md skills/*.md | grep -v ":0$"` returns nothing (zero OC references)
- `grep "/api/v1/" SKILL.md` shows REST API examples
- `grep "mcpServers" SKILL.md` shows MCP config snippet
</verification>

<success_criteria>
- SKILL.md v4.0.0 has no OpenClaw-specific instructions (SKILL-01)
- REST API and MCP tool references are present and accurate
- MCP config snippet is copy-paste ready
- All skills/*.md category files updated to match
</success_criteria>

<output>
After completion, create `.planning/phases/65-admin-standalone/65-02-SUMMARY.md`
</output>
