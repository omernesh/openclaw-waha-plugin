---
phase: 65-admin-standalone
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/site/index.html
  - docs/site/docs.html
autonomous: true
requirements: [SITE-01, SITE-02]

must_haves:
  truths:
    - "Landing page has product overview, feature list, and getting started link"
    - "Docs page has interactive API examples with copy buttons"
    - "Docs page has copy-paste MCP config snippets for Claude Desktop, Cursor, Continue"
    - "Both pages are self-contained HTML files with no build step required"
  artifacts:
    - path: "docs/site/index.html"
      provides: "chatlytics.ai landing page"
      min_lines: 100
      contains: "Chatlytics"
    - path: "docs/site/docs.html"
      provides: "API documentation with interactive examples"
      min_lines: 150
      contains: "/api/v1/"
  key_links:
    - from: "docs/site/index.html"
      to: "docs/site/docs.html"
      via: "href link from landing page to docs"
      pattern: "docs.html"
    - from: "docs/site/docs.html"
      to: "MCP config"
      via: "copy-paste config snippet"
      pattern: "mcpServers"
---

<objective>
Create the chatlytics.ai landing page and API documentation site.

Purpose: SITE-01 provides a public product overview at chatlytics.ai. SITE-02 provides developer documentation with interactive API examples and copy-paste MCP config. Both are static HTML files — deployment to chatlytics.ai is out of scope (just create the deliverables).
Output: docs/site/index.html (landing page) + docs/site/docs.html (API docs).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/65-admin-standalone/65-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create landing page and API docs site</name>
  <read_first>
    - docs/PRD-v2.md (first 50 lines — product description for landing page copy)
    - src/openapi.yaml (if exists, for accurate endpoint documentation) OR src/api-routes.ts (for endpoint paths)
    - src/mcp-server.ts (lines 1-100 for MCP tool names and descriptions)
  </read_first>
  <files>
    docs/site/index.html
    docs/site/docs.html
  </files>
  <action>
    **Create `docs/site/` directory** (mkdir -p docs/site).

    **docs/site/index.html — Landing page (SITE-01):**

    Self-contained HTML with inline CSS (Tailwind CDN via `<script src="https://cdn.tailwindcss.com">`). No build step. Dark theme with blue/indigo accent colors. Professional SaaS landing page feel.

    Sections:
    1. **Hero**: "Chatlytics" heading + "WhatsApp for AI Agents" subheading. One-liner: "Connect any AI agent to WhatsApp in minutes. MCP server, REST API, CLI tool — all in one Docker container." CTA button: "Get Started" linking to docs.html.
    2. **Features grid** (3-column on desktop, stacked on mobile):
       - MCP Server: "Connect Claude, Cursor, or any MCP client with a single config snippet"
       - REST API: "Send messages, read chats, manage groups — all via authenticated HTTP endpoints"
       - CLI Tool: "npx chatlytics send 'hello' --to 'John' — WhatsApp from your terminal"
       - Multi-Tenant: "Isolated workspaces per customer — crash one, others keep running"
       - Mimicry Engine: "Human-like timing, typing indicators, quiet hours — avoid bans"
       - Docker Ready: "docker compose up and you're live. SQLite data persists via volumes"
    3. **Quick start**: Docker compose snippet (3 lines: docker compose up), then MCP config JSON, then REST curl example.
    4. **Footer**: "Built by Omer Nesher" + GitHub link + link to docs.html.

    **docs/site/docs.html — API documentation (SITE-02):**

    Self-contained HTML with inline CSS (Tailwind CDN). Dark theme matching landing page.

    Sections:
    1. **Navigation sidebar** (fixed left on desktop, hamburger on mobile): Authentication, REST API, MCP Server, CLI, Webhooks, Configuration.
    2. **Authentication**: API key format (ctl_xxx), Bearer header, where to get keys (admin panel > API Keys tab).
    3. **REST API**: For each endpoint group (Send, Messages, Search, Directory, Sessions, Status), show:
       - Method + path
       - Request body (JSON with syntax highlighting via `<pre><code>`)
       - Response example
       - **Copy button** on each code block (vanilla JS: `navigator.clipboard.writeText()` with fallback, button text changes to "Copied!" for 2 seconds)
    4. **MCP Server**: Config snippets for Claude Desktop, Cursor, and Continue.dev — each with a copy button. List all 10 MCP tools with descriptions.
    5. **CLI**: Installation (`npx chatlytics`), env vars (CHATLYTICS_URL, CHATLYTICS_API_KEY), subcommand examples (send, search, status, read).
    6. **Webhooks**: Callback URL config, HMAC signature verification example (Node.js code), retry behavior.
    7. **Section anchors** for deep-linking (#authentication, #rest-api, #mcp, #cli, #webhooks).

    **Copy button implementation** (vanilla JS, no React):
    ```javascript
    document.querySelectorAll('.copy-btn').forEach(btn => {
      btn.addEventListener('click', () => {
        const code = btn.closest('.code-block').querySelector('code').textContent;
        navigator.clipboard.writeText(code).then(() => {
          btn.textContent = 'Copied!';
          setTimeout(() => { btn.textContent = 'Copy'; }, 2000);
        });
      });
    });
    ```

    **IMPORTANT**: These files go in `docs/site/`, NOT in `dist/admin/`. They are NOT served by the Chatlytics process — they are deployment artifacts for chatlytics.ai (Research Pitfall 5).
  </action>
  <verify>
    <automated>cd D:\docker\waha-oc-plugin && test -f docs/site/index.html && test -f docs/site/docs.html && grep -c "Chatlytics" docs/site/index.html && grep -c "/api/v1/" docs/site/docs.html</automated>
  </verify>
  <acceptance_criteria>
    - docs/site/index.html exists and contains "Chatlytics" (landing page title)
    - docs/site/index.html contains "docs.html" (link to docs)
    - docs/site/index.html contains "docker" (getting started section)
    - docs/site/docs.html exists and contains "/api/v1/" (REST API examples)
    - docs/site/docs.html contains "mcpServers" (MCP config snippet)
    - docs/site/docs.html contains "clipboard" (copy button implementation)
    - docs/site/docs.html contains "ctl_" (API key format)
    - Neither file is in dist/admin/ (not served by Chatlytics process)
  </acceptance_criteria>
  <done>
    Landing page at docs/site/index.html has product overview, feature grid, quick start, and link to docs. Docs page at docs/site/docs.html has interactive API examples with copy buttons, MCP config snippets, and section anchors for deep-linking. Both are self-contained HTML with Tailwind CDN.
  </done>
</task>

</tasks>

<verification>
- `ls docs/site/index.html docs/site/docs.html` — both files exist
- `grep "Chatlytics" docs/site/index.html` — landing page has product name
- `grep "/api/v1/" docs/site/docs.html` — docs has API examples
- `grep "mcpServers" docs/site/docs.html` — docs has MCP config
- `grep "clipboard" docs/site/docs.html` — docs has copy functionality
</verification>

<success_criteria>
- chatlytics.ai landing page has product overview, feature list, and getting started link (SITE-01)
- API documentation has interactive examples and copy-paste MCP config snippets (SITE-02)
- Both pages are self-contained HTML — no build step, no framework dependency
- Pages are in docs/site/ not dist/admin/ (correct deployment location)
</success_criteria>

<output>
After completion, create `.planning/phases/65-admin-standalone/65-03-SUMMARY.md`
</output>
