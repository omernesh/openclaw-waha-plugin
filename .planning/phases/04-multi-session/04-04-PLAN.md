---
phase: 04-multi-session
plan: 04
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/monitor.ts
autonomous: false
requirements:
  - MSESS-04

must_haves:
  truths:
    - "Admin panel has a Sessions tab visible alongside existing tabs"
    - "Sessions tab shows all registered sessions with name, sessionId, role, subRole"
    - "Sessions tab shows connection status (healthy/unhealthy) from health monitor"
    - "Sessions tab is read-only (no inline editing of roles)"
  artifacts:
    - path: "src/monitor.ts"
      provides: "Sessions tab HTML/JS in admin panel, enhanced /api/admin/sessions endpoint"
      contains: "Sessions"
  key_links:
    - from: "src/monitor.ts (Sessions tab)"
      to: "/api/admin/sessions"
      via: "fetch call to load session data"
      pattern: "api/admin/sessions"
    - from: "src/monitor.ts (Sessions tab)"
      to: "src/health.ts"
      via: "getHealthState() for connection status"
      pattern: "getHealthState"
---

<objective>
Add a Sessions tab to the admin panel showing all registered sessions with their roles, sub-roles, and live connection status.

Purpose: Gives the user visibility into which sessions are configured, their permissions (role/subRole), and whether they're connected to WhatsApp.
Output: New Sessions tab in the admin panel with session data display.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-multi-session/04-CONTEXT.md
@.planning/phases/04-multi-session/04-RESEARCH.md
@.planning/phases/04-multi-session/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — ResolvedWahaAccount with role/subRole -->

From src/accounts.ts (after Plan 01):
```typescript
export type ResolvedWahaAccount = {
  accountId: string;
  enabled: boolean;
  name?: string;
  baseUrl: string;
  apiKey: string;
  apiKeySource: string;
  session: string;
  config: WahaAccountConfig;
  role: string;     // "bot" | "human" etc.
  subRole: string;  // "full-access" | "listener" etc.
};

export function listEnabledWahaAccounts(cfg: CoreConfig): ResolvedWahaAccount[];
```

From src/health.ts:
```typescript
export function getHealthState(): { sessions: Record<string, { healthy: boolean; lastCheck: number; consecutiveFailures: number }> };
```

From src/monitor.ts — existing admin panel tab pattern:
The admin panel is embedded HTML/JS in monitor.ts. Tabs are rendered as `<button>` elements that toggle `<div>` sections. The existing tabs are: Directory, Config, Filter Stats, Status.

Existing /api/admin/sessions endpoint returns WAHA session status. It needs to be enhanced to also return role/subRole from config.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance /api/admin/sessions endpoint to include role/subRole data</name>
  <files>src/monitor.ts</files>
  <action>
1. Find the existing `GET /api/admin/sessions` handler in monitor.ts.

2. Enhance the response to include role and subRole information from the account config. The response should merge:
   - Session status from WAHA API (existing behavior)
   - Role/subRole from `listEnabledWahaAccounts(cfg)`
   - Health state from `getHealthState()`

   The enhanced response format per session:
   ```json
   {
     "sessionId": "3cf11776_logan",
     "name": "Sammie",
     "role": "bot",
     "subRole": "full-access",
     "healthy": true,
     "consecutiveFailures": 0,
     "lastCheck": 1710000000000,
     "wahaStatus": "ONLINE"
   }
   ```

3. Import `listEnabledWahaAccounts` from `./accounts.js` if not already imported. Import `getHealthState` from `./health.js` if not already imported.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>GET /api/admin/sessions returns session data including role, subRole, health status, and WAHA connection status.</done>
</task>

<task type="auto">
  <name>Task 2: Add Sessions tab to admin panel HTML/JS</name>
  <files>src/monitor.ts</files>
  <action>
1. Add a "Sessions" tab button to the tab bar in the admin panel HTML. Follow the existing tab pattern (look for the other tab buttons like "Directory", "Config", "Filter Stats", "Status").

2. Add a "Sessions" tab content div with the same pattern as other tabs. The content should:
   - Fetch data from `/api/admin/sessions` on tab activation
   - Display a card/table for each session showing:
     - Session name (or sessionId if no name)
     - Session ID
     - Role badge (e.g., "bot" in blue, "human" in green)
     - Sub-role badge (e.g., "full-access" in green, "listener" in yellow)
     - Connection status dot (green for healthy, red for unhealthy, grey for unknown)
     - Last health check timestamp
   - Style consistently with existing admin panel tabs (use same CSS classes/patterns)
   - Note: Read-only — no editing controls per user decision (role changes via config API)

3. Add the tab switching logic for the Sessions tab, following the exact pattern used by the other tabs.

4. Add a small note at the bottom of the Sessions tab: "Roles are configured via the Config tab or config API. This view is read-only."

5. Use the existing admin panel CSS classes and styling patterns. Do NOT add a separate stylesheet — keep it inline as per the established pattern in monitor.ts.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Sessions tab renders in admin panel showing all sessions with name, sessionId, role, subRole, and connection status. Styled consistently with existing tabs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Sessions tab in admin panel</name>
  <files>src/monitor.ts</files>
  <action>
Deploy to hpg6 (both locations) and restart gateway. Open admin panel and verify the Sessions tab works correctly.
  </action>
  <verify>Manual browser verification</verify>
  <done>Sessions tab renders correctly with all session data, role badges, and connection status dots. Other tabs unaffected.</done>
  <what-built>Sessions tab in admin panel showing registered sessions with roles, sub-roles, and connection status</what-built>
  <how-to-verify>
    1. Deploy to hpg6 (both locations) and restart gateway
    2. Open admin panel in browser (the webhook URL with /admin path)
    3. Verify "Sessions" tab appears alongside existing tabs (Directory, Config, Filter Stats, Status)
    4. Click Sessions tab — should show both sessions:
       - 3cf11776_logan (Sammie) — role: bot, subRole: full-access, status: green dot
       - 3cf11776_omer (Omer) — role: human, subRole: listener, status: should show connection state
    5. Verify no editing controls are present (read-only)
    6. Verify other tabs still work correctly
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — no type errors
- Admin panel renders Sessions tab with correct data
- Other admin panel tabs unaffected
</verification>

<success_criteria>
- Sessions tab visible in admin panel tab bar
- Shows all registered sessions with name, sessionId, role, subRole
- Shows connection status from health monitor (green/red dot)
- Read-only (no editing controls)
- Styled consistently with existing tabs
- Other tabs unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-session/04-04-SUMMARY.md`
</output>
