---
phase: 58-sdk-decoupling
plan: 03
type: execute
wave: 2
depends_on: ["58-01"]
files_modified:
  - src/inbound.ts
  - src/inbound.test.ts
autonomous: true
requirements: [CORE-01]

must_haves:
  truths:
    - "inbound.ts has zero openclaw/plugin-sdk imports"
    - "inbound.test.ts has zero openclaw/plugin-sdk mocks"
    - "DM security gate behavior is preserved (DMs are correctly allowed/blocked)"
    - "Group policy resolution behavior is preserved (group messages correctly filtered)"
    - "Outbound reply delivery behavior is preserved (bot replies reach WhatsApp)"
    - "All 683 tests still pass — zero regressions in inbound test suite"
  artifacts:
    - path: "src/inbound.ts"
      provides: "Webhook inbound handler, DM filter, group policy, reply delivery"
    - path: "src/inbound.test.ts"
      provides: "Inbound handler test suite"
  key_links:
    - from: "src/inbound.ts"
      to: "src/platform-types.js"
      via: "import type { RuntimeEnv, OutboundReplyPayload }"
      pattern: "from.*platform-types"
    - from: "src/inbound.ts"
      to: "src/account-utils.js"
      via: "import { normalizeAccountId }"
      pattern: "from.*account-utils"
    - from: "src/inbound.ts"
      to: "local shim functions"
      via: "inline implementations replacing SDK business logic"
      pattern: "resolveAllowlistProviderRuntimeGroupPolicy|resolveDmGroupAccessWithCommandGate"
---

<objective>
Remove all 15 openclaw/plugin-sdk symbol imports from inbound.ts and update inbound.test.ts to remove SDK mocks. This is the highest-risk file — it imports 8 SDK modules with several carrying real business logic.

Purpose: inbound.ts is the last non-channel file with SDK imports. Completing this makes the codebase standalone-ready (CORE-01 fully satisfied when combined with Plans 01 and 02).
Output: inbound.ts and inbound.test.ts SDK-free, all inbound message handling behavior preserved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/58-sdk-decoupling/58-RESEARCH.md
@.planning/phases/58-sdk-decoupling/58-01-SUMMARY.md
@src/inbound.ts
@src/inbound.test.ts

<interfaces>
<!-- From Plan 01 outputs -->

From src/platform-types.ts:
```typescript
export type RuntimeEnv = { ... };
export type OutboundReplyPayload = { ... };
export function isWhatsAppGroupJid(jid: string): boolean;
```

From src/account-utils.ts:
```typescript
export function normalizeAccountId(id: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inbound.ts SDK imports with local shims</name>
  <files>src/inbound.ts</files>
  <read_first>
    src/inbound.ts (FULL FILE — read in chunks of 200 lines to understand all SDK symbol usage sites)
    src/platform-types.ts (created by Plan 01)
    src/account-utils.ts (created by Plan 01)
  </read_first>
  <action>
    Replace all 8 SDK import lines (lines 1-19) with local imports and inline shims. Work through each symbol group:

    **Group 1: Simple type/utility replacements (LOW risk)**

    - `isWhatsAppGroupJid` -> `import { isWhatsAppGroupJid } from "./platform-types.js"`
    - `normalizeAccountId` -> `import { normalizeAccountId } from "./account-utils.js"`
    - `type OpenClawConfig` -> use `CoreConfig` from `./types.js` directly (it already extends OpenClawConfig, so any place using OpenClawConfig can use CoreConfig)
    - `type RuntimeEnv` -> `import type { RuntimeEnv } from "./platform-types.js"`
    - `type OutboundReplyPayload` -> `import type { OutboundReplyPayload } from "./platform-types.js"`

    **Group 2: Simple function replacements (LOW-MEDIUM risk)**

    - `GROUP_POLICY_BLOCKED_LABEL` -> inline as `const GROUP_POLICY_BLOCKED_LABEL = "blocked";`
      Verify actual value: `ssh omer@100.114.126.43 'node -e "const m = require(\"/usr/lib/node_modules/openclaw/dist/config-runtime.js\"); console.log(m.GROUP_POLICY_BLOCKED_LABEL)"'`

    - `logInboundDrop(runtime, reason, details)` -> inline:
      ```typescript
      function logInboundDrop(runtime: RuntimeEnv, reason: string, details?: Record<string, unknown>): void {
        runtime.log?.(`[inbound-drop] ${reason}${details ? " " + JSON.stringify(details) : ""}`);
      }
      ```
      Read all callsites in inbound.ts to verify the signature and behavior.

    - `createReplyPrefixOptions(config)` -> read callsite in inbound.ts. Likely returns `{ prefix?: string }` from config. Inline based on actual usage.

    - `warnMissingProviderGroupPolicyFallbackOnce()` -> inline as a one-shot warning:
      ```typescript
      let _warnedMissingPolicy = false;
      function warnMissingProviderGroupPolicyFallbackOnce(): void {
        if (!_warnedMissingPolicy) {
          _warnedMissingPolicy = true;
          log.warn("Group policy provider not configured — using default policy");
        }
      }
      ```

    **Group 3: Business logic replacements (HIGH risk — read SDK source first)**

    Before implementing these, read the SDK source on hpg6:
    ```bash
    ssh omer@100.114.126.43 'cat /usr/lib/node_modules/openclaw/dist/config-runtime.js' > /tmp/sdk-config-runtime.js
    ssh omer@100.114.126.43 'cat /usr/lib/node_modules/openclaw/dist/reply-payload.js' > /tmp/sdk-reply-payload.js
    ssh omer@100.114.126.43 'cat /usr/lib/node_modules/openclaw/dist/security-runtime.js' > /tmp/sdk-security-runtime.js
    ```

    - `resolveDefaultGroupPolicy(config)` -> read SDK source. Likely reads `config.channels?.waha?.groupPolicy` and returns the default. Inline.

    - `resolveAllowlistProviderRuntimeGroupPolicy(config, accountId, groupJid)` -> read SDK source. Resolves group policy from allowlist provider. This is the group filtering logic. Read all callsites in inbound.ts to understand what return values are checked against.

    - `createNormalizedOutboundDeliverer(sendFn)` -> read SDK source. Likely wraps a send function with normalization. Read the callsite in inbound.ts `deliverWahaReply` to understand exactly how it's used.

    - `formatTextWithAttachmentLinks(payload)` -> read SDK source. Formats text with attachment URLs appended. Read callsite in inbound.ts.

    - `resolveOutboundMediaUrls(payload)` -> read SDK source. Extracts media URLs from payload. Read callsite in inbound.ts.

    - `readStoreAllowFromForDmPolicy(runtime, channel, accountId)` -> read SDK source AND the existing local shim `createScopedPairingAccess` at line ~63 of inbound.ts. This reads the pairing store allow-from list.

    - `resolveDmGroupAccessWithCommandGate(params)` -> read SDK source. This is the core DM security gate. It determines whether a DM message should be accepted, blocked, or requires pairing. Read ALL callsites in inbound.ts (there are ~10 test cases exercising it). The local implementation must match the exact return values checked in the code.

    **Implementation order for Group 3:**
    1. Read all SDK sources from hpg6
    2. Read all callsites in inbound.ts for each function
    3. Implement each shim, adding a comment: `// Phase 58: Local shim — replaces openclaw/plugin-sdk/{module}. DO NOT CHANGE.`
    4. Run tests after each individual function replacement if possible

    After ALL replacements:
    1. `npx tsc --noEmit`
    2. `npm test -- --reporter=dot`
    3. Specifically: `npm test -- --reporter=verbose src/inbound.test.ts` to verify all inbound tests pass with detailed output
  </action>
  <verify>
    <automated>cd D:/docker/waha-oc-plugin && grep "openclaw/plugin-sdk" src/inbound.ts && echo "FAIL" || echo "PASS" && npm test -- --reporter=dot src/inbound.test.ts 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "openclaw/plugin-sdk" src/inbound.ts` returns zero results
    - `grep "platform-types" src/inbound.ts` returns at least 1 result
    - `grep "account-utils" src/inbound.ts` returns at least 1 result
    - `npm test -- --reporter=dot src/inbound.test.ts` shows all inbound tests passing
    - `grep "GROUP_POLICY_BLOCKED_LABEL" src/inbound.ts` returns at least 1 result (constant preserved)
    - `grep "resolveDmGroupAccessWithCommandGate\|Phase 58" src/inbound.ts` returns at least 2 results (shim exists with marker)
  </acceptance_criteria>
  <done>inbound.ts has zero SDK imports. All 15 SDK symbols replaced with local implementations. All inbound tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update inbound.test.ts SDK mocks + final verification</name>
  <files>src/inbound.test.ts</files>
  <read_first>
    src/inbound.test.ts (FULL FILE — read in chunks, focus on lines 130-200 for mock blocks and lines 390-650 for DM gate tests)
    src/inbound.ts (the updated file from Task 1 — to confirm which local modules the tests should mock)
  </read_first>
  <action>
    **Remove ALL `vi.mock("openclaw/plugin-sdk*")` blocks** from inbound.test.ts. There are approximately 10 mock blocks:

    1. `vi.mock("openclaw/plugin-sdk", ...)` (line ~136)
    2. `vi.mock("openclaw/plugin-sdk/config-runtime", ...)` (line ~154)
    3. `vi.mock("openclaw/plugin-sdk/reply-payload", ...)` (line ~161)
    4. `vi.mock("openclaw/plugin-sdk/channel-runtime", ...)` (line ~167)
    5. `vi.mock("openclaw/plugin-sdk/channel-inbound", ...)` (line ~171)
    6. `vi.mock("openclaw/plugin-sdk/security-runtime", ...)` (line ~175)
    7. `vi.mock("openclaw/plugin-sdk/whatsapp-shared", ...)` (line ~180)
    8. `vi.mock("openclaw/plugin-sdk/account-id", ...)` (line ~184)
    9. `vi.mock("openclaw/plugin-sdk/account-resolution", ...)` (line ~189)
    10. `vi.mock("openclaw/plugin-sdk/secret-input", ...)` (line ~196)

    For each removed mock, determine if the test needs a mock for the LOCAL replacement module instead:

    - If tests call `vi.mocked(resolveDmGroupAccessWithCommandGate)` to control its return value, add `vi.mock("./inbound.js", ...)` partial mock or refactor to mock the local shim. The approach depends on whether the shim is inline in inbound.ts (not mockable) or in a separate module.

    - If the shim is inline in inbound.ts, the test must exercise the real shim logic. Update test assertions to match the local shim behavior (which must match the SDK behavior per Task 1).

    - For the DM gate tests (lines ~390-650), these import `resolveDmGroupAccessWithCommandGate` dynamically. If the function is now inline in inbound.ts, these tests need to be restructured to test behavior (send a DM message, verify it's allowed/blocked) rather than mocking the gate function.

    **Also update** any `await import("openclaw/plugin-sdk/security-runtime")` dynamic imports in test assertions to reference the local module.

    After ALL changes:
    1. `npm test -- --reporter=verbose src/inbound.test.ts` — all tests must pass
    2. `npm test -- --reporter=dot` — full suite must pass
    3. Final phase-level check: `grep -rn 'from "openclaw/plugin-sdk' src/ --include="*.ts" | grep -v ".bak" | grep -v "src/channel.ts" | grep -v "index.ts"` — must return zero results (matches actual import statements, not comments)
  </action>
  <verify>
    <automated>cd D:/docker/waha-oc-plugin && grep "openclaw/plugin-sdk" src/inbound.test.ts && echo "FAIL" || echo "PASS" && npm test -- --reporter=dot 2>&1 | tail -5 && echo "--- Final SDK import check ---" && grep -rn 'from "openclaw/plugin-sdk' src/ --include="*.ts" | grep -v ".bak" | grep -v "src/channel.ts" | grep -v "index.ts" || echo "PHASE COMPLETE: zero SDK imports outside channel.ts/index.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "openclaw/plugin-sdk" src/inbound.test.ts` returns zero results
    - `npm test -- --reporter=dot` shows all tests passing (683 passed)
    - `grep -rn 'from "openclaw/plugin-sdk' src/ --include="*.ts" | grep -v ".bak" | grep -v "src/channel.ts" | grep -v "index.ts"` returns zero results (phase success criterion — matches actual imports, not comments. config-schema.ts has a comment mentioning the SDK but no actual import, so it correctly passes this check)
    - No test file imports from SDK modules
  </acceptance_criteria>
  <done>inbound.test.ts has zero SDK mocks. Full test suite passes. Phase 58 success criterion met: zero SDK imports outside channel.ts and index.ts.</done>
</task>

</tasks>

<verification>
- `grep -rn 'from "openclaw/plugin-sdk' src/ --include="*.ts" | grep -v ".bak" | grep -v "src/channel.ts" | grep -v "index.ts"` returns ZERO lines (matches actual imports only — config-schema.ts comment is not a false positive)
- `npm test -- --reporter=dot` shows 683 tests passed
- `npm test -- --reporter=verbose src/inbound.test.ts` shows all inbound tests passing
- DM security gate tests pass (DMs correctly allowed/blocked)
- Group policy tests pass (group messages correctly filtered)
</verification>

<success_criteria>
inbound.ts and inbound.test.ts are completely SDK-free. All business logic behavior is preserved via local shims. Full test suite green. Phase 58 CORE-01 is fully satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/58-sdk-decoupling/58-03-SUMMARY.md`
</output>
