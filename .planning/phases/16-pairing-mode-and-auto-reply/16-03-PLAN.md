---
phase: 16-pairing-mode-and-auto-reply
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - src/monitor.ts
autonomous: true
requirements: [PAIR-04, PAIR-05, REPLY-04]

must_haves:
  truths:
    - "Admin panel Settings tab has a Pairing Mode section with enable toggle, passcode display, TTL dropdown, challenge message editor, and wa.me link generator"
    - "Admin panel Settings tab has an Auto-Reply section with enable toggle, message editor with template variable hints, and interval dropdown"
    - "Admin panel Directory tab shows 'Source: Pairing' badge on pairing-granted contacts alongside TTL badges"
    - "Admin panel has a revoke button on pairing grants that removes the grant and refreshes the view"
  artifacts:
    - path: "src/monitor.ts"
      provides: "Pairing Mode and Auto-Reply UI sections in Settings, pairing source badges in Directory, revoke API endpoint"
      contains: "Pairing Mode"
  key_links:
    - from: "src/monitor.ts"
      to: "src/directory.ts"
      via: "getPairingGrants and revokePairingGrant calls"
      pattern: "getPairingGrants"
    - from: "src/monitor.ts"
      to: "POST /api/admin/config"
      via: "Config save includes pairingMode and autoReply fields"
      pattern: "pairingMode"
---

<objective>
Add admin panel UI for pairing mode configuration (Settings tab), auto-reply configuration (Settings tab), pairing grant visibility with revoke (Directory tab), and the API endpoint for revoking pairing grants.

Purpose: Enables admins to configure pairing mode and auto-reply, view active pairing grants, and revoke access -- all from the web UI.
Output: Updated src/monitor.ts with new UI sections and API routes
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-pairing-mode-and-auto-reply/16-01-PLAN.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/directory.ts (modified in 16-01):
```typescript
getPairingGrants(): Array<{ jid: string; expiresAt: number | null; grantedAt: number | null; source: string }>;
revokePairingGrant(jid: string): void;
```

From src/config-schema.ts (modified in 16-01):
```typescript
pairingMode: z.object({
  enabled: z.boolean().optional().default(false),
  passcode: z.string().optional(),
  grantTtlMinutes: z.number().int().positive().optional().default(1440),
  challengeMessage: z.string().optional().default("Welcome! Please enter the 6-digit passcode to get started."),
  hmacSecret: z.string().optional(),
}).optional().default({}),

autoReply: z.object({
  enabled: z.boolean().optional().default(false),
  message: z.string().optional().default("Hey! Thanks for reaching out..."),
  intervalMinutes: z.number().int().min(0).optional().default(1440),
}).optional().default({}),
```

Existing monitor.ts patterns:
- Settings tab Access Control section starts around line 595
- `setChk` / `getChk` for checkbox binding
- `showToast` for feedback
- All JS embedded in template literal (double-escape backslashes!)
- `<details><summary>` for collapsible sections
- Config save via POST /api/admin/config with `{"waha": {...}}` wrapper
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pairing Mode and Auto-Reply sections to Settings tab</name>
  <files>src/monitor.ts</files>
  <read_first>src/monitor.ts</read_first>
  <action>
**CRITICAL: All embedded JS/HTML in monitor.ts is inside a backtick template literal. ALL backslashes in regex and strings MUST be double-escaped (\\\\w not \\w, \\\\' not \\').**

**Step 1: Add Pairing Mode section to Settings tab HTML.**

Find the Access Control `<details>` section (around line 595). AFTER the Access Control section's closing `</details>`, add a new collapsible section:

```html
<details>
  <summary>Pairing Mode</summary>
  <div class="card" style="margin-top:8px">
    <div style="display:flex;align-items:center;gap:8px;margin-bottom:12px">
      <label>Enable Pairing Mode</label>
      <label class="toggle" style="margin-left:auto"><input type="checkbox" id="pairingEnabled" name="pairingEnabled"><span class="slider"></span></label>
    </div>
    <div id="pairingFields" style="display:none">
      <div style="margin-bottom:8px">
        <label>Passcode (6 digits)</label>
        <div style="display:flex;gap:8px">
          <input type="text" id="pairingPasscode" name="pairingPasscode" maxlength="6" pattern="[0-9]{6}" placeholder="123456" style="flex:1;font-family:monospace;font-size:1.1em;letter-spacing:2px">
          <button type="button" id="generatePasscode" class="btn btn-sm">Generate</button>
        </div>
      </div>
      <div style="margin-bottom:8px">
        <label>Grant TTL</label>
        <select id="pairingGrantTtl" name="pairingGrantTtl">
          <option value="60">1 hour</option>
          <option value="360">6 hours</option>
          <option value="1440" selected>24 hours</option>
          <option value="10080">7 days</option>
          <option value="43200">30 days</option>
          <option value="0">Never expires</option>
        </select>
      </div>
      <div style="margin-bottom:8px">
        <label>Challenge Message</label>
        <textarea id="pairingChallengeMsg" name="pairingChallengeMsg" rows="2" style="width:100%"></textarea>
      </div>
      <div style="margin-bottom:8px">
        <label>wa.me Deep Link</label>
        <div style="display:flex;gap:8px;align-items:center">
          <input type="text" id="pairingDeepLink" readonly style="flex:1;font-family:monospace;font-size:0.85em;background:#f5f5f5">
          <button type="button" id="copyDeepLink" class="btn btn-sm">Copy</button>
        </div>
        <small style="color:#666">Share this link for zero-friction authorization (JID-specific token generated server-side)</small>
      </div>
    </div>
  </div>
</details>
```

**Step 2: Add Auto-Reply section after Pairing Mode:**

```html
<details>
  <summary>Auto-Reply</summary>
  <div class="card" style="margin-top:8px">
    <div style="display:flex;align-items:center;gap:8px;margin-bottom:12px">
      <label>Send rejection message to unauthorized DMs</label>
      <label class="toggle" style="margin-left:auto"><input type="checkbox" id="autoReplyEnabled" name="autoReplyEnabled"><span class="slider"></span></label>
    </div>
    <div id="autoReplyFields" style="display:none">
      <div style="margin-bottom:8px">
        <label>Rejection Message</label>
        <textarea id="autoReplyMessage" name="autoReplyMessage" rows="3" style="width:100%"></textarea>
        <small style="color:#666">Template variables: {admin_name}</small>
      </div>
      <div style="margin-bottom:8px">
        <label>Rate Limit (minutes between replies to same contact)</label>
        <select id="autoReplyInterval" name="autoReplyInterval">
          <option value="60">1 hour</option>
          <option value="360">6 hours</option>
          <option value="1440" selected>24 hours</option>
          <option value="10080">7 days</option>
        </select>
      </div>
    </div>
  </div>
</details>
```

**Step 3: Add JavaScript for pairing/auto-reply UI logic.**

In the embedded script section (where `setChk`, `getChk`, config load/save live), add:

**Config load (in the function that populates form fields from config, near `setChk('canInitiateGlobal', ...)`):**
```javascript
// Phase 16: Pairing Mode config load
var pm = w.pairingMode || {};
setChk('pairingEnabled', pm.enabled === true);
document.getElementById('pairingPasscode').value = pm.passcode || '';
document.getElementById('pairingGrantTtl').value = String(pm.grantTtlMinutes || 1440);
document.getElementById('pairingChallengeMsg').value = pm.challengeMessage || 'Welcome! Please enter the 6-digit passcode to get started.';
document.getElementById('pairingFields').style.display = pm.enabled ? '' : 'none';

// Phase 16: Auto-Reply config load
var ar = w.autoReply || {};
setChk('autoReplyEnabled', ar.enabled === true);
document.getElementById('autoReplyMessage').value = ar.message || 'Hey! Thanks for reaching out. Unfortunately, I\\'m not permitted to chat with you right now. Please ask {admin_name} to add you to my allow list.';
document.getElementById('autoReplyInterval').value = String(ar.intervalMinutes || 1440);
document.getElementById('autoReplyFields').style.display = ar.enabled ? '' : 'none';
```

**IMPORTANT:** The `I\\'m` above needs DOUBLE-escaping because we're inside a template literal. The actual string in the template literal should be `I\\\\'m` to produce `I\\'m` in the JS, which produces `I'm` at runtime. Read the existing code carefully for the exact escaping pattern used elsewhere.

**Toggle show/hide for fields:**
```javascript
document.getElementById('pairingEnabled').addEventListener('change', function() {
  document.getElementById('pairingFields').style.display = this.checked ? '' : 'none';
});
document.getElementById('autoReplyEnabled').addEventListener('change', function() {
  document.getElementById('autoReplyFields').style.display = this.checked ? '' : 'none';
});
```

**Generate passcode button:**
```javascript
document.getElementById('generatePasscode').addEventListener('click', function() {
  var code = String(Math.floor(100000 + Math.random() * 900000));
  document.getElementById('pairingPasscode').value = code;
});
```

**Copy deep link button:**
```javascript
document.getElementById('copyDeepLink').addEventListener('click', function() {
  var el = document.getElementById('pairingDeepLink');
  if (el.value) {
    navigator.clipboard.writeText(el.value).then(function() { showToast('Link copied!'); });
  }
});
```

**Config save (in the function that collects form values, near `canInitiateGlobal: getChk('canInitiateGlobal')`):**
```javascript
pairingMode: {
  enabled: getChk('pairingEnabled'),
  passcode: document.getElementById('pairingPasscode').value || undefined,
  grantTtlMinutes: parseInt(document.getElementById('pairingGrantTtl').value) || 1440,
  challengeMessage: document.getElementById('pairingChallengeMsg').value || undefined,
},
autoReply: {
  enabled: getChk('autoReplyEnabled'),
  message: document.getElementById('autoReplyMessage').value || undefined,
  intervalMinutes: parseInt(document.getElementById('autoReplyInterval').value) || 1440,
},
```

Note: Do NOT include hmacSecret in the save payload from frontend -- it's managed server-side only.

**Step 4: Add wa.me deep link generation API route.**

Add a new route in the API routes section of monitor.ts:

```typescript
// Phase 16: Generate wa.me deep link for a specific session+JID
// GET /api/admin/pairing/deeplink?session={sessionId}&jid={jid}
// Returns { link: string } or { error: string }
```

The handler should:
1. Read the session's pairingMode.hmacSecret from config
2. Call `getPairingEngine(accountId, hmacSecret).generateDeepLinkToken(jid)`
3. Extract phone number from JID (strip `@c.us`): e.g., `972544329000@c.us` -> `972544329000`
4. Return `{ link: "https://wa.me/${phone}?text=PAIR-${token}" }`
5. Import `getPairingEngine` from `./pairing.js` at the top of monitor.ts

**Step 5: Add pairing grant revoke API route.**

```typescript
// Phase 16: Revoke a pairing grant
// DELETE /api/admin/pairing/grant/:jid
// Returns { ok: true } or { error: string }
```

The handler should:
1. Call `getDirectoryDb(accountId).revokePairingGrant(jid)`
2. Also remove from config allowFrom array (same pattern as syncExpiredToConfig)
3. Return `{ ok: true }`

**Step 6: Add "Source: Pairing" badge to Directory contact cards.**

In the `buildContactCard` function (or wherever contact cards are rendered in the Directory tab), check if the contact has `source === 'pairing'` in its data. If so, add a small badge:
```html
<span class="badge" style="background:#e3f2fd;color:#1565c0;font-size:0.75em;padding:2px 6px;border-radius:4px;margin-left:4px">Pairing</span>
```

The GET /api/admin/directory endpoint already returns allow_list data. Extend it to include the `source` field from the allow_list table (add `source` to the SELECT query if not already present).

Also add a small "Revoke" link next to the Pairing badge:
```html
<a href="#" onclick="revokePairingGrant('${jid}');return false" style="color:#c62828;font-size:0.75em;margin-left:4px">Revoke</a>
```

Add the `revokePairingGrant` JavaScript function:
```javascript
async function revokePairingGrant(jid) {
  if (!confirm('Revoke pairing access for ' + jid + '?')) return;
  try {
    var r = await fetch('/api/admin/pairing/grant/' + encodeURIComponent(jid), { method: 'DELETE' });
    if (!r.ok) throw new Error('Failed');
    showToast('Pairing access revoked');
    // Refresh the directory listing
    loadContactsTable?.() || location.reload();
  } catch(e) {
    showToast('Failed to revoke: ' + e.message, 'error');
  }
}
```

**DOUBLE-ESCAPING REMINDER:** Every backslash in embedded JS regex/strings must be doubled. Run a mental audit on all added code before writing. Common pitfalls: `\\d`, `\\'`, `\\n` in strings.
  </action>
  <verify>
    <automated>grep -n "Pairing Mode" src/monitor.ts && grep -n "Auto-Reply" src/monitor.ts && grep -n "pairingEnabled\|autoReplyEnabled" src/monitor.ts && grep -n "generatePasscode\|copyDeepLink" src/monitor.ts && grep -n "revokePairingGrant" src/monitor.ts && grep -n "api/admin/pairing" src/monitor.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep "Pairing Mode" src/monitor.ts returns a match (section heading)
    - grep "Auto-Reply" src/monitor.ts returns a match (section heading)
    - grep "pairingEnabled" src/monitor.ts returns matches (checkbox + config load/save)
    - grep "autoReplyEnabled" src/monitor.ts returns matches (checkbox + config load/save)
    - grep "generatePasscode" src/monitor.ts returns a match (generate button handler)
    - grep "copyDeepLink" src/monitor.ts returns a match (copy button handler)
    - grep "revokePairingGrant" src/monitor.ts returns a match (revoke function + API route)
    - grep "api/admin/pairing" src/monitor.ts returns matches (deep link + revoke API routes)
    - grep "pairingMode" in config save block returns a match
    - grep "autoReply" in config save block returns a match
    - grep "Pairing.*badge\|source.*pairing" src/monitor.ts returns a match (directory badge)
    - grep "grantTtlMinutes\|pairingGrantTtl" src/monitor.ts returns matches (TTL dropdown)
    - grep "autoReplyInterval\|intervalMinutes" src/monitor.ts returns matches (rate limit dropdown)
  </acceptance_criteria>
  <done>Admin panel Settings tab has Pairing Mode section (toggle, passcode, TTL, challenge message, deep link generator) and Auto-Reply section (toggle, message editor with template vars, rate limit). Directory shows "Pairing" source badge with Revoke link. API routes handle deep link generation and grant revocation.</done>
</task>

</tasks>

<verification>
Full admin panel verification:
```bash
# Settings tab sections exist
grep -c "Pairing Mode\|Auto-Reply" src/monitor.ts
# Expected: >= 2

# Config round-trip (load + save)
grep -c "pairingMode\|autoReply" src/monitor.ts
# Expected: >= 4 (load + save for each)

# API routes
grep -c "api/admin/pairing" src/monitor.ts
# Expected: >= 2 (deeplink + revoke)
```
</verification>

<success_criteria>
1. Settings tab has a collapsible "Pairing Mode" section with enable toggle, passcode field with Generate button, TTL dropdown, challenge message textarea, and wa.me deep link display with Copy button
2. Settings tab has a collapsible "Auto-Reply" section with enable toggle, rejection message textarea with template variable hints, and rate limit interval dropdown
3. Directory tab shows "Pairing" badge on contacts granted via pairing, with a Revoke link
4. API endpoint generates wa.me deep links for specific JIDs using HMAC tokens
5. API endpoint revokes pairing grants and removes from config allowFrom
6. Config save includes pairingMode and autoReply objects in the {"waha": {...}} payload
</success_criteria>

<output>
After completion, create `.planning/phases/16-pairing-mode-and-auto-reply/16-03-SUMMARY.md`
</output>
