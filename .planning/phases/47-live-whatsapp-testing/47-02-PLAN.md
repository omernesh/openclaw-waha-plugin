---
phase: 47-live-whatsapp-testing
plan: 02
type: execute
wave: 2
depends_on: [47-01]
files_modified: []
autonomous: false
requirements: [TST-01, TST-02, TST-03, TST-04, TST-05, TST-06]

must_haves:
  truths:
    - "/join <invite-link> causes bot to join the group and reply with confirmation (TST-01)"
    - "/join <name> works for exact match; returns candidate list for ambiguous match (TST-02)"
    - "/leave <name> removes bot from group/channel and sends confirmation reply (TST-03)"
    - "/list returns all memberships; /list groups and /list channels return filtered subsets (TST-04)"
    - "Agent LLM can retrieve a group invite link when asked (TST-05)"
    - "Admin UI Leave button and Join by Link input work in browser without errors (TST-06)"
  artifacts:
    - path: "src/commands.ts"
      provides: "slash command handlers"
      contains: "handleSlashCommand"
    - path: "src/inbound.ts"
      provides: "slash command wiring in pipeline"
      contains: "COMMANDS_RE"
    - path: "src/admin/src/components/tabs/directory/GroupsTab.tsx"
      provides: "Leave button per group row"
    - path: "src/admin/src/components/tabs/directory/ChannelsTab.tsx"
      provides: "Unfollow button per channel row"
    - path: "src/admin/src/components/tabs/DirectoryTab.tsx"
      provides: "Join by Link input"
  key_links:
    - from: "WhatsApp /join message"
      to: "handleSlashCommand in commands.ts"
      via: "COMMANDS_RE in inbound.ts"
    - from: "Admin UI Leave button"
      to: "DELETE /api/admin/directory/:jid/leave in monitor.ts"
      via: "api.leaveEntry(jid)"
    - from: "Admin UI Join by Link"
      to: "POST /api/admin/directory/join in monitor.ts"
      via: "api.joinByLink(link)"
---

<objective>
Run all six live test scenarios for v1.18 features via real WhatsApp messages and browser admin panel interaction.

Purpose: Prove all v1.18 features work end-to-end on real WhatsApp. These are acceptance tests — the milestone cannot ship without them passing.
Output: All 6 TST requirements verified; human confirms results at checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-live-whatsapp-testing/47-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test slash commands via WAHA API (TST-01, TST-02, TST-03, TST-04)</name>
  <files></files>
  <action>
    Send test messages from the omer session (3cf11776_omer) to the bot via the test group. After each command, check gateway logs.

    Send helper (run each on hpg6 via SSH):
    ssh omer@100.114.126.43 'curl -s -X POST http://127.0.0.1:3004/api/sendText \
      -H "Content-Type: application/json" \
      -H "X-Api-Key: XcTCX9cn84LE/uMm3SnHEvm0giwtNnHBmGR7OGeAOpA=" \
      -d "{\"chatId\":\"120363421825201386@g.us\",\"text\":\"MESSAGE\",\"session\":\"3cf11776_omer\"}"'

    Log check helper:
    ssh omer@100.114.126.43 'journalctl --user -u openclaw-gateway --since "30 seconds ago" --no-pager | tail -20'

    TST-04 — /list commands (safest, no state change):
    1. Send "/list" — expect formatted list of all groups + channels bot is member of
    2. Send "/list groups" — expect filtered list showing only groups
    3. Send "/list channels" — expect filtered list showing only channels/newsletters

    TST-02 — /join by name:
    4. Send "/join NonExistentGroupXYZ123" — expect no-match error reply
    5. Send "/join " with a partial name that could match multiple groups — expect candidate list
       (use a common word like "test" if there are test groups; otherwise skip if no ambiguous match available)

    TST-01 — /join by invite link:
    Create a disposable test group first, then get its invite code:
    ssh omer@100.114.126.43 'curl -s -X POST http://127.0.0.1:3004/api/groups \
      -H "Content-Type: application/json" \
      -H "X-Api-Key: XcTCX9cn84LE/uMm3SnHEvm0giwtNnHBmGR7OGeAOpA=" \
      -d "{\"name\":\"TST-01 Join Test\",\"participants\":[\"972544329000@c.us\"],\"session\":\"3cf11776_omer\"}"'

    From the returned groupId, get invite code:
    ssh omer@100.114.126.43 'curl -s "http://127.0.0.1:3004/api/groups/GROUP_JID/invite-code?session=3cf11776_omer" \
      -H "X-Api-Key: XcTCX9cn84LE/uMm3SnHEvm0giwtNnHBmGR7OGeAOpA="'

    Then send "/join https://chat.whatsapp.com/CODE" — expect bot joins and replies with confirmation.

    TST-03 — /leave:
    Use the disposable group just joined (TST-01 test group). Send "/leave TST-01 Join Test" — expect bot leaves and sends confirmation.
    If the exact name doesn't match, use the name from /list output.

    After all commands, capture final log excerpt:
    ssh omer@100.114.126.43 'journalctl --user -u openclaw-gateway --since "15 minutes ago" --no-pager | grep -iE "(slash|command|join|leave|list)" | tail -30'
  </action>
  <verify>
    <automated>ssh omer@100.114.126.43 'journalctl --user -u openclaw-gateway --since "15 minutes ago" --no-pager | grep -icE "slash|handleSlashCommand|/list|/join|/leave" || echo "0"'</automated>
  </verify>
  <done>
    - /list returns formatted membership list in WhatsApp (TST-04)
    - /list groups and /list channels return filtered subsets (TST-04)
    - /join by name: no-match returns error reply (TST-02)
    - /join by invite link: bot joins group and sends confirmation (TST-01)
    - /leave by name: bot leaves group and sends confirmation (TST-03)
    - No "Unhandled" or "TypeError" in gateway logs during any test
  </done>
</task>

<task type="auto">
  <name>Task 2: Test invite link retrieval via LLM action (TST-05)</name>
  <files></files>
  <action>
    Test that the LLM agent can retrieve a group invite link via the getInviteCode action.

    Send a natural language message to the bot asking for the invite link:
    ssh omer@100.114.126.43 'curl -s -X POST http://127.0.0.1:3004/api/sendText \
      -H "Content-Type: application/json" \
      -H "X-Api-Key: XcTCX9cn84LE/uMm3SnHEvm0giwtNnHBmGR7OGeAOpA=" \
      -d "{\"chatId\":\"120363421825201386@g.us\",\"text\":\"What is the invite link for this group?\",\"session\":\"3cf11776_omer\"}"'

    Wait 20 seconds for LLM to process and reply, then check logs:
    ssh omer@100.114.126.43 'journalctl --user -u openclaw-gateway --since "3 minutes ago" --no-pager | grep -i "invite\|getInviteCode" | tail -10'

    Also verify the WAHA invite code endpoint works directly (independent of LLM):
    ssh omer@100.114.126.43 'curl -s "http://127.0.0.1:3004/api/groups/120363421825201386@g.us/invite-code?session=3cf11776_logan" \
      -H "X-Api-Key: XcTCX9cn84LE/uMm3SnHEvm0giwtNnHBmGR7OGeAOpA="'

    Expect a JSON response with a "code" field (the invite code string).
  </action>
  <verify>
    <automated>ssh omer@100.114.126.43 'curl -s "http://127.0.0.1:3004/api/groups/120363421825201386@g.us/invite-code?session=3cf11776_logan" -H "X-Api-Key: XcTCX9cn84LE/uMm3SnHEvm0giwtNnHBmGR7OGeAOpA=" | grep -c "code"'</automated>
  </verify>
  <done>
    - Direct WAHA API call to /api/groups/:jid/invite-code returns a JSON object with "code" field
    - Bot replies in chat with the invite link in chat.whatsapp.com/CODE format
    - No errors in gateway logs for getInviteCode action (TST-05)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of all live tests (TST-01 through TST-06)</name>
  <what-built>
    All WhatsApp slash command tests (TST-01 through TST-05) have been run via WAHA API. The executor sent /list, /list groups, /list channels, /join by invite link, /join by name, /leave, and tested LLM invite link retrieval. Logs have been captured.
  </what-built>
  <how-to-verify>
    Check your WhatsApp in the test group (120363421825201386@g.us) and verify:

    1. TST-04 (/list): Bot replied with a formatted group + channel membership list. "/list groups" showed only groups. "/list channels" showed only channels.
    2. TST-02 (/join by name): Non-existent name returned an error. Ambiguous name returned a numbered candidate list.
    3. TST-01 (/join by link): Bot joined the disposable test group via the invite URL and sent a confirmation message.
    4. TST-03 (/leave): Bot left the disposable group and sent a leave confirmation.
    5. TST-05 (invite link): Bot replied with a chat.whatsapp.com/... link when asked "What is the invite link for this group?"

    Then test TST-06 (Admin UI):
    Open the admin panel in your browser. Navigate to the Directory tab.
    - Confirm groups have a "Leave" button on each row
    - Confirm channels have an "Unfollow" button on each row
    - Click Leave on a non-critical test group — confirm AlertDialog appears, click Confirm — toast shows success
    - Paste a WhatsApp invite link in the "Join by invite link" input and click Join — confirm success toast

    Report back:
    - Which tests PASSED
    - Which tests FAILED and what the bot replied (or did not reply)
    - Admin UI status (working / any errors seen)
  </how-to-verify>
  <files></files>
  <action>Human reviews WhatsApp messages and admin panel browser. No automated action required — this is a verification checkpoint.</action>
  <verify>
    <automated>HUMAN_VERIFY — check WhatsApp messages in test group and admin panel in browser</automated>
  </verify>
  <done>All 6 TST requirements confirmed passing by human reviewer</done>
  <resume-signal>Type "all pass", list failures, or describe issues seen</resume-signal>
</task>

</tasks>

<verification>
Human confirms all 6 TST requirements after this plan:
- TST-01: /join by invite link — bot joins and confirms
- TST-02: /join by name — exact match joins, ambiguous shows candidates
- TST-03: /leave by name — bot leaves and confirms
- TST-04: /list, /list groups, /list channels — correct filtered output
- TST-05: LLM invite link retrieval — bot replies with link
- TST-06: Admin UI Leave and Join by Link — functional with feedback
</verification>

<success_criteria>
All 6 TST requirements confirmed working on real WhatsApp and in admin panel browser. v1.18 milestone ready to ship (git tag, npm publish, deploy).
</success_criteria>

<output>
After completion, create `.planning/phases/47-live-whatsapp-testing/47-02-SUMMARY.md`
</output>
