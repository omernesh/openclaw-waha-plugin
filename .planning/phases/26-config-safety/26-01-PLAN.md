---
phase: 26-config-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/monitor.ts
  - src/config-schema.ts
autonomous: true
requirements:
  - CFG-01
  - CFG-02
  - CFG-03
  - CFG-04
  - CFG-05

must_haves:
  truths:
    - "Submitting invalid config via POST /api/admin/config returns structured field-level errors and does NOT modify the config file on disk"
    - "Every successful config save creates a backup before writing, with the 3 most recent backups retained"
    - "GET /api/admin/config/export returns the full openclaw.json as a downloadable JSON file"
    - "POST /api/admin/config/import validates against Zod schema, applies valid config, rejects invalid with field errors"
  artifacts:
    - path: "src/config-schema.ts"
      provides: "validateWahaConfig export function"
      contains: "export function validateWahaConfig"
    - path: "src/monitor.ts"
      provides: "Config validation, backup rotation, export/import endpoints"
      contains: "rotateConfigBackups"
  key_links:
    - from: "src/monitor.ts"
      to: "src/config-schema.ts"
      via: "import validateWahaConfig"
      pattern: "validateWahaConfig"
    - from: "src/monitor.ts POST /api/admin/config"
      to: "config-schema.ts validation"
      via: "validateWahaConfig call before writeFileSync"
      pattern: "validateWahaConfig.*before.*write"
---

<objective>
Add config validation, backup rotation, and export/import endpoints to the backend.

Purpose: Prevent corrupt config saves, enable config portability, and provide backup safety net.
Output: Validated config saves, backup rotation, export/import API endpoints.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config-schema.ts
@src/monitor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add validation helper to config-schema.ts and wire validation + backup + export/import into monitor.ts</name>
  <files>src/config-schema.ts, src/monitor.ts</files>
  <read_first>src/config-schema.ts, src/monitor.ts (lines 219-230 for getConfigPath, lines 675-727 for POST /api/admin/config handler)</read_first>
  <action>
**In config-schema.ts:**
Add a `validateWahaConfig` export function that:
- Takes an unknown value, runs it through `WahaConfigSchema.safeParse()`
- On success: returns `{ valid: true, data: parsed }`
- On failure: returns `{ valid: false, errors: ZodIssue[] }` where each issue has `path` (string[]) and `message` (string)
- Export the return type as `ConfigValidationResult`

**In monitor.ts:**

1. **Add `rotateConfigBackups` function** near `getConfigPath()` (after line ~222):
   - Takes `configPath: string`
   - Before each save, copies current config to backup
   - Backup naming: `openclaw.json.bak.1` (newest), `.bak.2`, `.bak.3` (oldest)
   - Rotation: delete .bak.3, rename .bak.2 -> .bak.3, rename .bak.1 -> .bak.2, copy current -> .bak.1
   - Use `existsSync`, `copyFileSync`, `renameSync` from `fs` (already imported)
   - Wrap in try/catch — backup failure logs warning but does NOT block save

2. **Update POST /api/admin/config handler** (line ~682):
   - After merging `incomingWaha` into `currentWaha` (after sensitive field preservation), validate the merged waha object:
     ```
     const result = validateWahaConfig(merged);
     if (!result.valid) {
       writeJsonResponse(res, 400, {
         error: "validation_failed",
         fields: result.errors.map(e => ({
           path: e.path.join('.'),
           message: e.message
         }))
       });
       return;
     }
     ```
   - Call `rotateConfigBackups(configPath)` BEFORE `writeFileSync`
   - Keep ALL existing logic intact (sensitive field preservation, restartRequired check, deep merge)

3. **Add GET /api/admin/config/export endpoint** (after the existing GET /api/admin/config handler, line ~679):
   - Route: `req.url === "/api/admin/config/export" && req.method === "GET"`
   - Read full `openclaw.json` from `getConfigPath()`
   - Return with `Content-Type: application/json` and `Content-Disposition: attachment; filename="openclaw-config.json"`
   - Return the raw file contents (full config, not just waha section)

4. **Add POST /api/admin/config/import endpoint** (after export):
   - Route: `req.url === "/api/admin/config/import" && req.method === "POST"`
   - Read request body, parse as JSON
   - Extract `channels.waha` from the imported config
   - Validate the waha section with `validateWahaConfig()`
   - On validation failure: return 400 with same field-level error format as config POST
   - On success: call `rotateConfigBackups(configPath)`, then `writeFileSync` the full imported config to disk
   - Return `{ ok: true }` on success

**CRITICAL constraints (from CLAUDE.md):**
- Config save path MUST be `~/.openclaw/openclaw.json` (getConfigPath already handles this)
- POST /api/admin/config MUST preserve `{"waha": {...}}` wrapper format
- Sensitive fields (session, apiKey, webhookHmacKey) MUST be preserved in the existing POST handler
- Add DO NOT CHANGE comments to the new validation and backup code
  </action>
  <verify>
    <automated>cd D:/docker/waha-oc-plugin && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function validateWahaConfig" src/config-schema.ts
    - grep -q "rotateConfigBackups" src/monitor.ts
    - grep -q "validation_failed" src/monitor.ts
    - grep -q "config/export" src/monitor.ts
    - grep -q "config/import" src/monitor.ts
    - grep -q "Content-Disposition" src/monitor.ts
    - grep -q "bak.1" src/monitor.ts
  </acceptance_criteria>
  <done>
    - POST /api/admin/config validates merged config against Zod schema before saving
    - Invalid config returns 400 with field-level errors (path + message per field)
    - Every successful save rotates backups (keeps last 3)
    - GET /api/admin/config/export returns full config as downloadable JSON
    - POST /api/admin/config/import validates and applies imported config
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- grep confirms all new functions and routes exist
- Backup rotation logic uses .bak.1/.bak.2/.bak.3 naming
- Validation uses WahaConfigSchema from config-schema.ts
</verification>

<success_criteria>
All 5 CFG requirements have backend implementations: Zod validation (CFG-01), structured errors (CFG-02), backup rotation (CFG-03), export endpoint (CFG-04), import endpoint (CFG-05).
</success_criteria>

<output>
After completion, create `.planning/phases/26-config-safety/26-01-SUMMARY.md`
</output>
