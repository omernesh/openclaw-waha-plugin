---
phase: 60-rest-api-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api-v1.ts
  - src/api-v1-auth.ts
  - src/monitor.ts
  - src/config-schema.ts
  - package.json
  - tests/api-v1.test.ts
  - tests/api-v1-auth.test.ts
autonomous: true
requirements: [API-01, API-02, API-04]

must_haves:
  truths:
    - "POST /api/v1/send with valid Bearer token sends a WhatsApp message and returns WAHA message ID"
    - "GET /api/v1/messages?chatId=X&session=Y returns message history"
    - "GET /api/v1/search?q=marketing returns matching contacts and groups"
    - "GET /api/v1/directory returns paginated contact/group listing"
    - "GET /api/v1/sessions returns session list with health status"
    - "GET /api/v1/mimicry returns read-only mimicry gate status"
    - "Request with missing or wrong API key returns 401"
    - "OPTIONS preflight returns 204 with CORS headers (no auth required)"
  artifacts:
    - path: "src/api-v1.ts"
      provides: "All /api/v1/ route handlers"
      exports: ["handleApiV1Request"]
    - path: "src/api-v1-auth.ts"
      provides: "Public API auth guard with timing-safe comparison"
      exports: ["requirePublicApiAuth", "setCorsHeaders"]
    - path: "tests/api-v1.test.ts"
      provides: "API route tests for API-01, API-04"
    - path: "tests/api-v1-auth.test.ts"
      provides: "Auth guard tests for API-02"
  key_links:
    - from: "src/monitor.ts"
      to: "src/api-v1.ts"
      via: "import handleApiV1Request, route dispatch for /api/v1/*"
      pattern: "handleApiV1Request"
    - from: "src/api-v1.ts"
      to: "src/proxy-send-handler.ts"
      via: "handleProxySend for /api/v1/send"
      pattern: "handleProxySend"
    - from: "src/api-v1.ts"
      to: "src/directory.ts"
      via: "getDirectoryDb for /api/v1/directory and /api/v1/search"
      pattern: "getDirectoryDb"
    - from: "src/api-v1-auth.ts"
      to: "node:crypto"
      via: "timingSafeEqual for timing-safe token comparison"
      pattern: "timingSafeEqual"
---

<objective>
Create the public REST API layer at /api/v1/ with 6 endpoints (send, messages, search, directory, sessions, mimicry), Bearer token authentication with timing-safe comparison, and CORS support.

Purpose: External callers (CLI, MCP server, third-party integrations) need authenticated access to Chatlytics WhatsApp capabilities without going through the admin panel.
Output: Working /api/v1/ endpoints, auth guard, CORS headers, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-rest-api-cli/60-RESEARCH.md

@src/monitor.ts
@src/proxy-send-handler.ts
@src/send.ts
@src/directory.ts
@src/health.ts
@src/accounts.ts
@src/config-schema.ts
@src/mimicry-gate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API auth guard, CORS helpers, and config schema extension</name>
  <files>src/api-v1-auth.ts, src/config-schema.ts, tests/api-v1-auth.test.ts, package.json</files>
  <read_first>src/monitor.ts (lines 129-145 for requireAdminAuth pattern), src/config-schema.ts (for Zod schema shape), src/proxy-send-handler.ts (for handleProxySend signature), package.json</read_first>
  <behavior>
    - Test: Missing Authorization header returns 401 with { error: "Authorization required" }
    - Test: Invalid Bearer token returns 401 with { error: "Invalid API key" }
    - Test: Valid Bearer token returns true (request proceeds)
    - Test: Token comparison is timing-safe (uses timingSafeEqual, not ===)
    - Test: When no publicApiKey configured AND no CHATLYTICS_API_KEY env, returns true (open access for backward compat)
    - Test: CHATLYTICS_API_KEY env var is used as fallback when config has no publicApiKey
    - Test: setCorsHeaders sets Access-Control-Allow-Origin, Allow-Methods, Allow-Headers, Max-Age
    - Test: handleCorsPreflightIfNeeded returns 204 for OPTIONS /api/v1/* with CORS headers
  </behavior>
  <action>
    1. Run `npm install swagger-ui-dist commander chalk cli-table3 && npm install --save-dev @stoplight/spectral-cli` to install all Phase 60 deps at once.

    2. Create `src/api-v1-auth.ts` with these exports:
       - `requirePublicApiAuth(req, res, coreCfg)`: Returns boolean. Reads token from `coreCfg.channels?.waha?.publicApiKey` falling back to `process.env.CHATLYTICS_API_KEY`. If neither set, returns true (open). Checks `Authorization: Bearer {token}` header. Uses `Buffer.from()` + `timingSafeEqual` from `node:crypto`. Returns 401 JSON on failure.
       - `setCorsHeaders(res)`: Sets 4 CORS headers — `Access-Control-Allow-Origin: *`, `Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS`, `Access-Control-Allow-Headers: Authorization, Content-Type`, `Access-Control-Max-Age: 86400`.
       - `handleCorsPreflightIfNeeded(req, res)`: If method is OPTIONS and URL starts with `/api/v1/`, calls setCorsHeaders, writes 204, ends response, returns true. Otherwise returns false.

    3. Add `publicApiKey` to config-schema.ts Zod shape: `publicApiKey: z.string().optional()` inside the waha channel schema. Use `.optional()` only (no default) — absence means open access.

    4. Write `tests/api-v1-auth.test.ts` with the behaviors listed above. Mock `IncomingMessage` and `ServerResponse` objects. Test timing-safe by verifying the code calls `timingSafeEqual` (import spy or check Buffer usage).

    Per API-02: Bearer ctl_xxx format, timing-safe comparison.
    Per API-04: CORS headers on all /api/v1/* responses.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose tests/api-v1-auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "timingSafeEqual" src/api-v1-auth.ts` returns >= 1
    - `grep -c "Access-Control-Allow-Origin" src/api-v1-auth.ts` returns >= 1
    - `grep -c "publicApiKey" src/config-schema.ts` returns >= 1
    - `npm test -- tests/api-v1-auth.test.ts` passes all tests
  </acceptance_criteria>
  <done>Auth guard validates Bearer tokens with timing-safe comparison, CORS headers set on all /api/v1/ responses, OPTIONS preflight returns 204, config schema accepts publicApiKey</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: API v1 route handlers and monitor.ts wiring</name>
  <files>src/api-v1.ts, src/monitor.ts, tests/api-v1.test.ts</files>
  <read_first>src/monitor.ts (lines 500-600 for server creation and route pattern), src/proxy-send-handler.ts (for handleProxySend), src/send.ts (for getWahaChatMessages), src/directory.ts (for DirectoryDb class and getDirectoryDb), src/accounts.ts (for listEnabledWahaAccounts), src/health.ts (for getHealthState), src/mimicry-gate.ts (for getCapStatus), src/api-v1-auth.ts (just created in Task 1)</read_first>
  <behavior>
    - Test: POST /api/v1/send with {chatId, session, text} calls handleProxySend and returns result
    - Test: POST /api/v1/send with invalid JSON returns 400
    - Test: GET /api/v1/messages?chatId=X&session=Y returns messages array
    - Test: GET /api/v1/messages without chatId returns 400
    - Test: GET /api/v1/search?q=term returns contacts and groups from directory
    - Test: GET /api/v1/directory returns paginated listing (supports ?type=, ?search=, ?limit=, ?offset=)
    - Test: GET /api/v1/sessions returns session array with health status
    - Test: GET /api/v1/mimicry returns cap status (read-only, never calls checkAndConsumeCap)
    - Test: All responses include CORS headers
    - Test: Unknown /api/v1/ route returns 404
  </behavior>
  <action>
    1. Create `src/api-v1.ts` exporting `handleApiV1Request(req, res, opts)` where opts contains `{ config: CoreConfig }`. This function handles all `/api/v1/*` routing:

    Route map:
    - `POST /api/v1/send` — Parse JSON body, call `handleProxySend({ body, cfg: opts.config })`, return result. Uses `readRequestBodyWithLimit` from http-client.ts with 1MB limit and 30s timeout.
    - `GET /api/v1/messages` — Require query params `chatId` and `session`. Call `getWahaChatMessages({ cfg: opts.config, accountId: session, chatId, limit, downloadMedia: false })`. Return messages array.
    - `GET /api/v1/search` — Require query param `q`. Get all enabled accounts via `listEnabledWahaAccounts(opts.config)`, for the first account get `getDirectoryDb(accountId, tenantId)`, call `db.searchAll(q, limit)`. Return `{ contacts, groups }`.
    - `GET /api/v1/directory` — Same as existing `/api/admin/directory` pattern. Support `?type=`, `?search=`, `?limit=`, `?offset=`. Call `db.listContacts()` / `db.listGroups()` / `db.listNewsletters()`.
    - `GET /api/v1/sessions` — Call `listEnabledWahaAccounts(opts.config)`, map each to `{ session, name, ...getHealthState(session) }`.
    - `GET /api/v1/mimicry` — Call `getCapStatus(session)` for each enabled account. Return array. MUST be read-only — never call `checkAndConsumeCap`.
    - Default: return 404 `{ error: "Not found" }`.

    All handlers call `setCorsHeaders(res)` before writing response. All use `writeJsonResponse` from monitor.ts (import it, or copy the 5-line helper into api-v1.ts to avoid circular deps).

    2. Wire into `src/monitor.ts`:
    - Import `handleApiV1Request` from `./api-v1.js`
    - Import `requirePublicApiAuth`, `handleCorsPreflightIfNeeded` from `./api-v1-auth.js`
    - Add route block AFTER the `/health` and `/metrics` routes, BEFORE the admin auth guard:
      ```
      // Phase 60 (API-01, API-02, API-04): Public REST API v1.
      // CORS preflight must be handled BEFORE auth guard.
      if (handleCorsPreflightIfNeeded(req, res)) return;
      if (req.url?.startsWith('/api/v1/')) {
        setCorsHeaders(res);
        if (!requirePublicApiAuth(req, res, opts.config)) return;
        await handleApiV1Request(req, res, { config: opts.config });
        return;
      }
      ```
    - Add DO NOT REMOVE comment explaining the route placement.

    3. Write `tests/api-v1.test.ts` with mocked dependencies (mock handleProxySend, getDirectoryDb, listEnabledWahaAccounts, getHealthState, getCapStatus, getWahaChatMessages). Test each route returns correct shape.

    Per API-01: All 6 endpoints under /api/v1/.
    Per API-04: setCorsHeaders called on every response.
  </action>
  <verify>
    <automated>npm test -- --reporter=verbose tests/api-v1.test.ts tests/api-v1-auth.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "handleApiV1Request" src/api-v1.ts` returns >= 1
    - `grep -c "handleApiV1Request" src/monitor.ts` returns >= 1
    - `grep "/api/v1/send" src/api-v1.ts` shows the send route handler
    - `grep "/api/v1/messages" src/api-v1.ts` shows the messages route handler
    - `grep "/api/v1/search" src/api-v1.ts` shows the search route handler
    - `grep "/api/v1/directory" src/api-v1.ts` shows the directory route handler
    - `grep "/api/v1/sessions" src/api-v1.ts` shows the sessions route handler
    - `grep "/api/v1/mimicry" src/api-v1.ts` shows the mimicry route handler
    - `grep "setCorsHeaders" src/monitor.ts` confirms CORS wiring
    - `grep "requirePublicApiAuth" src/monitor.ts` confirms auth wiring
    - `npm test -- tests/api-v1.test.ts` passes all tests
    - `npm test` passes (no regressions in existing 594+ tests)
  </acceptance_criteria>
  <done>All 6 /api/v1/ endpoints respond correctly, auth guard protects all routes, CORS headers present, no regressions in existing test suite</done>
</task>

</tasks>

<verification>
1. `npm test -- tests/api-v1.test.ts tests/api-v1-auth.test.ts` — all new tests pass
2. `npm test` — full suite passes (no regressions)
3. `grep -r "handleApiV1Request" src/` — function exists and is wired in monitor.ts
4. `grep -r "timingSafeEqual" src/api-v1-auth.ts` — timing-safe comparison used
5. `grep -r "setCorsHeaders" src/monitor.ts` — CORS applied to /api/v1/ routes
</verification>

<success_criteria>
- 6 REST endpoints functional under /api/v1/ (send, messages, search, directory, sessions, mimicry)
- Bearer token auth with timing-safe comparison blocks invalid requests with 401
- CORS preflight (OPTIONS) returns 204 without requiring auth
- All responses include CORS headers
- All new + existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/60-rest-api-cli/60-01-SUMMARY.md`
</output>
