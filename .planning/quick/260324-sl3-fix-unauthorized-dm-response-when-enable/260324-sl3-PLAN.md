---
phase: quick
plan: 260324-sl3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/inbound.ts
  - src/inbound.test.ts
autonomous: true
requirements: [FIX-DM-SCOPE]
must_haves:
  truths:
    - "Auto-reply only fires for actual DMs (@c.us), never for groups or newsletters"
    - "Passcode pairing only fires for actual DMs (@c.us), never for groups or newsletters"
    - "Existing DM auto-reply and passcode behavior unchanged for @c.us contacts"
  artifacts:
    - path: "src/inbound.ts"
      provides: "Fixed guard condition on Phase 16 pairing/auto-reply block"
      contains: "chatId.endsWith(\"@c.us\")"
    - path: "src/inbound.test.ts"
      provides: "Test coverage for newsletter/group exclusion"
  key_links:
    - from: "src/inbound.ts"
      to: "Phase 16 pairing/auto-reply block"
      via: "guard condition at line ~687"
      pattern: "chatId\\.endsWith.*@c\\.us"
---

<objective>
Fix the Phase 16 unauthorized DM response (auto-reply and passcode pairing) to only fire for actual DMs (@c.us), not groups or newsletters.

Purpose: The guard `!isGroup` on line 687 of inbound.ts is true for both DMs AND newsletters (@newsletter). This means auto-reply rejection messages and passcode challenges are sent to newsletter chats, which is wrong. The fix is to use `chatId.endsWith("@c.us")` instead of `!isGroup`.

Output: Patched inbound.ts with correct DM-only guard, plus test coverage.
</objective>

<execution_context>
@.planning/quick/260324-sl3-fix-unauthorized-dm-response-when-enable/260324-sl3-PLAN.md
</execution_context>

<context>
@CLAUDE.md
@src/inbound.ts (lines 440-442 for isGroup definition, lines 672-850 for Phase 16 block)
@src/inbound.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix DM-only guard and add test coverage</name>
  <files>src/inbound.ts, src/inbound.test.ts</files>
  <behavior>
    - Test: newsletter chatId (@newsletter) with auto-reply enabled does NOT trigger auto-reply (message passes through)
    - Test: newsletter chatId (@newsletter) with pairing enabled does NOT trigger pairing challenge
    - Test: DM chatId (@c.us) with auto-reply enabled DOES trigger auto-reply (existing behavior preserved)
    - Test: group chatId (@g.us) still excluded (existing behavior preserved)
  </behavior>
  <action>
1. In `src/inbound.ts` line 687, change the guard from:
   ```
   if (!isGroup && !triggerActivated && !message.fromMe && account.role === "bot") {
   ```
   to:
   ```
   const isDm = typeof chatId === "string" && chatId.endsWith("@c.us");
   if (isDm && !triggerActivated && !message.fromMe && account.role === "bot") {
   ```
   Place the `isDm` const BEFORE the if-block (around line 686). Add a comment:
   ```
   // FIX: Use explicit @c.us check instead of !isGroup — newsletters (@newsletter) are NOT DMs
   // and must never receive auto-reply or pairing challenges. DO NOT CHANGE back to !isGroup.
   ```

2. Also check lines 952, 984, 1010 which use `!isGroup` for DM-specific logic — these are downstream of the gateway's own access check and may have the same bug. If they also apply DM-only logic (sending pairing replies, DM filter), apply the same `isDm` guard or add `&& chatId.endsWith("@c.us")`. Specifically:
   - Line 952: `!isGroup && access.decision !== "allow"` — this sends gateway pairing reply to non-group chats. Add `chatId.endsWith("@c.us")` check.
   - Line 984: `!isGroup && access.decision === "allow"` — TTL expiry check. Same fix.
   - Line 1010: `!isGroup && !triggerActivated` — DM keyword filter. Same fix.

   For all of these, replace `!isGroup` with the `isDm` variable. Move the `isDm` const declaration up near `isGroup` (around line 442) so it's available throughout the function:
   ```
   const isGroup = typeof message.chatId === "string" && message.chatId.endsWith("@g.us");
   const isDm = typeof chatId === "string" && chatId.endsWith("@c.us"); // FIX: explicit DM check, excludes newsletters
   ```

3. In `src/inbound.test.ts`, add tests for the newsletter exclusion. Create test cases that verify:
   - A message from a newsletter chatId (e.g. `120363xxx@newsletter`) with autoReply.enabled=true does NOT get blocked by the Phase 16 block (it should pass through)
   - A message from `972xxx@c.us` with autoReply.enabled=true DOES get the auto-reply treatment

4. Make a backup before editing: `cp src/inbound.ts src/inbound.ts.bak.260324-sl3`
  </action>
  <verify>
    <automated>npx vitest run src/inbound.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Guard on line ~687 uses `isDm` (chatId.endsWith("@c.us")) instead of `!isGroup`
    - All other `!isGroup` DM-specific checks (lines ~952, ~984, ~1010) also use `isDm`
    - Tests pass proving newsletters don't trigger auto-reply or pairing
    - Existing DM behavior unchanged
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/inbound.test.ts` — all tests pass
- `npx tsc --noEmit` — no type errors
- Manual: grep for `!isGroup` in inbound.ts — should only remain in places that genuinely mean "non-group" (e.g., analytics chatType detection at line 668, LID mapping at line 462)
</verification>

<success_criteria>
- Auto-reply and passcode pairing ONLY fire for @c.us chatIds
- Newsletter messages (@newsletter) pass through without triggering rejection
- Group messages (@g.us) still excluded (unchanged)
- All existing inbound tests pass
- No regressions in DM filter, TTL check, or keyword filter paths
</success_criteria>

<output>
After completion, create `.planning/quick/260324-sl3-fix-unauthorized-dm-response-when-enable/260324-sl3-SUMMARY.md`
</output>
