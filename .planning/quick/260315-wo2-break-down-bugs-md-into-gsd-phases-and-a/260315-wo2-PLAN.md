---
phase: quick-260315-wo2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/ROADMAP.md]
autonomous: true
requirements: [ALL-BUGS]
must_haves:
  truths:
    - "ROADMAP.md contains 5 new phases (07-11) after the existing Phase 6"
    - "Every bug from BUGS.md (1-18) is mapped to exactly one phase"
    - "Each phase has requirement IDs, goal, and UAT criteria"
  artifacts:
    - path: "docs/ROADMAP.md"
      provides: "Extended roadmap with phases 07-11"
      contains: "Phase 7"
  key_links: []
---

<objective>
Append 5 new phases (07-11) to docs/ROADMAP.md, mapping all 18 admin panel bugs from BUGS.md into structured GSD phases.

Purpose: Create a clear execution roadmap for fixing all known admin panel bugs.
Output: Updated docs/ROADMAP.md with phases 07-11.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/ROADMAP.md
@BUGS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append phases 07-11 to ROADMAP.md</name>
  <files>docs/ROADMAP.md</files>
  <action>
Read docs/ROADMAP.md and BUGS.md. Insert 5 new phases BETWEEN the existing "Phase 6: Platform Abstraction" section and the "## Version History" section. Keep all existing content untouched.

Use the EXACT same markdown format as the existing phases (## heading, focus line, bullet list with requirement IDs in bold). Each requirement ID should follow the pattern used in existing phases (short prefix + number).

**Phase 7: Admin Panel Critical Fixes (Priority: HIGH)**
Focus: Fix broken functionality that prevents normal admin panel use.

Requirements (from BUGS.md):
- Bug 7 → **AP-01: Save & Restart Graceful Recovery** — After "Save and Restart", show a "Restarting..." overlay, poll the server until it responds (up to 60s), then auto-reload. No more 502 crash screen.
- Bug 8 → **AP-02: Directory Pagination Fix** — Fix "Load More" button: default initial load to 50 items, fix offset/pagination so clicking "Load More" fetches the next page instead of duplicating entries.
- Bug 12 → **AP-03: Group Filter Override 502 Fix** — Fix the API call that fails with HTTP 502 when toggling "Override global filter" checkbox on a group.

UAT: Save & Restart shows overlay and recovers. Load More loads new items. Group filter override saves without error.

**Phase 8: Shared UI Components (Priority: MEDIUM)**
Focus: Build reusable UI components used across multiple admin panel sections.

Requirements (from BUGS.md):
- Bug 1 → **UI-01: JID-to-Name Resolution in Dashboard** — Replace raw JIDs/LIDs/phone numbers with resolved contact names in the Dashboard Access Control section. Add tooltips to section titles.
- Bug 4 → **UI-02: Contact Picker with Search** — Build a reusable contact/group picker component with UTF-8 fuzzy search, multi-select, and paired JID handling (@c.us + @lid as one contact). Use in Allow From, Group Allow From, and God Mode Users fields.
- Bug 5 → **UI-03: Tag-Style Input Component** — Build a reusable tag/bubble input: type word, press comma/space to create a bubble with 'x' to delete, press enter to add. Use for Mention Patterns, Custom Keywords, and Group Keywords inputs.
- Bug 6 → **UI-04: God Mode Users Display** — Show contact names instead of raw numbers/LIDs. Each contact on its own line with remove button. Add/remove paired JIDs together.

UAT: All JID displays show human names. Tag inputs work with comma/space/enter. Contact picker supports Hebrew + English fuzzy search. God Mode shows names with remove buttons.

**Phase 9: Settings UX Improvements (Priority: MEDIUM)**
Focus: Improve Settings tab usability and clarity.

Requirements (from BUGS.md):
- Bug 3 → **UX-01: DM Policy Pairing Investigation** — Investigate how "pairing" approval mode works. Either implement it properly with tests or remove/disable the option with a clear explanation.
- Bug 9 → **UX-02: Contact Settings Tooltips** — Add tooltips to all Contact Settings panel fields (Mode, Mention Only, Custom Keywords, Can Initiate) explaining what each does. Clarify "Active" vs "Listen Only" meanings.
- Bug 13 → **UX-03: Group Filter Override UX** — Add per-group trigger operator option. Replace keywords plain text with tag-style input (reuse UI-03 component).
- Bug 14 → **UX-04: Directory Tab Switching & Search** — Clear search bar when switching tabs. Add 'x' clear button in search bar. Rename "Newsletters" tab to "Channels".

UAT: Pairing mode is either working or removed. All settings fields have tooltips. Group filter has trigger operator. Tab switching clears search. Newsletters renamed to Channels.

**Phase 10: Directory & Group Enhancements (Priority: MEDIUM)**
Focus: Improve directory browsing, group participant management, and bulk operations.

Requirements (from BUGS.md):
- Bug 10 → **DIR-01: Groups Pagination** — Replace long group list with paginated table. Upper and lower nav bars with page numbers and "Display [X] groups" selector.
- Bug 11 → **DIR-02: Group Participants Fix** — Fix groups showing "0 participants" / failing to load. Show contact names instead of raw LIDs. Reflect global allowlist state in participant buttons.
- Bug 16 → **DIR-03: Group Participant Roles** — Add role dropdown per participant: "Bot Admin" (can assign managers, configure bot via DM), "Manager" (manage bot in this group), "Participant" (no special rights).
- Bug 17 → **DIR-04: Bulk Edit** — Select multiple contacts/groups/participants and apply changes in bulk (allow DM, change role, etc.).

UAT: Groups paginated with nav controls. All participants load with names. Roles assignable per participant. Bulk selection and edit works.

**Phase 11: Dashboard, Sessions & Log (Priority: LOW)**
Focus: Complete dashboard information and improve log readability.

Requirements (from BUGS.md):
- Bug 2 → **DASH-01: Multi-Session Dashboard** — Show all connected sessions (not just logan). Display both omer and logan sessions with their respective ports and status.
- Bug 15 → **SESS-01: Session Role Editing** — Add dropdown to change session type/role directly from the Sessions tab instead of requiring Config tab edits.
- Bug 18 → **LOG-01: Structured Log Display** — Improve log tab with clearly formatted timestamps, visual separation between entries, and structured layout for readability.

UAT: Dashboard shows all sessions. Session roles editable via dropdown. Log entries have visible timestamps and clear separation.

IMPORTANT: Insert the new phases BEFORE the "## Version History" section. Do NOT modify any existing phases or version history entries. Match the existing markdown style exactly (## headings, bullet lists with bold requirement IDs).
  </action>
  <verify>
    <automated>grep -c "## Phase" docs/ROADMAP.md</automated>
  </verify>
  <done>docs/ROADMAP.md contains all original content plus 5 new phases (07-11). All 18 bugs from BUGS.md are mapped. Each phase has a goal, focus, requirement IDs, and UAT criteria. The "## Version History" section remains at the bottom.</done>
</task>

</tasks>

<verification>
- grep for "Phase 7" through "Phase 11" in docs/ROADMAP.md confirms all 5 new phases exist
- All 18 bug numbers (1-18) are referenced in the new phases
- Version History section still present at the end of the file
- Existing phases 1-6 are unchanged
</verification>

<success_criteria>
- docs/ROADMAP.md has phases 1-11 (6 existing + 5 new)
- Every bug from BUGS.md (1-18) appears in exactly one phase
- Phase grouping matches CONTEXT.md decisions: Phase 7 (bugs 7,8,12), Phase 8 (bugs 1,4,5,6), Phase 9 (bugs 3,9,13,14), Phase 10 (bugs 10,11,16,17), Phase 11 (bugs 2,15,18)
- Existing ROADMAP.md content is untouched
</success_criteria>

<output>
After completion, create `.planning/quick/260315-wo2-break-down-bugs-md-into-gsd-phases-and-a/260315-wo2-SUMMARY.md`
</output>
