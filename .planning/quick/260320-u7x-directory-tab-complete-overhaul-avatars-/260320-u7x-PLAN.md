---
id: 260320-u7x
type: quick
title: "Directory Tab Complete Overhaul — Avatars, Stacked Layout, Pagination, Action Buttons"
files_modified:
  - src/admin/src/components/shared/Avatar.tsx
  - src/admin/src/components/shared/DataTable.tsx
  - src/admin/src/components/tabs/DirectoryTab.tsx
  - src/admin/src/components/tabs/directory/ContactsTab.tsx
  - src/admin/src/components/tabs/directory/GroupsTab.tsx
  - src/admin/src/components/tabs/directory/ChannelsTab.tsx
  - src/admin/src/components/tabs/directory/ParticipantRow.tsx
autonomous: true
---

<objective>
Restore all missing old GUI features to the React Directory tab. The old legacy HTML/JS admin panel had: colored avatar circles with initials, stacked name+JID in one column, clickable "Allow DM" buttons in rows, explicit "Settings"/"Participants" buttons, full numbered pagination with page jumps, and per-page size dropdowns. The current React implementation is missing ALL of these.

Purpose: Match the old GUI's feature completeness and visual density in the React admin panel.
Output: Updated Directory tab components with all 10 gaps closed.
</objective>

<context>
@.planning/STATE.md
@docs/OLD-GUI-INVENTORY.md

Key existing files (all under src/admin/src/):
- components/shared/DataTable.tsx — generic table with server pagination (Previous/Next only)
- components/tabs/DirectoryTab.tsx — parent tab with search, sub-tabs, centralized data fetch
- components/tabs/directory/ContactsTab.tsx — contact list with bulk select, row click opens sheet
- components/tabs/directory/GroupsTab.tsx — group list with expandable participant rows
- components/tabs/directory/ChannelsTab.tsx — channel list with bulk select, row click opens sheet
- components/tabs/directory/ParticipantRow.tsx — lazy-loaded participant list for groups
- components/tabs/directory/ContactSettingsSheet.tsx — side sheet for contact/channel settings
- lib/api.ts — typed API client (toggleAllowDm endpoint exists)
- types.ts — DirectoryContact, ParticipantEnriched types

<interfaces>
<!-- DataTable props — executor must extend these -->
From src/admin/src/components/shared/DataTable.tsx:
```typescript
interface DataTableProps<TData> {
  columns: ColumnDef<TData, unknown>[]
  data: TData[]
  total: number
  pagination: { pageIndex: number; pageSize: number }
  onPaginationChange: (pagination: { pageIndex: number; pageSize: number }) => void
  rowSelection?: Record<string, boolean>
  onRowSelectionChange?: (selection: Record<string, boolean>) => void
  onRowClick?: (row: TData) => void
  loading?: boolean
  getRowId?: (row: TData) => string
  expandedRowId?: string | null
  renderExpandedRow?: (row: TData) => React.ReactNode
}
```

From src/admin/src/types.ts:
```typescript
export interface DirectoryContact {
  jid: string
  displayName: string | null
  firstSeenAt: number
  lastMessageAt: number
  messageCount: number
  isGroup: boolean
  dmSettings?: ContactDmSettings
  allowedDm: boolean
  expiresAt: number | null
  expired: boolean
  source: string | null
}

export interface ParticipantEnriched {
  groupJid: string
  participantJid: string
  displayName: string | null
  isAdmin: boolean
  allowInGroup: boolean
  allowDm: boolean
  participantRole: 'bot_admin' | 'manager' | 'participant'
  globallyAllowed: boolean
  isBotSession: boolean
}
```

From lib/api.ts — relevant endpoints:
```typescript
toggleAllowDm: (jid: string, body: { allowed: boolean }) => request<void>(...)
```

From DirectoryTab.tsx — pagination state:
```typescript
const [pagination, setPagination] = useState({ pageIndex: 0, pageSize: 50 })
```
</interfaces>
</context>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Create Avatar component + Overhaul DataTable pagination</name>
  <files>
    src/admin/src/components/shared/Avatar.tsx
    src/admin/src/components/shared/DataTable.tsx
  </files>
  <action>
**1a. Create `Avatar.tsx` — Colored circle with 2-letter initials:**
- New file: `src/admin/src/components/shared/Avatar.tsx`
- Props: `{ name: string | null; size?: 'sm' | 'md' }` (sm=28px for participant rows, md=36px for main rows)
- Extract first 2 letters of name (or "?" if null). For multi-word names, use first letter of first two words.
- Deterministic background color from a hash of the name string. Use a palette of 12+ distinct colors (teal, blue, purple, orange, pink, green, amber, indigo, rose, cyan, emerald, fuchsia). Hash: sum of charCodes mod palette length.
- Render: `<div>` with rounded-full, flex items-center justify-center, white text, font-semibold, the computed bg color as inline style or a Tailwind class.
- Export: `Avatar` component and `getInitials(name: string | null): string` utility.

**1b. Overhaul DataTable pagination — full numbered pages + page-size dropdown:**
- Add new optional prop `pageSizeOptions?: number[]` (default: `[10, 25, 50, 100]`).
- Replace the current "Previous/Next" only pagination with:
  - Left side: page-size dropdown using `<Select>` from shadcn — label: "{Entity} per page:" with the page size options. When changed, reset pageIndex to 0 and call `onPaginationChange` with new pageSize.
  - Right side: Full numbered pagination bar:
    - First page button (`<<`), Previous (`<`), up to 5 numbered page buttons (with current page highlighted), Next (`>`), Last page (`>>`).
    - Page number logic: show pages around the current page (window of 5). If near start, show 1-5. If near end, show last 5. Ellipsis not needed — just show the window.
    - All buttons disabled when `loading`.
  - Keep the existing "Page X of Y (Z total)" text between the page-size dropdown and the page buttons.
- DO NOT break existing props or behavior. The new `pageSizeOptions` prop is optional — if omitted, defaults are used.
- Import `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from `@/components/ui/select`.
  </action>
  <verify>
    <automated>cd D:/docker/waha-oc-plugin/src/admin && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Avatar.tsx exists with colored initials. DataTable has full numbered pagination with first/last/numbered buttons and a page-size dropdown. TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Apply visual overhaul to all Directory sub-tabs + ParticipantRow</name>
  <files>
    src/admin/src/components/tabs/DirectoryTab.tsx
    src/admin/src/components/tabs/directory/ContactsTab.tsx
    src/admin/src/components/tabs/directory/GroupsTab.tsx
    src/admin/src/components/tabs/directory/ChannelsTab.tsx
    src/admin/src/components/tabs/directory/ParticipantRow.tsx
  </files>
  <action>
**CRITICAL: Preserve ALL existing functionality.** Read every DO NOT CHANGE comment before editing. All existing bulk mode, row selection, presence dots, expanded rows, ContactSettingsSheet wiring, GroupFilterOverride, etc. MUST remain working.

**2a. DirectoryTab.tsx — Wire pageSize through to sub-tabs:**
- Change the default `pageSize` from 50 to 25 (matching old GUI default).
- The pageSize change propagation already works via `setPagination` — no other changes needed here.

**2b. ContactsTab.tsx — Avatar, stacked name+JID, Allow DM button, Settings button:**
- Import `Avatar` from `@/components/shared/Avatar`.
- Replace the separate `displayName` and `jid` columns with a SINGLE "Name" column that renders:
  - `<Avatar name={row.original.displayName} />` on the left
  - Stacked layout on the right: display name on top (font-medium), JID below in smaller muted text (`text-xs text-muted-foreground font-mono`). Strip `@c.us` from JID display.
  - Keep the presence dot indicator — render it as an overlay on the avatar or inline with the name. Preserve the existing presenceMap logic.
- Replace the read-only "DM Access" Badge column with a clickable "Allow DM" Button column:
  - When `allowedDm` is true: render a green `<Button variant="outline" size="sm">` with text "Allowed" and green styling (`text-green-600 border-green-600`).
  - When false: render a muted `<Button variant="outline" size="sm">` with text "Allow DM".
  - On click: call `api.toggleAllowDm(jid, { allowed: !currentValue })`, show toast, call `onRefresh()`. Use `e.stopPropagation()` to prevent row click from firing.
- Add a "Settings" button column (last column):
  - Render `<Button variant="ghost" size="sm">Settings</Button>` with a gear icon (`Settings` from lucide-react).
  - On click: `e.stopPropagation()` then `setSelectedJid(row.original.jid)` (opens the existing ContactSettingsSheet).
- Keep Messages and Last Message columns as-is.
- Maintain all bulk mode logic (select column, BulkEditToolbar, etc.) exactly as-is.

**2c. GroupsTab.tsx — Avatar, stacked name+JID, explicit Participants button:**
- Import `Avatar` from `@/components/shared/Avatar`.
- Replace the `expand` chevron column + separate `displayName` and `jid` columns with:
  - Single "Group" column: Avatar + stacked group name (font-medium) + JID below (text-xs muted font-mono).
- Keep Members, Messages, Last Message columns.
- Add a "Participants" button column:
  - Render `<Button variant="outline" size="sm">Participants</Button>` with `ChevronDown` icon that rotates when expanded.
  - On click: `e.stopPropagation()` then toggle `expandedGroupJid`. This replaces the current row-click-to-expand behavior.
- ALSO keep row click to expand (both paths work — button is just more discoverable).
- Preserve the existing `expandedRowId` / `renderExpandedRow` / `ParticipantRow` wiring.

**2d. ChannelsTab.tsx — Avatar, stacked name+JID, Allow DM + Settings buttons:**
- Import `Avatar` from `@/components/shared/Avatar`.
- Merge `displayName` and `jid` columns into single "Channel" column with Avatar + stacked name/JID.
- Add "Allow DM" clickable button column (same pattern as ContactsTab 2b above — calls `api.toggleAllowDm`).
- Add "Settings" button column (same pattern as ContactsTab 2b — opens ContactSettingsSheet via `setSelectedJid`).
- Keep Messages and First Seen columns.
- Maintain all bulk mode logic exactly as-is.

**2e. ParticipantRow.tsx — Avatar + LID display:**
- Import `Avatar` with `size="sm"`.
- Add `<Avatar name={p.displayName} size="sm" />` before the participant name span.
- Below the display name, show the participantJid in `text-xs text-muted-foreground font-mono` (this handles LID display — the server already resolves names, so the JID shown will be the @lid or @c.us identifier).
- Keep all existing controls (Allow in Group, Allow DM, Role dropdown, Bot badge, Admin badge, GroupFilterOverride) exactly as-is.
  </action>
  <verify>
    <automated>cd D:/docker/waha-oc-plugin/src/admin && npx tsc --noEmit 2>&1 | head -30 && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <done>
All 10 gaps from the audit are addressed:
1. Avatar circles with colored initials — all tabs + participant rows
2. Stacked name+JID layout — all tabs
3. Allow DM clickable button — contacts + channels
4. Settings button per contact/channel row — contacts + channels
5. Side sheet approach kept (decision: sheet is cleaner than inline expand)
6. Full pagination with numbered pages — DataTable
7. Per-page dropdown (10, 25, 50, 100) — DataTable
8. Explicit Participants button — groups
9. Channel action buttons match contacts — channels
10. LID/JID display in participant rows — ParticipantRow
Build succeeds with no errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build verification + cross-check against OLD-GUI-INVENTORY.md</name>
  <files>
    src/admin/src/components/shared/Avatar.tsx
    src/admin/src/components/shared/DataTable.tsx
    src/admin/src/components/tabs/DirectoryTab.tsx
    src/admin/src/components/tabs/directory/ContactsTab.tsx
    src/admin/src/components/tabs/directory/GroupsTab.tsx
    src/admin/src/components/tabs/directory/ChannelsTab.tsx
    src/admin/src/components/tabs/directory/ParticipantRow.tsx
  </files>
  <action>
**Verification checklist — read every modified file and cross-check against OLD-GUI-INVENTORY.md:**

1. **Avatar circles**: Verify Avatar.tsx exists, is imported in ContactsTab, GroupsTab, ChannelsTab, and ParticipantRow. Verify colored background from name hash + 2-letter initials.

2. **Stacked name+JID**: In each tab, verify the Name column renders displayName on top + JID below (text-xs muted mono). NO separate JID column should exist.

3. **Allow DM button (Contacts)**: Verify ContactsTab has a clickable button (not just a badge) in the allowedDm column. Verify it calls api.toggleAllowDm with e.stopPropagation().

4. **Allow DM button (Channels)**: Same verification for ChannelsTab.

5. **Settings button (Contacts)**: Verify ContactsTab has a Settings button column that calls setSelectedJid with stopPropagation.

6. **Settings button (Channels)**: Same verification for ChannelsTab.

7. **Participants button (Groups)**: Verify GroupsTab has an explicit Participants button that toggles expandedGroupJid.

8. **Full pagination**: Verify DataTable renders numbered page buttons (not just Prev/Next). Verify first/last page jump buttons.

9. **Page-size dropdown**: Verify DataTable renders a Select with options [10, 25, 50, 100]. Verify DirectoryTab default pageSize is 25.

10. **ParticipantRow avatar + JID**: Verify Avatar with size="sm" and participantJid displayed below name.

11. **No regressions**: Verify all DO NOT CHANGE comments are still respected. Check:
    - Bulk mode still works (select column, BulkEditToolbar)
    - Presence dots still render on contacts
    - ContactSettingsSheet still opens/saves correctly
    - GroupFilterOverride still renders in ParticipantRow
    - Timestamps still multiply by 1000

12. **Build clean**: Run `npx vite build` and verify no errors, no TypeScript errors.

13. **Existing tests**: Run `npx vitest run --config vitest.config.ts` to verify no test regressions.

If ANY check fails, fix it immediately before reporting completion.
  </action>
  <verify>
    <automated>cd D:/docker/waha-oc-plugin/src/admin && npx tsc --noEmit && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>All 10 gaps verified closed. Build passes. No regressions in existing functionality. All DO NOT CHANGE markers respected.</done>
</task>

</tasks>

<verification>
- `cd src/admin && npx tsc --noEmit` — zero TypeScript errors
- `cd src/admin && npx vite build` — successful production build
- `cd src/admin && npx vitest run --config vitest.config.ts` — existing tests pass
- Visual: Avatar circles visible on all directory rows, stacked name+JID layout, clickable buttons
</verification>

<success_criteria>
All 10 audit gaps from the planning context are addressed. Production build succeeds. No regressions in existing bulk mode, presence display, ContactSettingsSheet, GroupFilterOverride, or ParticipantRow functionality.
</success_criteria>

<output>
After completion, create `.planning/quick/260320-u7x-directory-tab-complete-overhaul-avatars-/260320-u7x-SUMMARY.md`
</output>
