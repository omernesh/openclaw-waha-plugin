---
phase: 23-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/admin/src/components/ui/skeleton.tsx
  - src/admin/src/components/ui/tooltip.tsx
  - src/admin/src/components/shared/TabErrorBoundary.tsx
  - src/admin/src/App.tsx
  - src/admin/src/components/tabs/DashboardTab.tsx
  - src/admin/src/components/tabs/SettingsTab.tsx
  - src/admin/src/components/tabs/SessionsTab.tsx
  - src/admin/src/components/tabs/QueueTab.tsx
  - src/admin/src/components/tabs/ModulesTab.tsx
  - src/admin/src/components/tabs/LogTab.tsx
  - src/admin/src/components/tabs/DirectoryTab.tsx
autonomous: true
requirements: [PLSH-01, PLSH-02, PLSH-03, CLNP-03]

must_haves:
  truths:
    - "If a tab's API call throws an unhandled error, that tab shows an error message with a Retry button instead of crashing the whole panel"
    - "All data-fetching tabs show structured skeleton placeholders (not plain 'Loading...' text) while initial fetch is in flight"
    - "Save/restart failures in SettingsTab surface as Sonner toast notifications instead of browser alert() dialogs"
    - "Tooltips in Directory and Settings tabs render above overflow boundaries without clipping"
  artifacts:
    - path: "src/admin/src/components/ui/skeleton.tsx"
      provides: "shadcn Skeleton primitive"
      exports: ["Skeleton"]
    - path: "src/admin/src/components/ui/tooltip.tsx"
      provides: "shadcn Tooltip primitives with portal rendering"
      exports: ["Tooltip", "TooltipTrigger", "TooltipContent", "TooltipProvider"]
    - path: "src/admin/src/components/shared/TabErrorBoundary.tsx"
      provides: "Per-tab error boundary with retry"
      exports: ["TabErrorBoundary"]
    - path: "src/admin/src/App.tsx"
      provides: "Error boundary wrapping each tab"
      contains: "TabErrorBoundary"
  key_links:
    - from: "src/admin/src/App.tsx"
      to: "src/admin/src/components/shared/TabErrorBoundary.tsx"
      via: "import and wrap each tab in renderActiveTab"
      pattern: "TabErrorBoundary"
    - from: "src/admin/src/components/tabs/SettingsTab.tsx"
      to: "sonner"
      via: "toast.error replacing alert()"
      pattern: "toast\\.error"
---

<objective>
Add error boundaries, skeleton loading states, toast notifications, and tooltip portal support across all admin panel tabs.

Purpose: Replace raw "Loading..." text and browser alert() dialogs with polished UX patterns (skeletons, toasts, error isolation). Ensure tooltips render correctly in overflow containers.
Output: TabErrorBoundary component, Skeleton/Tooltip UI primitives, skeleton states in all 7 tabs, toast calls replacing all alert() in SettingsTab.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-polish/23-CONTEXT.md

@src/admin/src/App.tsx
@src/admin/src/main.tsx
@src/admin/src/components/tabs/DashboardTab.tsx
@src/admin/src/components/tabs/SettingsTab.tsx
@src/admin/src/components/tabs/SessionsTab.tsx
@src/admin/src/components/tabs/QueueTab.tsx
@src/admin/src/components/tabs/ModulesTab.tsx
@src/admin/src/components/tabs/LogTab.tsx
@src/admin/src/components/tabs/DirectoryTab.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UI primitives and TabErrorBoundary</name>
  <files>
    src/admin/src/components/ui/skeleton.tsx,
    src/admin/src/components/ui/tooltip.tsx,
    src/admin/src/components/shared/TabErrorBoundary.tsx
  </files>
  <read_first>
    src/admin/src/components/ui/badge.tsx (for shadcn component style reference),
    src/admin/src/lib/utils.ts (for cn utility import path),
    src/admin/src/App.tsx
  </read_first>
  <action>
    1. Create `src/admin/src/components/ui/skeleton.tsx` — standard shadcn Skeleton component:
       - A single `div` with `animate-pulse rounded-md bg-primary/10` classes
       - Accept className prop, merge with cn()
       - Export named `Skeleton`

    2. Create `src/admin/src/components/ui/tooltip.tsx` — standard shadcn Tooltip using Radix:
       - Install nothing new — @radix-ui/react-tooltip is already a dep of shadcn patterns
       - Check if @radix-ui/react-tooltip is installed: if not, run `npm install @radix-ui/react-tooltip` in src/admin/
       - Export: TooltipProvider, Tooltip, TooltipTrigger, TooltipContent
       - TooltipContent MUST use `sideOffset={4}` and render via Radix portal (default behavior — do NOT set `container` or `forceMount` to prevent portal)
       - Standard shadcn styling: `z-50 overflow-hidden rounded-md bg-primary px-3 py-1.5 text-xs text-primary-foreground animate-in fade-in-0 zoom-in-95`

    3. Create `src/admin/src/components/shared/TabErrorBoundary.tsx` — React class component:
       - Props: `children: React.ReactNode`, `tabName: string`
       - State: `{ hasError: boolean; error: Error | null }`
       - `static getDerivedStateFromError(error)` sets hasError + error
       - `componentDidCatch(error, info)` logs to console.error
       - Render on error: centered card with red AlertTriangle icon (from lucide-react), error message text, tab name in heading, and a "Retry" Button that resets `hasError` to false
       - Render on no error: `this.props.children`
       - Accept a `key` prop externally (React built-in) — callers pass `refreshKey` as key to force remount on refresh, which also resets error state
       - Export named `TabErrorBoundary`
  </action>
  <acceptance_criteria>
    - grep "export.*Skeleton" src/admin/src/components/ui/skeleton.tsx returns match
    - grep "export.*TooltipContent" src/admin/src/components/ui/tooltip.tsx returns match
    - grep "export.*TabErrorBoundary" src/admin/src/components/shared/TabErrorBoundary.tsx returns match
    - grep "getDerivedStateFromError" src/admin/src/components/shared/TabErrorBoundary.tsx returns match
    - grep "hasError" src/admin/src/components/shared/TabErrorBoundary.tsx returns match
  </acceptance_criteria>
  <verify>
    <automated>cd src/admin && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Skeleton, Tooltip, and TabErrorBoundary components exist, type-check, and are ready for use.</done>
</task>

<task type="auto">
  <name>Task 2: Wire error boundaries, skeleton loading, and toasts into all tabs</name>
  <files>
    src/admin/src/App.tsx,
    src/admin/src/components/tabs/DashboardTab.tsx,
    src/admin/src/components/tabs/SettingsTab.tsx,
    src/admin/src/components/tabs/SessionsTab.tsx,
    src/admin/src/components/tabs/QueueTab.tsx,
    src/admin/src/components/tabs/ModulesTab.tsx,
    src/admin/src/components/tabs/LogTab.tsx,
    src/admin/src/components/tabs/DirectoryTab.tsx
  </files>
  <read_first>
    src/admin/src/App.tsx,
    src/admin/src/components/tabs/DashboardTab.tsx,
    src/admin/src/components/tabs/SettingsTab.tsx,
    src/admin/src/components/tabs/DirectoryTab.tsx,
    src/admin/src/components/tabs/SessionsTab.tsx,
    src/admin/src/components/tabs/QueueTab.tsx,
    src/admin/src/components/tabs/ModulesTab.tsx,
    src/admin/src/components/tabs/LogTab.tsx
  </read_first>
  <action>
    **App.tsx — Error boundary wrapping:**
    - Import TabErrorBoundary from '@/components/shared/TabErrorBoundary'
    - In `renderActiveTab()`, wrap EACH tab's return value with `<TabErrorBoundary key={refreshKey} tabName={activeTab}>...</TabErrorBoundary>`
    - The `key={refreshKey}` ensures the error boundary resets when user clicks refresh
    - Preserve the existing DO NOT CHANGE comment about lifted state

    **SettingsTab.tsx — Replace alert() with toast:**
    - Add `import { toast } from 'sonner'` at top
    - Line ~231: Replace `alert(\`Save failed: ...\`)` with `toast.error(\`Save failed: ...\`)`
    - Line ~247: Replace `alert(\`Save & Restart failed: ...\`)` with `toast.error(\`Save & Restart failed: ...\`)`
    - Line ~274: Replace `alert('Gateway did not respond after 60s. Check logs.')` with `toast.error('Gateway did not respond after 60s. Check logs.')`
    - Also add `toast.success('Settings saved')` after the `setDirty(false)` in handleSave (line ~229)
    - Also add `toast.success('Restarting gateway...')` before `setRestarting(true)` in handleSaveAndRestart

    **DashboardTab.tsx — Skeleton loading:**
    - Import Skeleton from '@/components/ui/skeleton'
    - Replace the `if (loading)` block (currently shows `<p>Loading...</p>`) with a skeleton layout:
      - One full-width Skeleton h-[120px] for Session Health card
      - Two Skeleton h-[80px] side by side for filter cards
      - One Skeleton h-[100px] for Presence card
      - One Skeleton h-[100px] for Access Control card
    - Wrap in `<div className="flex flex-col gap-4 p-4">` to match the real layout

    **SettingsTab.tsx — Skeleton loading:**
    - Import Skeleton from '@/components/ui/skeleton'
    - Replace the `if (loading)` block with skeleton layout:
      - Three Skeleton h-[200px] stacked vertically (representing card sections)

    **SessionsTab.tsx — Skeleton loading:**
    - Import Skeleton from '@/components/ui/skeleton'
    - Find the loading conditional and replace with:
      - Two Skeleton h-[140px] cards side by side representing session cards

    **QueueTab.tsx — Skeleton loading:**
    - Import Skeleton from '@/components/ui/skeleton'
    - Replace loading state with:
      - Three Skeleton h-[80px] for queue stat cards

    **ModulesTab.tsx — Skeleton loading:**
    - Import Skeleton from '@/components/ui/skeleton'
    - Replace loading state with:
      - Two Skeleton h-[100px] for module cards

    **LogTab.tsx — Skeleton loading:**
    - Import Skeleton from '@/components/ui/skeleton'
    - Replace loading state with:
      - One Skeleton h-[40px] for filter bar
      - One Skeleton h-[300px] for log entries area

    **DirectoryTab.tsx — Skeleton loading:**
    - Import Skeleton from '@/components/ui/skeleton'
    - Replace loading state with:
      - One Skeleton h-[40px] for search/filter bar
      - One Skeleton h-[400px] for table area

    For ALL skeleton replacements: keep the same conditional structure (`if (loading) return ...`). The skeleton JSX replaces only the return value inside that conditional.
  </action>
  <acceptance_criteria>
    - grep "TabErrorBoundary" src/admin/src/App.tsx returns match
    - grep "toast.error" src/admin/src/components/tabs/SettingsTab.tsx returns match
    - grep "toast.success" src/admin/src/components/tabs/SettingsTab.tsx returns match
    - grep -c "alert(" src/admin/src/components/tabs/SettingsTab.tsx returns 0
    - grep "Skeleton" src/admin/src/components/tabs/DashboardTab.tsx returns match
    - grep "Skeleton" src/admin/src/components/tabs/SettingsTab.tsx returns match
    - grep "Skeleton" src/admin/src/components/tabs/SessionsTab.tsx returns match
    - grep "Skeleton" src/admin/src/components/tabs/QueueTab.tsx returns match
    - grep "Skeleton" src/admin/src/components/tabs/ModulesTab.tsx returns match
    - grep "Skeleton" src/admin/src/components/tabs/LogTab.tsx returns match
    - grep "Skeleton" src/admin/src/components/tabs/DirectoryTab.tsx returns match
  </acceptance_criteria>
  <verify>
    <automated>cd src/admin && npx tsc --noEmit 2>&1 | head -30 && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 tabs wrapped in error boundaries, all loading states show skeletons, all SettingsTab alert() calls replaced with Sonner toasts. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `cd src/admin && npx vite build` completes without errors
2. `grep -r "alert(" src/admin/src/components/tabs/` returns zero matches
3. `grep -r "TabErrorBoundary" src/admin/src/App.tsx` confirms wrapping
4. `grep -r "Skeleton" src/admin/src/components/tabs/` shows matches in all 7 tab files
5. `grep "TooltipContent" src/admin/src/components/ui/tooltip.tsx` confirms portal tooltip exists
</verification>

<success_criteria>
- Zero `alert()` calls remain in any tab file
- All 7 tabs show skeleton placeholders during loading instead of "Loading..." text
- Each tab is wrapped in TabErrorBoundary in App.tsx
- Tooltip and Skeleton UI primitives exist and type-check
- Vite build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-polish/23-01-SUMMARY.md`
</output>
