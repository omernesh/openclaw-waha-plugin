---
phase: 09-settings-ux-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/monitor.ts
autonomous: false
requirements: [UX-01, UX-02, UX-04]

must_haves:
  truths:
    - "DM Policy pairing option is either functional (sends code reply to unknown sender) or disabled with explanation tooltip"
    - "All four Contact Settings fields (Mode, Mention Only, Custom Keywords, Can Initiate) show tooltip on hover"
    - "Switching directory tabs clears the search bar text"
    - "Search bar has an x clear button that clears text and reloads directory"
    - "Newsletters tab is labeled Channels"
  artifacts:
    - path: "src/monitor.ts"
      provides: "Contact settings tooltips, pairing mode resolution, tab/search UX fixes"
      contains: "data-tip"
  key_links:
    - from: "buildContactCard() tooltip spans"
      to: ".tip CSS class"
      via: "class='tip' data-tip='...'"
      pattern: "class=\"tip\" data-tip="
    - from: "switchDirTab()"
      to: "dir-search input"
      via: "getElementById and value clear"
      pattern: "dir-search.*value.*=''"
    - from: "clearDirSearch()"
      to: "loadDirectory()"
      via: "function call after clearing input"
      pattern: "clearDirSearch.*loadDirectory"
---

<objective>
Add Contact Settings tooltips, resolve DM Policy pairing mode, and fix tab/search UX in the admin panel.

Purpose: Improve Settings tab usability by adding explanatory tooltips to Contact Settings fields, resolving the broken/untested pairing mode, making tab switching clear the search bar, adding a clear button to the search bar, and renaming "Newsletters" to "Channels".

Output: Updated src/monitor.ts with tooltip spans, pairing mode resolved, switchDirTab search clearing, search bar clear button, and tab label rename.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-settings-ux-improvements/09-RESEARCH.md
@.planning/phases/09-settings-ux-improvements/09-UI-SPEC.md

<interfaces>
<!-- Phase 8 factory functions available in monitor.ts script block -->
<!-- These are NOT used in this plan but are documented for awareness -->

From src/monitor.ts (existing patterns to follow):
```
<!-- Tooltip CSS class (line ~334): -->
.tip { display:inline-block; width:16px; height:16px; ... }
.tip::after { content:attr(data-tip); ... }

<!-- Tooltip usage pattern (line ~540): -->
<label>DM Policy <span class="tip" data-tip="How to handle...">?</span></label>

<!-- switchDirTab function (line ~1928): -->
function switchDirTab(tab, btn) {
  currentDirTab = tab;
  document.querySelectorAll('.dir-tab').forEach(...);
  if (btn) btn.classList.add('active');
  dirOffset = 0;
  dirAutoImported = false;
  loadDirectory();
}

<!-- buildContactCard function (line ~2043): -->
function buildContactCard(contact) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pairing mode investigation, contact settings tooltips, tab/search UX fixes</name>
  <files>src/monitor.ts</files>
  <read_first>
    - src/monitor.ts (full file — executor needs to see current buildContactCard, switchDirTab, dir-search HTML, DM Policy select, and Newsletters tab button)
    - src/inbound.ts (lines 320-340 for createScopedPairingAccess, lines 570-660 for pairing flow)
    - src/channel.ts (lines 660-680 for pairing config section)
    - .planning/phases/09-settings-ux-improvements/09-UI-SPEC.md (component contracts for exact tooltip copy)
    - .planning/phases/09-settings-ux-improvements/09-RESEARCH.md (pairing investigation detail, code examples)
  </read_first>
  <action>
**UX-01: DM Policy Pairing Mode**

The pairing mode investigation is binary. Since live testing requires deploying to hpg6 (which happens after phase execution), the executor should:

1. Read `src/inbound.ts` lines ~327-332 and ~572-660 to trace the pairing flow:
   - `createScopedPairingAccess({ core, channel: CHANNEL_ID, accountId })` creates scoped accessor
   - If `access.decision === "pairing"`, calls `pairing.upsertPairingRequest({ id: senderId })`
   - On `created === true`, sends text reply via `sendWahaText` with pairing code
2. Read `src/channel.ts` lines ~667-671 for the pairing config handler
3. Since live testing cannot happen during execution, **disable the pairing option** with an explanation:
   - In the DM Policy `<select>` HTML (Settings section), find the `<option value="pairing">` element
   - Add `disabled` attribute: `<option value="pairing" disabled>pairing (not available)</option>`
   - Update the DM Policy label tooltip (`data-tip`) to: `"How to handle DMs from unknown senders. open: accept all. closed: block all. allowlist: only contacts in Allow From list. pairing: not supported in current SDK integration."`
   - Add HTML comment before the option: `<!-- UX-01: pairing disabled - SDK integration not verified -->`

**UX-02: Contact Settings Tooltips**

In the `buildContactCard()` function (around line 2043), add `.tip` spans to these four fields using the exact same HTML pattern as existing Settings tooltips:

1. **Mode** label: After the text "Mode", before `</label>`, add:
   `<span class="tip" data-tip="Active: bot responds to this contact. Listen Only: messages arrive but bot does not reply.">?</span>`

2. **Mention Only** label: After the checkbox + text "Mention Only", add:
   `<span class="tip" data-tip="When checked, bot only responds if it is explicitly @mentioned in the message.">?</span>`

3. **Custom Keywords** label: After the text "Custom Keywords", before `</label>`, add:
   `<span class="tip" data-tip="Comma-separated regex patterns. Bot responds only if the message matches one. Overrides global keyword filter for this contact.">?</span>`

4. **Can Initiate** label: After the checkbox + text "Can Initiate", add:
   `<span class="tip" data-tip="When checked, bot is allowed to send the first message to this contact. Uncheck to prevent unsolicited outbound messages.">?</span>`

The pattern in JS string concatenation is:
```
'<span class="tip" data-tip="TOOLTIP_TEXT">?</span>'
```
No apostrophes in tooltip text. All four tooltip texts are given above exactly as they should appear.

**UX-04: Tab Switch Clears Search**

In `switchDirTab()` function (around line 1928), add two lines BEFORE `dirOffset = 0;`:
```javascript
var searchEl = document.getElementById('dir-search');
if (searchEl) searchEl.value = '';
```

**UX-04: Search Bar Clear Button**

Find the dir-search input HTML (around line 823). It currently looks like:
```html
<input type="text" id="dir-search" placeholder="Search..." ...>
```

Replace the bare input with a wrapper div containing the input and a clear button:
```html
<div style="position:relative;flex:1;">
  <input type="text" id="dir-search" placeholder="Search..." style="width:100%;padding-right:28px;" ...existing styles...>
  <button id="dir-search-clear" onclick="clearDirSearch()" aria-label="Clear search" style="position:absolute;right:8px;top:50%;transform:translateY(-50%);background:none;border:none;color:#94a3b8;cursor:pointer;font-size:1rem;line-height:1;padding:0 4px;" onmouseover="this.style.color='#e2e8f0'" onmouseout="this.style.color='#94a3b8'">&#x2715;</button>
</div>
```

Note: Move the `flex:1` style from the input (if it has it) to the wrapper div. The input should have `width:100%` instead.

Add the `clearDirSearch()` function in the JavaScript section (near `switchDirTab`):
```javascript
function clearDirSearch() {
  document.getElementById('dir-search').value = '';
  dirOffset = 0;
  loadDirectory();
}
```

**UX-04: Newsletters Tab Rename**

Find the Newsletters tab button (around line 820):
```html
<button class="dir-tab" onclick="switchDirTab('newsletters',this)" id="dtab-newsletters">Newsletters</button>
```

Change only the visible text from `Newsletters` to `Channels`:
```html
<button class="dir-tab" onclick="switchDirTab('newsletters',this)" id="dtab-newsletters">Channels</button>
```

Keep the JS key `'newsletters'` and all backend query parameters unchanged — only the display label changes.
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/monitor.ts contains `disabled>pairing (not available)</option>`
    - src/monitor.ts contains `pairing: not supported in current SDK integration`
    - src/monitor.ts contains `UX-01: pairing disabled`
    - src/monitor.ts contains `data-tip="Active: bot responds to this contact`
    - src/monitor.ts contains `data-tip="When checked, bot only responds if it is explicitly @mentioned`
    - src/monitor.ts contains `data-tip="Comma-separated regex patterns. Bot responds only if the message matches one`
    - src/monitor.ts contains `data-tip="When checked, bot is allowed to send the first message`
    - src/monitor.ts contains `function clearDirSearch()`
    - src/monitor.ts contains `dir-search-clear`
    - src/monitor.ts contains `&#x2715;`
    - src/monitor.ts contains `>Channels</button>` (not `>Newsletters</button>`)
    - switchDirTab function contains `dir-search` and `value = ''`
    - Full test suite passes with zero regressions
  </acceptance_criteria>
  <done>DM Policy pairing option is disabled with explanation tooltip, all four Contact Settings fields have tooltips, tab switching clears search, search bar has x clear button, Newsletters tab reads Channels</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Tooltips on Contact Settings fields, pairing mode disabled, tab switch clears search, search bar x button, Newsletters renamed to Channels</what-built>
  <how-to-verify>
    1. Open admin panel in browser
    2. Go to Directory tab - verify tab reads "Channels" (not "Newsletters")
    3. Type something in the search bar - verify the x button appears and clicking it clears the text and reloads
    4. Type something in search, then switch tabs (Contacts -> Groups -> Channels) - verify search clears each time
    5. Go to Directory -> expand a contact -> verify Mode, Mention Only, Custom Keywords, and Can Initiate each have a ? tooltip icon
    6. Hover over each ? icon - verify tooltip text appears and is readable
    7. Go to Config tab -> DM Policy dropdown - verify "pairing (not available)" option is grayed out / disabled
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Full vitest suite passes (313+ tests, zero regressions)
- `npx tsc --noEmit` passes (if tsconfig exists) OR vitest confirms no type errors
- All four tooltip data-tip attributes present in buildContactCard
- clearDirSearch function defined
- switchDirTab clears search input
- Pairing option has disabled attribute
- Newsletters display text changed to Channels
</verification>

<success_criteria>
1. All Contact Settings fields show explanatory tooltips on hover
2. Pairing mode is disabled with clear explanation in the DM Policy tooltip
3. Tab switching clears the search bar
4. Search bar has a functional x clear button
5. "Newsletters" tab displays as "Channels"
6. Full test suite green with zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/09-settings-ux-improvements/09-01-SUMMARY.md`
</output>
