---
phase: 05-documentation-and-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/send.ts
  - src/channel.ts
  - tests/send-utils.test.ts
  - tests/channel-utils.test.ts
  - tests/action-handlers.test.ts
autonomous: true
requirements:
  - DOC-02
  - DOC-03
must_haves:
  truths:
    - "fuzzyScore, toArr, resolveChatId, autoResolveTarget have passing unit tests covering happy paths and edge cases"
    - "send, poll, edit, search action handlers have integration tests verifying correct WAHA API calls and error handling"
    - "All existing 137+ tests still pass after export additions"
  artifacts:
    - path: "tests/send-utils.test.ts"
      provides: "Unit tests for fuzzyScore and toArr"
      min_lines: 40
    - path: "tests/channel-utils.test.ts"
      provides: "Unit tests for resolveChatId and autoResolveTarget"
      min_lines: 50
    - path: "tests/action-handlers.test.ts"
      provides: "Integration tests for send, poll, edit, search handlers"
      min_lines: 80
  key_links:
    - from: "tests/send-utils.test.ts"
      to: "src/send.ts"
      via: "import { fuzzyScore, toArr }"
      pattern: "import.*fuzzyScore.*from"
    - from: "tests/channel-utils.test.ts"
      to: "src/channel.ts"
      via: "import { resolveChatId, autoResolveTarget }"
      pattern: "import.*resolveChatId.*from"
    - from: "tests/action-handlers.test.ts"
      to: "src/channel.ts"
      via: "import handleAction with vi.mock chain"
      pattern: "handleAction"
---

<objective>
Add unit tests for core utilities (fuzzyScore, toArr, resolveChatId, autoResolveTarget) and integration tests for action handlers (send, poll, edit, search) against a mock WAHA API.

Purpose: Satisfy DOC-02 (unit test coverage for core utilities) and DOC-03 (integration test coverage for action handlers). LRU cache and token bucket are already tested -- this plan covers the remaining four test targets plus four action handler tests.

Output: Three new test files, two minor export additions to source files, all tests green.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-and-testing/05-RESEARCH.md

<interfaces>
<!-- Functions that need export keyword added -->

From src/send.ts (line ~1466, NOT exported):
```typescript
function fuzzyScore(query: string, name: string): number {
  // Returns: 1.0 exact match, 0.9 starts-with, 0.7 word-boundary, 0.5 includes, 0.1 empty query (list-all), 0 no match
}
```

From src/send.ts (line ~1492, already exported):
```typescript
export function toArr(val: unknown): unknown[] {
  // Wraps non-array values in array, passes arrays through
}
```

From src/channel.ts (line ~322, NOT exported):
```typescript
function resolveChatId(params: Record<string, unknown>, toolContext?: { currentChannelId?: string }): string {
  // Resolution order: params.chatId -> params.to -> toolContext.currentChannelId
  // Throws if none found
}
```

From src/channel.ts (line ~346, NOT exported):
```typescript
async function autoResolveTarget(chatId: string, cfg: CoreConfig, accountId?: string): Promise<string> {
  // If chatId looks like a JID, returns as-is
  // Otherwise calls resolveWahaTarget for fuzzy name matching
}
```

<!-- Established mock patterns from existing tests -->

From tests/read-messages.test.ts:
- vi.hoisted for mock function refs
- vi.mock("openclaw/plugin-sdk") with full zod-based stub
- vi.mock("../src/send.js") with selective overrides
- vi.mock("../src/secret-input.js") stub
- vi.mock("../src/http-client.js") stub
- handleAction import after mocks

From tests/http-client.test.ts:
- mockFetchOk/mockFetchErr pattern using vi.stubGlobal("fetch")
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export functions and write unit tests for fuzzyScore, toArr, resolveChatId, autoResolveTarget</name>
  <files>src/send.ts, src/channel.ts, tests/send-utils.test.ts, tests/channel-utils.test.ts</files>
  <behavior>
    fuzzyScore tests (tests/send-utils.test.ts):
    - Returns 1.0 for exact match (case-insensitive)
    - Returns 0.9 when name starts with query
    - Returns 0.7 for word-boundary match
    - Returns 0.5 for substring includes match
    - Returns 0.1 for empty query (list-all mode)
    - Returns 0 for no match

    toArr tests (tests/send-utils.test.ts):
    - Wraps a string in an array
    - Wraps a number in an array
    - Passes an existing array through unchanged
    - Wraps null/undefined in an array

    resolveChatId tests (tests/channel-utils.test.ts):
    - Returns params.chatId when present
    - Returns params.to when chatId absent
    - Returns toolContext.currentChannelId as fallback
    - Throws when no chatId source available

    autoResolveTarget tests (tests/channel-utils.test.ts):
    - Returns JID as-is when input looks like a JID (contains @)
    - Calls resolveWahaTarget when input is a human name (mock resolveWahaTarget)
    - Returns resolved JID from resolveWahaTarget
  </behavior>
  <action>
    1. Add `export` keyword to `fuzzyScore` in src/send.ts (line ~1466). Add comment: `// Exported for testing -- DO NOT CHANGE signature`
    2. Add `export` keyword to `resolveChatId` and `autoResolveTarget` in src/channel.ts (lines ~322, ~346). Add comment: `// Exported for testing -- DO NOT CHANGE signature`
    3. Create tests/send-utils.test.ts with pure function tests for fuzzyScore and toArr. No mocks needed -- these are pure functions. Import from `../src/send.js`.
    4. Create tests/channel-utils.test.ts:
       - resolveChatId: pure function tests (no mocks needed, just pass params objects)
       - autoResolveTarget: needs vi.mock for `../src/send.js` to mock `resolveWahaTarget`. Use vi.hoisted pattern. Also mock `openclaw/plugin-sdk` (required by channel.ts imports). Follow the exact mock pattern from tests/read-messages.test.ts.
    5. Run `npm test` to verify all new + existing tests pass.

    IMPORTANT: Do NOT change function signatures. Only add `export` keyword. Read the DO NOT CHANGE comments in both files before editing.
  </action>
  <verify>
    <automated>npm test -- tests/send-utils.test.ts tests/channel-utils.test.ts --reporter=verbose</automated>
  </verify>
  <done>fuzzyScore (6 tests), toArr (4 tests), resolveChatId (4 tests), autoResolveTarget (3 tests) all pass. All 137+ existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write integration tests for send, poll, edit, search action handlers</name>
  <files>tests/action-handlers.test.ts</files>
  <behavior>
    send handler:
    - Happy path: handleAction("send", {text:"hello"}, cfg, toolContext) calls sendWahaText with correct chatId and text
    - Error path: handleAction("send", {text:"hello"}, cfg, toolContext) with sendWahaText throwing returns error result

    poll handler:
    - Happy path: handleAction("poll", {poll:{name:"Vote",options:["A","B"]}}, cfg, toolContext) calls sendWahaPoll with correct params
    - Error path: sendWahaPoll throws, returns error result

    edit handler:
    - Happy path: handleAction("edit", {messageId:"true_xxx_yyy", text:"edited"}, cfg, toolContext) calls editWahaMessage
    - Error path: editWahaMessage throws, returns error result

    search handler:
    - Happy path: handleAction("search", {query:"test"}, cfg) calls resolveWahaTarget and returns results
    - Error path: resolveWahaTarget throws, returns error result
  </behavior>
  <action>
    1. Create tests/action-handlers.test.ts following the exact vi.mock chain from tests/read-messages.test.ts:
       - vi.hoisted for mockSendWahaText, mockSendWahaPoll, mockEditWahaMessage, mockResolveWahaTarget
       - vi.mock("openclaw/plugin-sdk") -- copy the full mock from read-messages.test.ts
       - vi.mock("../src/send.js") -- override sendWahaText, sendWahaPoll, editWahaMessage, resolveWahaTarget
       - vi.mock("../src/secret-input.js") -- return { getSecretInput: vi.fn() }
       - vi.mock("../src/http-client.js") -- return { callWahaApi: vi.fn(), warnOnError: vi.fn(), configureReliability: vi.fn() }
       - Import handleAction from "../src/channel.js" AFTER all mocks
    2. Create makeCfg helper (copy from role-guardrail.test.ts pattern).
    3. Write 8 tests total: 1 happy path + 1 error path per handler (send, poll, edit, search).
    4. For search handler: the action uses resolveWahaTarget directly (mode "none", no target). Mock resolveWahaTarget to return results array.
    5. Run full test suite to verify no regressions.

    IMPORTANT: The `search` action has mode "none" -- it does NOT receive a target/chatId from the gateway. It only receives parameters like {query: "..."}.
  </action>
  <verify>
    <automated>npm test -- tests/action-handlers.test.ts --reporter=verbose</automated>
  </verify>
  <done>8 integration tests pass (2 per handler: send, poll, edit, search). Full suite of 137+ existing tests still green.</done>
</task>

</tasks>

<verification>
Run full test suite after both tasks:
```
npm test
```
All tests (existing 137+ new ~25) must pass. Zero regressions from export additions.
</verification>

<success_criteria>
- tests/send-utils.test.ts exists with ~10 passing tests for fuzzyScore and toArr
- tests/channel-utils.test.ts exists with ~7 passing tests for resolveChatId and autoResolveTarget
- tests/action-handlers.test.ts exists with 8 passing tests for send, poll, edit, search handlers
- src/send.ts has `export function fuzzyScore` (was `function fuzzyScore`)
- src/channel.ts has `export function resolveChatId` and `export async function autoResolveTarget`
- `npm test` passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-and-testing/05-01-SUMMARY.md`
</output>
