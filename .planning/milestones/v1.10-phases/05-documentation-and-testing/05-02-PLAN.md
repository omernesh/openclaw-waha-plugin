---
phase: 05-documentation-and-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - README.md
autonomous: true
requirements:
  - DOC-01
  - DOC-04
must_haves:
  truths:
    - "SKILL.md contains error scenario guidance so Sammie knows what to do when sends fail, rate limits hit, or sessions disconnect"
    - "SKILL.md contains rate limit awareness section explaining automatic retry behavior and when to wait"
    - "SKILL.md contains multi-session examples showing triggerWord usage, session roles, readMessages, and cross-session routing"
    - "README.md reflects current version with installation steps, full configuration reference (including Phase 1-4 fields), deployment to both hpg6 locations, and troubleshooting"
  artifacts:
    - path: "SKILL.md"
      provides: "Agent-facing documentation with error handling, rate limits, multi-session"
      contains: "Error Handling"
    - path: "README.md"
      provides: "User-facing setup and configuration guide"
      contains: "Multi-Session"
  key_links:
    - from: "SKILL.md"
      to: "src/channel.ts"
      via: "Documents actions registered in listActions/handleAction"
      pattern: "readMessages|sendMulti|muteChat"
    - from: "README.md"
      to: "package.json"
      via: "Version and npm package name"
      pattern: "waha-openclaw-channel"
---

<objective>
Refresh SKILL.md with error scenarios, rate limit guidance, and multi-session examples. Update README.md with current version, full configuration reference, deployment instructions, and troubleshooting guide.

Purpose: Satisfy DOC-01 (SKILL.md refresh for Sammie's awareness of new capabilities and error handling) and DOC-04 (README.md as onboarding guide for new users). These are the final documentation deliverables for the milestone.

Output: Updated SKILL.md (v4.0.0) and README.md (current version) with complete coverage of Phases 1-4 features.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-documentation-and-testing/05-RESEARCH.md

@SKILL.md
@README.md
@CLAUDE.md
@src/config-schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh SKILL.md with error scenarios, rate limits, and multi-session documentation</name>
  <files>SKILL.md</files>
  <action>
    Read the current SKILL.md completely first. Then add three new sections while preserving all existing content. Bump version to 4.0.0.

    1. **Add "Error Handling and Recovery" section** after the existing action reference tables:
       - Table format with columns: Error Pattern | What Happened | What To Do
       - Include these scenarios (from 05-RESEARCH.md):
         - "Session '...' has sub-role 'listener' and cannot send" -> Use bot session
         - "Could not resolve '...' to a WhatsApp JID" -> Use search action first
         - "Ambiguous target '...'. Possible matches:" -> Use exact JID from search
         - "WAHA API rate limited (429)" -> Wait ~1s, plugin retries 3x automatically
         - "timed out after 30000ms" -> Check WAHA health, mutation ops may have succeeded
         - "Session health: unhealthy" -> Reconnect session in WAHA dashboard

    2. **Add "Rate Limiting" section** near the error handling section:
       - Explain the token bucket rate limiter (~20 req/s default, configurable)
       - Explain automatic 429 retry with exponential backoff (1s/2s/4s, max 3 retries)
       - Note that presence/typing calls are deprioritized (not exempt)
       - Guidance: "If you see rate limit errors, wait a few seconds before retrying. The plugin handles most rate limits automatically."

    3. **Add "Multi-Session" section** covering:
       - Session roles: bot (full-access) vs human (listener)
       - Trigger word activation: `!sammie` prefix in any chat triggers bot processing
       - Cross-session routing: bot sends from its own session when it's a group member, falls back to user's session otherwise
       - readMessages action: read recent messages from monitored chats (limit 1-50, default 10)
       - Example trigger word usage in a group
       - Example readMessages call

    4. **Update the action reference tables** to include new actions from Phases 3-4:
       - muteChat, unmuteChat (Phase 3)
       - sendMulti (Phase 3)
       - sendLinkPreview (Phase 3)
       - readMessages (Phase 4)

    5. Bump the version in SKILL.md frontmatter from 3.3.0 to 4.0.0.

    IMPORTANT: Preserve ALL existing content. Only ADD sections and update version. Do NOT remove or rewrite existing action tables, quick reference, or examples unless they contain incorrect information that conflicts with Phase 1-4 changes.
  </action>
  <verify>
    <automated>grep -c "Error Handling" SKILL.md && grep -c "Rate Limit" SKILL.md && grep -c "Multi-Session" SKILL.md && grep -c "readMessages" SKILL.md && grep "version:" SKILL.md | head -1</automated>
  </verify>
  <done>SKILL.md v4.0.0 contains: Error Handling and Recovery section with 6+ error scenarios, Rate Limiting section explaining automatic retry, Multi-Session section with trigger word and readMessages examples, updated action tables with muteChat/unmuteChat/sendMulti/sendLinkPreview/readMessages.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md with current version, config reference, deploy guide, and troubleshooting</name>
  <files>README.md</files>
  <action>
    Read the current README.md completely first. Then update to reflect current state (v1.11.0 with Phases 1-4).

    1. **Update version references** from 1.9.4 to 1.11.0 throughout.

    2. **Update/add Configuration section** with ALL current config fields:
       - Base config: baseUrl, apiKey, session, enabled (existing)
       - Reliability config (Phase 1): rateLimiter (tokensPerSecond, maxBurst), timeout (requestTimeoutMs), retry (maxRetries, backoffBaseMs, backoffMaxMs)
       - Resilience config (Phase 2): healthCheck (enabled, intervalMs, unhealthyThreshold), inboundQueue (maxSize)
       - Feature config (Phase 3): autoLinkPreview (boolean, default true)
       - Multi-session config (Phase 4): sessions array [{name, sessionId, role, subRole}], triggerWord, triggerResponseMode
       - Provide a complete JSON example showing all config fields

    3. **Update/add Deployment section** making BOTH hpg6 locations prominent:
       - Runtime: `~/.openclaw/extensions/waha/`
       - Dev/workspace: `~/.openclaw/workspace/skills/waha-openclaw-channel/`
       - Step-by-step: build, scp to BOTH, restart gateway
       - Emphasize: "ALWAYS deploy to BOTH locations -- forgetting one causes silent version mismatch"

    4. **Add Troubleshooting section** covering:
       - "Session disconnected" -> Check admin panel Status tab, reconnect in WAHA dashboard
       - "Rate limited (429)" -> Plugin retries automatically; if persistent, reduce tokensPerSecond
       - "Listener cannot send" -> Expected behavior; use bot session for sending
       - "Target not resolved" -> Use search action first, then exact JID
       - "Gateway restart didn't help" -> Check both deploy locations match
       - How to check logs: `journalctl --user -u openclaw-gateway --since "5 minutes ago" --no-pager`

    5. **Update Features section** to mention Phases 1-4 additions:
       - Reliability: timeouts, rate limiting, retry with backoff
       - Resilience: session health monitoring, inbound queue
       - Features: link previews, mute/unmute, mentions, multi-send
       - Multi-session: roles, trigger words, cross-session routing

    6. **Update npm install instructions** if needed (package name: waha-openclaw-channel).

    Read src/config-schema.ts to get the exact config field names and defaults before writing the configuration section.
  </action>
  <verify>
    <automated>grep -c "1.11.0" README.md && grep -c "Troubleshooting" README.md && grep -c "triggerWord" README.md && grep -c "extensions/waha" README.md</automated>
  </verify>
  <done>README.md references v1.11.0, contains complete Configuration section with all Phase 1-4 fields, Deployment section emphasizing both hpg6 locations, Troubleshooting section with 6+ common issues, and updated Features list.</done>
</task>

</tasks>

<verification>
Manual review: read SKILL.md and README.md to confirm all required sections present and accurate.
Automated spot checks:
```bash
grep "Error Handling" SKILL.md
grep "Rate Limit" SKILL.md
grep "Multi-Session" SKILL.md
grep "4.0.0" SKILL.md
grep "1.11.0" README.md
grep "Troubleshooting" README.md
grep "triggerWord" README.md
```
</verification>

<success_criteria>
- SKILL.md version bumped to 4.0.0
- SKILL.md has Error Handling and Recovery section with 6+ error scenarios in table format
- SKILL.md has Rate Limiting section explaining automatic retry behavior
- SKILL.md has Multi-Session section with trigger word and readMessages examples
- SKILL.md action tables updated with muteChat, unmuteChat, sendMulti, sendLinkPreview, readMessages
- README.md references v1.11.0 (not 1.9.4)
- README.md has complete Configuration section with reliability, resilience, multi-session fields
- README.md has Deployment section emphasizing both hpg6 locations
- README.md has Troubleshooting section with 6+ common issues
</success_criteria>

<output>
After completion, create `.planning/phases/05-documentation-and-testing/05-02-SUMMARY.md`
</output>
