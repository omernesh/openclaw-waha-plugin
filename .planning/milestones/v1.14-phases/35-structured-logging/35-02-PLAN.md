---
phase: 35-structured-logging
plan: 02
type: execute
wave: 2
depends_on:
  - "35-01"
files_modified:
  - src/monitor.ts
  - src/sync.ts
  - src/directory.ts
  - src/health.ts
  - src/http-client.ts
  - src/send.ts
  - src/channel.ts
  - src/inbound-queue.ts
  - src/accounts.ts
  - src/analytics.ts
  - src/shutup.ts
  - src/auto-reply.ts
  - src/dm-filter.ts
  - src/error-formatter.ts
  - src/policy-edit.ts
  - src/policy-enforcer.ts
  - src/rules-resolver.ts
  - src/rules-loader.ts
  - src/signature.ts
  - src/media.ts
  - src/config-io.ts
autonomous: true
requirements:
  - OBS-01

must_haves:
  truths:
    - "Zero console.log/warn/error calls remain in production src/*.ts files"
    - "Every source file uses logger.child({ component }) for its logs"
    - "Log output includes component name matching the source file"
    - "Logger level is initialized from config logLevel field on startup"
  artifacts:
    - path: "src/monitor.ts"
      provides: "Structured logging for webhook server, admin API, health"
      contains: "logger.child"
    - path: "src/sync.ts"
      provides: "Structured logging for sync cycles"
      contains: "logger.child"
  key_links:
    - from: "src/monitor.ts"
      to: "src/logger.ts"
      via: "import { createLogger } from './logger.js'"
      pattern: "from.*logger"
    - from: "src/monitor.ts"
      to: "src/logger.ts"
      via: "setLogLevel called when config loads"
      pattern: "setLogLevel"
---

<objective>
Replace all 151 console.log/warn/error calls across 21 source files with structured logger calls.

Purpose: Complete the structured logging migration — after this, all log output is JSON with consistent fields.
Output: All source files use logger.child({ component }) pattern, zero console.* calls remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-structured-logging/35-01-SUMMARY.md

<interfaces>
From src/logger.ts (created in Plan 01):
```typescript
export type LogLevel = "debug" | "info" | "warn" | "error";
export function createLogger(opts?: { level?: LogLevel; component?: string; [key: string]: unknown }): Logger;
export function setLogLevel(level: LogLevel): void;
export const logger: Logger;
// Logger interface: { debug(msg, extra?), info(msg, extra?), warn(msg, extra?), error(msg, extra?), child(fields) }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace console.* in high-volume files (monitor, sync, directory, health, http-client, send)</name>
  <files>src/monitor.ts, src/sync.ts, src/directory.ts, src/health.ts, src/http-client.ts, src/send.ts</files>
  <read_first>src/logger.ts (to confirm exports), src/monitor.ts (heaviest file — 60 calls), src/sync.ts, src/directory.ts, src/health.ts, src/http-client.ts, src/send.ts</read_first>
  <action>
    For each file:
    1. Add import: `import { createLogger } from "./logger.js";`
    2. Create file-level child logger: `const log = createLogger({ component: "monitor" });` (use filename without extension as component)
    3. Replace every `console.log(...)` with `log.info(...)`, `console.warn(...)` with `log.warn(...)`, `console.error(...)` with `log.error(...)`
    4. Convert string template logs to structured fields where practical:
       - `console.log(\`[waha] sync completed in ${ms}ms\`)` becomes `log.info("sync completed", { durationMs: ms })`
       - `console.warn(\`[waha] fetch failed for ${session}: ${err}\`)` becomes `log.warn("fetch failed", { sessionId: session, error: String(err) })`
    5. Strip `[waha]` / `[WAHA]` prefixes — the component field replaces them
    6. For monitor.ts: call `setLogLevel(config.logLevel)` where config is loaded/updated (in the config POST handler and on startup). Import `setLogLevel` from logger.js.

    Counts to verify: monitor.ts (60), sync.ts (15), directory.ts (14), health.ts (10), http-client.ts (8), send.ts (8) = 115 replacements.

    IMPORTANT: Preserve all DO NOT CHANGE / DO NOT REMOVE comments. Only change the console.* calls themselves.
  </action>
  <verify>
    <automated>grep -rc "console\.\(log\|warn\|error\)" src/monitor.ts src/sync.ts src/directory.ts src/health.ts src/http-client.ts src/send.ts | grep -v ":0$" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep returns 0 (no files with remaining console.* calls)
    - `npx tsc --noEmit` passes with no errors
  </acceptance_criteria>
  <done>
    Zero console.log/warn/error in these 6 files. All use logger.child with appropriate component names. setLogLevel wired in monitor.ts config loading.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace console.* in remaining 15 files + final verification</name>
  <files>src/channel.ts, src/inbound-queue.ts, src/accounts.ts, src/analytics.ts, src/shutup.ts, src/auto-reply.ts, src/dm-filter.ts, src/error-formatter.ts, src/policy-edit.ts, src/policy-enforcer.ts, src/rules-resolver.ts, src/rules-loader.ts, src/signature.ts, src/media.ts, src/config-io.ts</files>
  <read_first>Each file briefly to identify console.* patterns (most have 1-5 calls each)</read_first>
  <action>
    For each of the 15 remaining files:
    1. Add import: `import { createLogger } from "./logger.js";`
    2. Create file-level child logger: `const log = createLogger({ component: "<filename>" });`
    3. Replace all console.log/warn/error with log.info/warn/error
    4. Convert string templates to structured fields where practical
    5. Strip [waha] prefixes

    File-specific counts: channel (6), inbound-queue (3), accounts (1), analytics (1), shutup (3), auto-reply (1), dm-filter (2), error-formatter (2), policy-edit (1), policy-enforcer (2), rules-resolver (3), rules-loader (5), signature (1), media (4), config-io (1) = 36 replacements.

    After all replacements, run a final sweep:
    - `grep -rn "console\.\(log\|warn\|error\)" src/*.ts` should return ZERO results (excluding test files and .bak files)

    IMPORTANT: Do NOT modify test files (*.test.ts). Only production source files.
    IMPORTANT: Preserve all DO NOT CHANGE / DO NOT REMOVE comments.
  </action>
  <verify>
    <automated>bash -c "count=$(grep -r 'console\.\(log\|warn\|error\)' src/*.ts 2>/dev/null | grep -v '\.test\.' | grep -v '\.bak\.' | wc -l); echo \"Remaining console calls: $count\"; test $count -eq 0 && npx tsc --noEmit"</automated>
  </verify>
  <acceptance_criteria>
    - Zero console.log/warn/error in any src/*.ts production file
    - TypeScript compilation passes
    - Every modified file has `import { createLogger } from "./logger.js"`
    - Every modified file has a `const log = createLogger({ component: "..." })` line
  </acceptance_criteria>
  <done>
    All 151 console.* calls across 21 files replaced with structured logger calls. Zero console.* remain in production code. TypeScript compiles clean.
  </done>
</task>

</tasks>

<verification>
- `grep -rn "console\.\(log\|warn\|error\)" src/*.ts | grep -v test | grep -v bak` returns empty
- `npx tsc --noEmit` passes
- `grep -rc "createLogger" src/*.ts | grep -v ":0$" | wc -l` returns 21 (all files using logger)
</verification>

<success_criteria>
Zero console.* calls in production source files. All logging goes through structured JSON logger with component fields. TypeScript compiles. setLogLevel wired to config.
</success_criteria>

<output>
After completion, create `.planning/phases/35-structured-logging/35-02-SUMMARY.md`
</output>
