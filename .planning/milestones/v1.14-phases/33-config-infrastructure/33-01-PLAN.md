---
phase: 33-config-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config-io.ts
  - src/config-io.test.ts
autonomous: true
requirements:
  - CON-01
  - MEM-01
  - DI-02

must_haves:
  truths:
    - "Config reads use async fs/promises, never readFileSync"
    - "Config writes use async fs/promises, never writeFileSync"
    - "Concurrent config writes are serialized through a promise-based mutex"
    - "Config writes use write-to-temp-then-rename so a crash mid-write leaves the previous valid file intact"
    - "Backup rotation happens before each write (3 rolling backups)"
  artifacts:
    - path: "src/config-io.ts"
      provides: "Config I/O module with mutex, async I/O, atomic writes"
      exports: ["readConfig", "writeConfig", "withConfigMutex"]
    - path: "src/config-io.test.ts"
      provides: "Tests for config-io module"
      min_lines: 50
  key_links:
    - from: "src/config-io.ts"
      to: "fs/promises"
      via: "import { readFile, writeFile, rename, copyFile, stat, unlink } from 'node:fs/promises'"
      pattern: "from ['\"]node:fs/promises['\"]"
    - from: "src/config-io.ts"
      to: "mutex"
      via: "internal promise-based mutex serializing all config writes"
      pattern: "configMutex|withConfigMutex"
---

<objective>
Create a standalone config I/O module (`src/config-io.ts`) that provides mutex-guarded, async, atomic config file operations.

Purpose: Foundation for replacing all sync config reads/writes across monitor.ts and sync.ts. Centralizes the read-modify-write pattern with safety guarantees.
Output: `src/config-io.ts` with exported functions, `src/config-io.test.ts` with passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create config-io module with mutex, async I/O, and atomic writes</name>
  <files>src/config-io.ts, src/config-io.test.ts</files>
  <read_first>
    - src/monitor.ts (lines 242-304 — getConfigPath, rotateConfigBackups, syncAllowListBatch patterns)
    - src/sync.ts (lines 186-213 — syncExpiredToConfig pattern)
  </read_first>
  <behavior>
    - Test 1: `readConfig(path)` returns parsed JSON object from file
    - Test 2: `readConfig(path)` throws on missing file
    - Test 3: `writeConfig(path, data)` writes JSON to a `.tmp` file then renames to target
    - Test 4: `writeConfig(path, data)` creates 3 rolling backups before writing
    - Test 5: `withConfigMutex(fn)` serializes concurrent calls — second call waits for first to complete
    - Test 6: `modifyConfig(path, fn)` does atomic read-modify-write under mutex
    - Test 7: Atomic write leaves original file intact if process crashes mid-write (simulated by failing writeFile)
  </behavior>
  <action>
Create `src/config-io.ts` with the following exports:

1. **`getConfigPath(): string`** — moved from monitor.ts. Returns `process.env.OPENCLAW_CONFIG_PATH ?? join(homedir(), '.openclaw', 'openclaw.json')`.

2. **Internal mutex** — a simple promise-chain mutex:
```typescript
let configMutexChain = Promise.resolve();
function withConfigMutex<T>(fn: () => Promise<T>): Promise<T> {
  const result = configMutexChain.then(fn, fn);
  configMutexChain = result.then(() => {}, () => {});
  return result;
}
```
Export `withConfigMutex` for callers that need custom read-modify-write flows.

3. **`readConfig(configPath: string): Promise<Record<string, unknown>>`** — uses `fs/promises.readFile(configPath, 'utf-8')`, returns `JSON.parse(raw)`.

4. **`writeConfig(configPath: string, config: Record<string, unknown>): Promise<void>`** — atomic write:
   - Call `rotateConfigBackups(configPath)` (async version)
   - Write to `configPath + '.tmp'` using `fs/promises.writeFile`
   - `fs/promises.rename(configPath + '.tmp', configPath)` to atomically swap
   - JSON formatted with 2-space indent: `JSON.stringify(config, null, 2)`

5. **`rotateConfigBackups(configPath: string): Promise<void>`** — async version of existing function:
   - Uses `fs/promises.rename` and `fs/promises.copyFile`
   - Uses `fs/promises.stat` to check existence (not existsSync)
   - Same .bak.1/.bak.2/.bak.3 rotation logic
   - Failure is non-fatal (catch and console.warn)

6. **`modifyConfig(configPath: string, fn: (config: Record<string, unknown>) => Record<string, unknown> | void): Promise<void>`** — convenience wrapper:
   - Acquires mutex via `withConfigMutex`
   - Reads config with `readConfig`
   - Calls `fn(config)` — if fn returns a new object, use that; if void, use the mutated original
   - Writes result with `writeConfig`

All imports use `node:fs/promises` and `node:path` and `node:os`. Zero sync fs calls.

Create `src/config-io.test.ts` using vitest:
- Use `vi.mock('node:fs/promises')` to mock file operations
- Test each behavior listed above
- For mutex test: create two overlapping `modifyConfig` calls and verify they serialize (second waits for first)
- For atomic write crash test: mock `writeFile` to throw, verify original file is NOT renamed/deleted
  </action>
  <verify>
    <automated>npx vitest run src/config-io.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/config-io.ts exists and contains `export function readConfig(`
    - src/config-io.ts contains `export function writeConfig(`
    - src/config-io.ts contains `export function modifyConfig(`
    - src/config-io.ts contains `export function withConfigMutex(`
    - src/config-io.ts contains `export function getConfigPath(`
    - src/config-io.ts contains `from "node:fs/promises"` (async I/O, MEM-01)
    - src/config-io.ts contains `.tmp` and `rename(` (atomic write, DI-02)
    - src/config-io.ts contains `configMutexChain` or equivalent mutex pattern (CON-01)
    - src/config-io.ts does NOT contain `readFileSync` or `writeFileSync`
    - src/config-io.ts contains `rotateConfigBackups` with `.bak.1`, `.bak.2`, `.bak.3`
    - src/config-io.test.ts exists with at least 5 test cases
    - `npx vitest run src/config-io.test.ts` exits 0
  </acceptance_criteria>
  <done>Config-io module exists with mutex, async I/O, atomic writes, backup rotation, and all tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run src/config-io.test.ts` — all tests pass
- `grep -c "readFileSync\|writeFileSync" src/config-io.ts` returns 0
- `grep -c "from.*node:fs/promises" src/config-io.ts` returns 1+
- `grep -c "\.tmp" src/config-io.ts` returns 1+ (atomic write pattern)
- `grep -c "configMutex" src/config-io.ts` returns 1+ (mutex)
</verification>

<success_criteria>
- config-io.ts module is a self-contained config I/O utility with zero sync filesystem calls
- All 7 behaviors tested and passing
- Module exports are ready for consumption by monitor.ts and sync.ts in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/33-config-infrastructure/33-01-SUMMARY.md`
</output>
