---
phase: 36-timeout-error-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/monitor.ts
  - src/media.ts
  - src/rate-limiter.ts
autonomous: true
requirements: [EH-01, EH-02, EH-03, EH-04, API-03]

must_haves:
  truths:
    - "No fetch() call in monitor.ts or media.ts can hang indefinitely"
    - "RateLimiter rejects new work when queue is full"
    - "Nominatim geocode respects 1-req/sec rate limit"
  artifacts:
    - path: "src/monitor.ts"
      provides: "Timeout-protected fetch calls in fetchBotJids, /api/admin/sessions, follow/unfollow"
      contains: "AbortSignal.timeout"
    - path: "src/media.ts"
      provides: "Timeout-protected downloadWahaMedia, Gemini polling, Nominatim geocode"
      contains: "AbortSignal.timeout"
    - path: "src/rate-limiter.ts"
      provides: "maxQueue parameter with queue overflow rejection"
      contains: "maxQueue"
  key_links:
    - from: "src/media.ts"
      to: "src/rate-limiter.ts"
      via: "Nominatim geocode rate limiter instance"
      pattern: "new RateLimiter"
---

<objective>
Add explicit timeouts to all bare fetch() calls and queue bounds to RateLimiter.

Purpose: Prevent indefinite hangs from unresponsive external services (WAHA, Gemini, Nominatim).
Output: All outbound HTTP calls have AbortSignal.timeout; RateLimiter has maxQueue overflow protection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/monitor.ts
@src/media.ts
@src/rate-limiter.ts
@src/http-client.ts (reference — callWahaApi already uses AbortSignal.timeout)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add maxQueue to RateLimiter + add AbortSignal.timeout to all bare fetch() calls</name>
  <files>src/rate-limiter.ts, src/monitor.ts, src/media.ts</files>
  <read_first>src/rate-limiter.ts, src/monitor.ts, src/media.ts, src/http-client.ts</read_first>
  <action>
**rate-limiter.ts (EH-04):**
- Add optional `maxQueue` parameter (3rd constructor arg, default `Infinity`)
- In `acquire()`, when `activeCount >= maxConcurrent` and `queue.length >= maxQueue`, throw `new Error("Rate limiter queue full (maxQueue=${this.maxQueue})")` instead of queuing
- Keep existing behavior unchanged when maxQueue is not provided

**monitor.ts (EH-01) — 3 bare fetch() calls to fix:**
1. `fetchBotJids()` (~line 336): Add `signal: AbortSignal.timeout(30_000)` to the fetch options
2. `/api/admin/sessions` handler (~line 1350): Add `signal: AbortSignal.timeout(30_000)` to the fetch options
3. Bulk follow/unfollow (~line 1662): Add `signal: AbortSignal.timeout(30_000)` to the fetch options

**media.ts (EH-02, EH-03, API-03) — 6 bare fetch() calls to fix:**
1. `downloadWahaMedia()` (~line 67): Add `signal: AbortSignal.timeout(30_000)` (EH-02)
2. LiteLLM vision API (~line 144): Add `signal: AbortSignal.timeout(60_000)` (vision analysis can be slow)
3. Gemini upload (~line 218): Add `signal: AbortSignal.timeout(60_000)` (video upload can be large)
4. Gemini status polling (~line 242): Add `signal: AbortSignal.timeout(5_000)` per poll request (EH-03)
5. Gemini generateContent (~line 254): Add `signal: AbortSignal.timeout(60_000)` (generation can be slow)
6. Nominatim geocode (~line 310): Add `signal: AbortSignal.timeout(5_000)`. Also create a module-level `const nominatimLimiter = new RateLimiter(1, 1100)` (1 concurrent, 1100ms delay = ~1 req/sec) and wrap the fetch in `nominatimLimiter.run()` (API-03)

Preserve all existing DO NOT CHANGE comments. Add a brief comment above each timeout noting why it exists.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "AbortSignal.timeout" src/monitor.ts returns 3 matches (fetchBotJids, sessions, follow/unfollow)
    - grep "AbortSignal.timeout" src/media.ts returns 6 matches (download, litellm, upload, polling, generate, nominatim)
    - grep "maxQueue" src/rate-limiter.ts returns at least 2 matches (constructor param + overflow check)
    - grep "nominatimLimiter" src/media.ts returns at least 2 matches (declaration + usage)
    - TypeScript compiles clean
  </acceptance_criteria>
  <done>All 9 bare fetch() calls have explicit timeouts. RateLimiter supports maxQueue. Nominatim has 1-req/sec rate limiting.</done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for RateLimiter maxQueue</name>
  <files>src/rate-limiter.test.ts</files>
  <read_first>src/rate-limiter.ts</read_first>
  <action>
Create `src/rate-limiter.test.ts` with tests:
1. "maxQueue defaults to Infinity — unbounded queue works as before" — create RateLimiter(1, 0), acquire 1 slot, queue 10 more, release, verify all resolve
2. "maxQueue rejects when queue is full" — create RateLimiter(1, 0, 2), acquire 1 slot (active), queue 2 more (fills queue), attempt 4th acquire — expect it to throw with "queue full"
3. "maxQueue allows exactly maxQueue items in queue" — create RateLimiter(1, 0, 1), acquire 1, queue 1 more (should succeed), attempt 3rd (should throw)
4. "release drains queue and allows new entries" — create RateLimiter(1, 0, 1), acquire 1, queue 1 (ok), release, then queue another 1 (should succeed since queue drained)

Use vitest (import { describe, it, expect } from 'vitest').
  </action>
  <verify>
    <automated>npx vitest run src/rate-limiter.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 tests pass
    - Tests cover: default unbounded, rejection at maxQueue, exact boundary, drain-and-requeue
  </acceptance_criteria>
  <done>RateLimiter maxQueue behavior is tested. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero errors
- `npx vitest run src/rate-limiter.test.ts` — all tests pass
- `grep -c "AbortSignal.timeout" src/monitor.ts` — returns 3
- `grep -c "AbortSignal.timeout" src/media.ts` — returns 6
- `grep "maxQueue" src/rate-limiter.ts` — present in constructor and acquire
</verification>

<success_criteria>
Every outbound fetch() in monitor.ts and media.ts has an explicit AbortSignal.timeout. RateLimiter supports maxQueue with overflow rejection. Nominatim geocode is rate-limited to 1 req/sec. All changes compile clean and tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/36-timeout-error-hardening/36-01-SUMMARY.md`
</output>
