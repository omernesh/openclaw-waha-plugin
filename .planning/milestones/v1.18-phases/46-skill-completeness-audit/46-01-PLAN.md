---
phase: 46-skill-completeness-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/whatsapp-messenger/SKILL.md
autonomous: true
requirements:
  - SKL-01
  - SKL-02
  - SKL-03

must_haves:
  truths:
    - "Every ACTION_HANDLERS key in channel.ts has a corresponding entry in SKILL.md"
    - "Every STANDARD_ACTIONS name (send, poll, react, edit, unsend, pin, unpin, read, delete, reply) is documented"
    - "The /join, /leave, /list slash commands are documented with syntax and behavior"
    - "Endpoints are organized by category: messaging, chat management, groups, contacts, channels, labels, status, presence, profile, media, calls, LID, API keys, policy"
    - "No hijacked/internal endpoints appear (sendText used for human behavior, sendWahaMedia internal, detectMimeViaHead, etc.)"
  artifacts:
    - path: "skills/whatsapp-messenger/SKILL.md"
      provides: "Complete whatsapp-messenger skill with all implemented endpoints"
      contains: "## Slash Commands"
  key_links:
    - from: "skills/whatsapp-messenger/SKILL.md"
      to: "src/channel.ts ACTION_HANDLERS"
      via: "action names match exactly"
      pattern: "joinGroup|getInviteCode|revokeInviteCode|editPolicy|search"
---

<objective>
Rewrite skills/whatsapp-messenger/SKILL.md to document every implemented WAHA API endpoint.

Purpose: The current skill only documents 5 endpoints (sendText, readMessages, sendPoll, sendReaction, sendLocation, listGroups). Over 100 actions are implemented in channel.ts. Agents using this skill are blind to ~95% of available capabilities.

Output: A comprehensive, category-organized SKILL.md that lists every action in EXPOSED_ACTIONS plus every ACTION_HANDLERS key, with parameters and a usage example per category. Also documents /join, /leave, /list slash commands from Phase 43.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/channel.ts
@src/commands.ts
@skills/whatsapp-messenger/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all actions and rewrite SKILL.md with complete endpoint coverage</name>
  <files>skills/whatsapp-messenger/SKILL.md</files>
  <action>
Read src/channel.ts fully to extract:
1. STANDARD_ACTIONS list (send, poll, react, edit, unsend, pin, unpin, read, delete, reply)
2. Every key in ACTION_HANDLERS (lines 155-374)
3. UTILITY_ACTIONS list to understand what is exposed vs. internal-only

Read src/commands.ts to extract /join, /leave, /list command syntax.

Then rewrite skills/whatsapp-messenger/SKILL.md with this structure:

**Keep the existing frontmatter** (name, description, version) and Connection Details + Omer's Contact Details sections unchanged.

Replace the "Operations" section with a comprehensive "Available Actions" section organized by these categories. For each action, list: action name, key parameters, one-line description. Include a curl example only for the primary send action — for the rest, a parameter table or brief inline note is sufficient.

Categories and actions to document:

**Messaging (standard actions — support target resolution):**
- send: chatId/to, text — send text message
- reply: chatId, messageId, text — reply to specific message
- poll: chatId, name, options[], multipleAnswers — create poll
- react: chatId, messageId, reaction (emoji) — react to message
- edit: chatId, messageId, text — edit sent message
- unsend / delete: chatId, messageId — delete message

**Rich Messages:**
- sendPoll: chatId, name, options[], multipleAnswers
- sendPollVote: chatId, pollMessageId, votes[]
- sendLocation: chatId, latitude, longitude, title
- sendContactVcard: chatId, contacts[]
- sendList: chatId, title, description, buttonText, sections[]
- sendLinkPreview: chatId, url, title, description, image
- sendButtonsReply: chatId, messageId, buttonId
- sendEvent: chatId, name, startTime, endTime, description, location
- forwardMessage: chatId, messageId
- sendMulti: chatId[] (up to 10), text — multi-recipient

**Media:**
- sendImage: chatId/to, image/url/file, caption
- sendVideo: chatId/to, video/url/file, caption
- sendFile: chatId/to, file/url, caption
- pin / unpin: chatId, messageId — pin/unpin message
- starMessage: chatId, messageId, star (bool)

**Chat Management:**
- read / readChatMessages: chatId — mark read
- getChats: — list all chats
- getChatsOverview: page, limit — paginated overview
- getChatMessages: chatId, limit, offset — get messages
- getChatMessage: chatId, messageId — single message
- readMessages: chatId, limit (default 10, max 50) — lean format for LLM
- deleteChat: chatId — delete chat
- clearChatMessages: chatId — clear history
- archiveChat / unarchiveChat: chatId
- unreadChat: chatId — mark unread
- getChatPicture: chatId
- muteChat: chatId, duration (seconds, optional)
- unmuteChat: chatId

**Groups:**
- createGroup: name, participants[]
- getGroups: — list all groups (returns dict, use Object.values)
- getGroup: groupId
- getGroupsCount: — count
- getGroupJoinInfo: groupId — preview before joining
- getParticipants: groupId
- deleteGroup: groupId — disband group (admin only)
- leaveGroup: groupId — leave group
- setGroupSubject: groupId, subject
- setGroupDescription: groupId, description
- setGroupPicture: groupId, file (URL)
- deleteGroupPicture: groupId
- getGroupPicture: groupId
- addParticipants: groupId, participants[]
- removeParticipants: groupId, participants[]
- promoteToAdmin: groupId, participants[]
- demoteFromAdmin: groupId, participants[]
- setInfoAdminOnly: groupId, adminOnly (bool)
- getInfoAdminOnly: groupId
- setMessagesAdminOnly: groupId, adminOnly (bool)
- getMessagesAdminOnly: groupId
- getInviteCode: groupId — returns invite link
- revokeInviteCode: groupId — invalidate current link
- joinGroup: inviteCode — join via invite code/link
- refreshGroups: — refresh local group cache

**Contacts:**
- getContacts: — list all contacts
- getContact: contactId (JID)
- checkContactExists: phone (number only, no @c.us)
- getContactAbout: contactId
- getContactPicture: contactId
- blockContact: contactId
- unblockContact: contactId

**Channels (Newsletters):**
- getChannels: — list followed channels
- createChannel: name, description, picture
- getChannel: channelId
- deleteChannel: channelId
- followChannel: channelId
- unfollowChannel: channelId
- muteChannel: channelId
- unmuteChannel: channelId
- searchChannelsByText: query
- previewChannelMessages: channelId
- searchChannelsByView: viewType (e.g., "RECOMMENDED")
- getChannelSearchViews: — available view types
- getChannelSearchCountries: — available countries
- getChannelSearchCategories: — available categories

**Labels:**
- getLabels: — list all labels
- createLabel: name, color (int, optional)
- updateLabel: labelId, name, color
- deleteLabel: labelId
- getChatLabels: chatId
- setChatLabels: chatId, labels[] ({id})
- getChatsByLabel: labelId

**Status / Stories:**
- sendTextStatus: text, backgroundColor, font (int)
- sendImageStatus: image (URL), caption
- sendVoiceStatus: voice (URL)
- sendVideoStatus: video (URL), caption
- deleteStatus: id

**Presence:**
- setPresenceStatus: status ("online" | "offline")
- getPresence: contactId
- subscribePresence: contactId
- getAllPresence: — all subscribed contacts

**Profile:**
- getProfile: — own profile
- setProfileName: name
- setProfileStatus: status (text)
- setProfilePicture: file (URL)
- deleteProfilePicture:

**LID Resolution:**
- findPhoneByLid: lid — LID to phone
- findLidByPhone: phone — phone to LID
- getAllLids: — all known LIDs

**Calls:**
- rejectCall: callId

**API Keys:**
- createApiKey: name
- getApiKeys:
- updateApiKey: keyId, name
- deleteApiKey: keyId

**Search / Discovery:**
- search: query, scope ("group"|"contact"|"channel"|"auto") — fuzzy name-to-JID lookup, no target needed
- resolveTarget: query, type ("group"|"contact"|"channel"|"auto") — same but utility form

**Policy (admin only):**
- editPolicy: scope ("contact"|"group"), targetId, field, value, actorId — edit contact/group allow policy

**Slash Commands (intercepted before LLM — no action call needed):**
Add a section "## Slash Commands" documenting:
- `/join <invite-link>` — join group via chat.whatsapp.com invite URL (bypass LLM)
- `/join <group-name>` — fuzzy match and join; ambiguous matches return numbered candidate list for confirmation
- `/leave <group-or-channel-name>` — fuzzy match and leave; confirms before acting
- `/list` — list all groups and channels the agent belongs to
- `/list groups` — groups only
- `/list channels` — channels/newsletters only
Note: These commands require the message sender to be in the admin authorization list.

Keep the existing "Guidelines" section at the bottom.

EXCLUDE: sendText (hijacked for human behavior mimicry), sendWahaMedia (internal), detectMimeViaHead (internal), sendWahaMediaBatch (internal routing), assertCanSend, buildFilePayload, fuzzyScore, toArr, resolveWahaTarget (internal — use `search` action instead).
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const s = fs.readFileSync('skills/whatsapp-messenger/SKILL.md','utf8'); const checks = ['joinGroup','getInviteCode','revokeInviteCode','editPolicy','search','sendMulti','muteChat','getLabels','sendTextStatus','findPhoneByLid','rejectCall','createApiKey','Slash Commands','/join','/leave','/list']; const missing = checks.filter(c => !s.includes(c)); if(missing.length) { console.error('MISSING:', missing); process.exit(1); } console.log('All checks passed');"</automated>
  </verify>
  <done>skills/whatsapp-messenger/SKILL.md contains all categories and every action listed above. Node verification script exits 0. No implemented action in EXPOSED_ACTIONS or ACTION_HANDLERS is absent from the skill.</done>
</task>

</tasks>

<verification>
Run the automated check from Task 1 verify block:
```
node -e "const fs = require('fs'); const s = fs.readFileSync('skills/whatsapp-messenger/SKILL.md','utf8'); const checks = ['joinGroup','getInviteCode','revokeInviteCode','editPolicy','search','sendMulti','muteChat','getLabels','sendTextStatus','findPhoneByLid','rejectCall','createApiKey','Slash Commands','/join','/leave','/list']; const missing = checks.filter(c => !s.includes(c)); if(missing.length) { console.error('MISSING:', missing); process.exit(1); } console.log('All checks passed');"
```
</verification>

<success_criteria>
- skills/whatsapp-messenger/SKILL.md lists all ~100+ actions organized by 14 categories
- Every key in ACTION_HANDLERS (channel.ts lines 155-374) appears in the skill
- All STANDARD_ACTIONS documented
- /join /leave /list slash commands documented with syntax
- No hijacked/internal functions included
- Node verification script exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/46-skill-completeness-audit/46-01-SUMMARY.md`
</output>
