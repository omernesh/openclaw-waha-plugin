---
phase: 31-test-coverage-sprint
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/admin/vitest.config.ts
  - src/admin/src/components/tabs/DashboardTab.test.tsx
  - src/admin/src/components/tabs/SettingsTab.test.tsx
  - src/admin/src/components/tabs/DirectoryTab.test.tsx
  - src/admin/src/components/tabs/LogTab.test.tsx
  - src/admin/src/components/tabs/AnalyticsTab.test.tsx
autonomous: true
requirements:
  - TST-05

must_haves:
  truths:
    - "Each React admin panel tab has at least one component test"
    - "Tests verify rendering without crash and at least one key interaction"
    - "All tests pass with npx vitest run from src/admin/"
  artifacts:
    - path: "src/admin/vitest.config.ts"
      provides: "Vitest config for React component tests with jsdom"
      min_lines: 8
    - path: "src/admin/src/components/tabs/DashboardTab.test.tsx"
      provides: "DashboardTab render + interaction test"
      min_lines: 20
    - path: "src/admin/src/components/tabs/SettingsTab.test.tsx"
      provides: "SettingsTab render + interaction test"
      min_lines: 20
    - path: "src/admin/src/components/tabs/DirectoryTab.test.tsx"
      provides: "DirectoryTab render + interaction test"
      min_lines: 20
    - path: "src/admin/src/components/tabs/LogTab.test.tsx"
      provides: "LogTab render + interaction test"
      min_lines: 20
    - path: "src/admin/src/components/tabs/AnalyticsTab.test.tsx"
      provides: "AnalyticsTab render + interaction test"
      min_lines: 20
  key_links:
    - from: "src/admin/src/components/tabs/DashboardTab.test.tsx"
      to: "src/admin/src/components/tabs/DashboardTab.tsx"
      via: "import and render"
      pattern: "import.*DashboardTab"
    - from: "src/admin/vitest.config.ts"
      to: "vitest"
      via: "jsdom environment config"
      pattern: "environment.*jsdom"
---

<objective>
Create React component tests for all admin panel tabs using vitest + @testing-library/react.

Purpose: The admin panel has 8 tab components with zero test coverage. Each tab needs at least one render test and one interaction test to catch regressions.
Output: vitest config for React/jsdom, 5 test files (one per major tab).
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/admin/package.json
@src/admin/src/components/tabs/DashboardTab.tsx
@src/admin/src/components/tabs/SettingsTab.tsx
@src/admin/src/components/tabs/DirectoryTab.tsx
@src/admin/src/components/tabs/LogTab.tsx
@src/admin/src/components/tabs/AnalyticsTab.tsx
@src/admin/src/lib/api.ts
@src/admin/vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up React testing infrastructure</name>
  <files>src/admin/vitest.config.ts, src/admin/package.json</files>
  <read_first>src/admin/package.json, src/admin/vite.config.ts, src/admin/tsconfig.json</read_first>
  <action>
    1. Install test dependencies in src/admin/:
       cd src/admin && npm install -D @testing-library/react @testing-library/jest-dom @testing-library/user-event jsdom

    2. Create src/admin/vitest.config.ts:
       - environment: 'jsdom'
       - globals: false
       - include pattern: ['src/**/*.test.{ts,tsx}']
       - setupFiles pointing to a test setup file
       - resolve.alias matching vite.config.ts (for @/ path alias if used)

    3. Create src/admin/src/test-setup.ts:
       - import '@testing-library/jest-dom/vitest'
       - Mock fetch globally (vi.stubGlobal)
       - Mock ResizeObserver (needed for many shadcn components)
       - Mock IntersectionObserver if needed

    4. Add test script to src/admin/package.json:
       "test": "vitest run --reporter=verbose"

    Verify: cd src/admin && npx vitest run --reporter=verbose (should find zero tests but not error)
  </action>
  <verify>
    <automated>cd src/admin && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep "jsdom" src/admin/vitest.config.ts confirms jsdom environment
    - grep "testing-library" src/admin/package.json confirms dependencies installed
    - cd src/admin && npx vitest run does not error
  </acceptance_criteria>
  <done>React testing infrastructure configured — vitest with jsdom, testing-library installed, setup file created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: React tab component tests</name>
  <files>
    src/admin/src/components/tabs/DashboardTab.test.tsx,
    src/admin/src/components/tabs/SettingsTab.test.tsx,
    src/admin/src/components/tabs/DirectoryTab.test.tsx,
    src/admin/src/components/tabs/LogTab.test.tsx,
    src/admin/src/components/tabs/AnalyticsTab.test.tsx
  </files>
  <read_first>
    src/admin/src/components/tabs/DashboardTab.tsx,
    src/admin/src/components/tabs/SettingsTab.tsx,
    src/admin/src/components/tabs/DirectoryTab.tsx,
    src/admin/src/components/tabs/LogTab.tsx,
    src/admin/src/components/tabs/AnalyticsTab.tsx,
    src/admin/src/lib/api.ts,
    src/admin/src/test-setup.ts
  </read_first>
  <behavior>
    DashboardTab:
    - Renders without crash
    - Shows loading state initially
    - Displays session health cards after data loads

    SettingsTab:
    - Renders without crash
    - Shows config form fields after data loads
    - Save button triggers API call

    DirectoryTab:
    - Renders without crash
    - Shows search input
    - Tab filters (contacts/groups/newsletters) are clickable

    LogTab:
    - Renders without crash
    - Shows log output area
    - Auto-scroll toggle is present

    AnalyticsTab:
    - Renders without crash
    - Shows chart containers
    - Range selector is present
  </behavior>
  <action>
    Create 5 test files, one per major tab. Each test file:

    1. Import { render, screen } from '@testing-library/react'
    2. Import { describe, it, expect, vi, beforeEach } from 'vitest'
    3. Import the tab component
    4. Mock the api.ts module (vi.mock('../../../lib/api')) — return sensible default data
    5. Mock any context providers (useEventSource, useTheme) if the component requires them
    6. Wrap renders in necessary providers (if components use React context)

    For each tab, write:
    - "renders without crash" — render(), expect no thrown error
    - "shows expected UI elements" — screen.getByText/getByRole for key elements
    - "handles user interaction" — at least one click/input test (use @testing-library/user-event)

    Mock API responses:
    - DashboardTab: mock GET /api/admin/sessions returning [{name: 'test', status: 'CONNECTED'}]
    - SettingsTab: mock GET /api/admin/config returning {waha: {dmFilter: 'allow-all'}}
    - DirectoryTab: mock GET /api/admin/directory returning {items: [], total: 0}
    - LogTab: mock GET /api/admin/logs returning {lines: ['test log line']}
    - AnalyticsTab: mock GET /api/admin/analytics returning {data: []}

    If a component uses SSE (useEventSource hook), mock it to return a no-op provider.
    If a component uses recharts, mock recharts components to render simple divs (recharts needs canvas).
  </action>
  <verify>
    <automated>cd src/admin && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - ls src/admin/src/components/tabs/*test* shows 5 test files
    - grep -c "describe\|it(" src/admin/src/components/tabs/DashboardTab.test.tsx returns >= 3
    - grep "render" src/admin/src/components/tabs/DashboardTab.test.tsx confirms render tests
    - cd src/admin && npx vitest run exits 0
  </acceptance_criteria>
  <done>All 5 major tab components have render + interaction tests. All pass in vitest with jsdom.</done>
</task>

</tasks>

<verification>
cd src/admin && npx vitest run --reporter=verbose
All React component tests pass, zero failures.
</verification>

<success_criteria>
- vitest.config.ts exists in src/admin with jsdom environment
- 5 tab test files exist (Dashboard, Settings, Directory, Log, Analytics)
- Each test file has at least 3 test cases (render, UI elements, interaction)
- cd src/admin && npx vitest run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/31-test-coverage-sprint/31-03-SUMMARY.md`
</output>
