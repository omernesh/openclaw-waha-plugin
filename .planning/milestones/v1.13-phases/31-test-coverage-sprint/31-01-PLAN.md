---
phase: 31-test-coverage-sprint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/directory.test.ts
  - src/shutup.test.ts
autonomous: true
requirements:
  - TST-03
  - TST-04

must_haves:
  truths:
    - "directory.ts CRUD operations each have at least one passing test"
    - "shutup.ts interactive mute/unmute flow has tests for happy path and cancellation"
    - "All tests pass with npx vitest run"
  artifacts:
    - path: "src/directory.test.ts"
      provides: "DirectoryDb CRUD tests"
      min_lines: 100
    - path: "src/shutup.test.ts"
      provides: "Shutup command flow tests"
      min_lines: 60
  key_links:
    - from: "src/directory.test.ts"
      to: "src/directory.ts"
      via: "import DirectoryDb, getDirectoryDb"
      pattern: "import.*DirectoryDb.*directory"
    - from: "src/shutup.test.ts"
      to: "src/shutup.ts"
      via: "import exported functions"
      pattern: "import.*shutup"
---

<objective>
Create test suites for directory.ts (SQLite CRUD) and shutup.ts (interactive mute/unmute flow).

Purpose: These are pure-logic modules with well-defined I/O — ideal for isolated unit tests with in-memory SQLite and mocked WAHA API calls.
Output: Two test files with passing tests covering all exported functions.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/directory.ts
@src/shutup.ts
@src/types.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: directory.ts CRUD test suite</name>
  <files>src/directory.test.ts</files>
  <read_first>src/directory.ts, src/types.ts, vitest.config.ts</read_first>
  <behavior>
    - Test DirectoryDb constructor with :memory: SQLite — tables created (contacts, dm_settings, allow_list, group_participants)
    - Test upsertContact / getContact / getAllContacts — insert, read back, update existing
    - Test contact search — searchContacts with name substring
    - Test DM settings — setDmSettings / getDmSettings for a contact JID
    - Test allow list — addToAllowList / removeFromAllowList / isAllowed
    - Test group participants — bulkUpsertGroupParticipants / getGroupParticipants
    - Test group filter overrides — setGroupFilterOverride / getGroupFilterOverride / clearGroupFilterOverride
    - Test LID mapping — setLidMapping / getLidMapping / getJidFromLid
    - Test muted groups — muteGroup / unmuteGroup / getMutedGroups / isGroupMuted
    - Test pending selections — setPendingSelection / getPendingSelection / clearPendingSelection
    - Test getDirectoryDb singleton — same accountId returns same instance
    - Test contact type filtering — getAllContacts with type='contact', type='group', type='newsletter'
    - Test pagination — getAllContacts with limit/offset
  </behavior>
  <action>
    Create src/directory.test.ts using vitest. Import DirectoryDb class directly and instantiate with ':memory:' for each test (use beforeEach). Test every exported function and class method listed in the behavior section. Use real SQLite (better-sqlite3 in-memory) — no mocking the database layer.

    For getDirectoryDb singleton test: call it twice with same accountId, verify same instance. Call with different accountId, verify different instance. Clean up singletons after test.

    Group tests by domain: "contacts", "dm_settings", "allow_list", "participants", "group_filter_overrides", "lid_mapping", "muted_groups", "pending_selections", "singleton".
  </action>
  <verify>
    <automated>npx vitest run src/directory.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" src/directory.test.ts returns >= 15 (at least 15 test cases)
    - grep "DirectoryDb" src/directory.test.ts confirms DirectoryDb is imported and instantiated
    - grep ":memory:" src/directory.test.ts confirms in-memory SQLite usage
    - npx vitest run src/directory.test.ts exits 0
  </acceptance_criteria>
  <done>All DirectoryDb CRUD operations tested — contacts, participants, DM settings, allow lists, overrides, LID mapping, muted groups, pending selections. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: shutup.ts interactive flow test suite</name>
  <files>src/shutup.test.ts</files>
  <read_first>src/shutup.ts, src/directory.ts, src/types.ts</read_first>
  <behavior>
    - Test SHUTUP_RE regex — matches /shutup, /unshutup, /unmute with optional "all" and duration
    - Test SHUTUP_RE regex — rejects non-matching strings
    - Test checkPendingSelection — returns null when no pending, returns record when pending
    - Test clearPendingSelection — clears existing pending selection
    - Test checkShutupAuthorization — god mode user is authorized
    - Test checkShutupAuthorization — non-god-mode user is rejected
    - Test handleShutupCommand — /shutup with single group mutes immediately
    - Test handleShutupCommand — /shutup with multiple groups creates pending selection
    - Test handleShutupCommand — /shutup all mutes all groups
    - Test handleShutupCommand — /unshutup unmutes a muted group
    - Test handleSelectionResponse — valid selection number confirms mute
    - Test handleSelectionResponse — invalid selection number sends error
    - Test handleSelectionResponse — "cancel" clears pending
    - Test duration parsing — /shutup 30m, /shutup 2h, /shutup 1d
  </behavior>
  <action>
    Create src/shutup.test.ts using vitest. Mock dependencies:
    - vi.mock('./send.js') — mock sendWahaText to capture sent messages
    - vi.mock('./directory.js') — mock getDirectoryDb to return a mock DirectoryDb with in-memory muted groups
    - vi.mock('./runtime.js') — mock getRuntime if needed for config access
    - vi.mock('./accounts.js') — mock resolveAccountSession

    Test SHUTUP_RE regex directly (pure, no mocks needed).
    Test checkPendingSelection / clearPendingSelection with mocked DirectoryDb.
    Test checkShutupAuthorization with mocked config (godModeSuperUsers).
    Test handleShutupCommand with various command patterns — verify correct sendWahaText calls and DirectoryDb.muteGroup calls.
    Test handleSelectionResponse — verify selection confirmation, cancellation, invalid input.
  </action>
  <verify>
    <automated>npx vitest run src/shutup.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" src/shutup.test.ts returns >= 10 (at least 10 test cases)
    - grep "SHUTUP_RE" src/shutup.test.ts confirms regex tests
    - grep "handleShutupCommand\|handleSelectionResponse" src/shutup.test.ts confirms flow tests
    - npx vitest run src/shutup.test.ts exits 0
  </acceptance_criteria>
  <done>Shutup interactive flow fully tested — regex parsing, authorization, single/multi group mute, unmute, pending selection, cancellation. All tests pass.</done>
</task>

</tasks>

<verification>
npx vitest run src/directory.test.ts src/shutup.test.ts --reporter=verbose
All tests pass, zero failures.
</verification>

<success_criteria>
- src/directory.test.ts exists with 15+ test cases covering all DirectoryDb CRUD methods
- src/shutup.test.ts exists with 10+ test cases covering regex, auth, mute/unmute, pending selection
- npx vitest run exits 0 for both files
</success_criteria>

<output>
After completion, create `.planning/phases/31-test-coverage-sprint/31-01-SUMMARY.md`
</output>
