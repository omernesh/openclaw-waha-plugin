---
phase: 31-test-coverage-sprint
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/monitor.test.ts
  - src/inbound.test.ts
autonomous: true
requirements:
  - TST-01
  - TST-02

must_haves:
  truths:
    - "Every admin API route in monitor.ts has at least one passing test"
    - "The inbound.ts message pipeline (filter, dedup, queue) has tests with mocked SDK"
    - "All tests pass with npx vitest run"
  artifacts:
    - path: "src/monitor.test.ts"
      provides: "Admin API route tests"
      min_lines: 150
    - path: "src/inbound.test.ts"
      provides: "Inbound pipeline tests"
      min_lines: 100
  key_links:
    - from: "src/monitor.test.ts"
      to: "src/monitor.ts"
      via: "import createWahaWebhookServer or route handlers"
      pattern: "import.*monitor"
    - from: "src/inbound.test.ts"
      to: "src/inbound.ts"
      via: "import handleWahaInbound, filter helpers"
      pattern: "import.*inbound"
---

<objective>
Create test suites for monitor.ts (admin API routes) and inbound.ts (message pipeline).

Purpose: These are the two largest untested modules (2266 + 1149 lines). They handle HTTP routing and message processing — the most critical paths in the plugin. Tests use mock HTTP req/res for monitor and mocked SDK imports for inbound.
Output: Two test files with passing tests covering admin API endpoints and inbound pipeline logic.
</objective>

<execution_context>
@C:/Users/omern/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/monitor.ts
@src/inbound.ts
@src/types.ts
@src/config-schema.ts
@vitest.config.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: monitor.ts admin API route tests</name>
  <files>src/monitor.test.ts</files>
  <read_first>src/monitor.ts, src/types.ts, src/config-schema.ts, vitest.config.ts</read_first>
  <behavior>
    - Test readWahaWebhookBody — reads request body up to max bytes
    - Test readWahaWebhookBody — rejects body exceeding max bytes
    - Test broadcastSSE — sends event to connected clients (or no-op with zero clients)
    - Test GET /api/admin/stats — returns JSON with filter statistics
    - Test GET /api/admin/config — returns current plugin config
    - Test POST /api/admin/config — accepts valid config, returns 200
    - Test POST /api/admin/config — rejects invalid config, returns 400 with errors
    - Test GET /api/admin/sessions — returns session status array
    - Test GET /api/admin/directory — returns paginated directory listing
    - Test GET /api/admin/directory/:jid — returns single contact
    - Test PUT /api/admin/directory/:jid/allow-dm — toggles DM allow
    - Test POST /api/admin/directory/refresh — triggers directory refresh
    - Test GET /api/admin/config/export — returns JSON file download
    - Test POST /api/admin/restart — triggers gateway restart
    - Test GET /api/admin/analytics — returns analytics data
    - Test GET /api/admin/health — returns recovery info
    - Test CORS headers — OPTIONS returns correct headers
  </behavior>
  <action>
    Create src/monitor.test.ts using vitest. Strategy: do NOT start the HTTP server. Instead, study how createWahaWebhookServer registers route handlers internally and either:

    Option A (preferred): If route handlers are extractable, import and call them directly with mock req/res.
    Option B: Create mock IncomingMessage and ServerResponse objects, then call the server's request handler function directly.

    For mock req/res:
    - Use node:events EventEmitter for req (with url, method, headers properties)
    - Use a writable object for res (with writeHead, end, setHeader methods that capture values)

    Mock dependencies:
    - vi.mock('./send.js') — mock WAHA API calls
    - vi.mock('./directory.js') — mock DirectoryDb
    - vi.mock('./runtime.js') — mock getRuntime for config
    - vi.mock('./accounts.js') — mock resolveAccountSession
    - vi.mock('./health.js') — mock health check functions
    - vi.mock('./analytics.js') — mock AnalyticsDb

    Test each API route by setting req.url and req.method, pushing body data, and asserting res.writeHead status code and res.end() JSON output.

    Group tests: "readWahaWebhookBody", "broadcastSSE", "admin API routes" (with nested describes per endpoint).
  </action>
  <verify>
    <automated>npx vitest run src/monitor.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" src/monitor.test.ts returns >= 15 (at least 15 test cases)
    - grep "/api/admin/" src/monitor.test.ts confirms API route coverage
    - grep "readWahaWebhookBody" src/monitor.test.ts confirms utility function tests
    - npx vitest run src/monitor.test.ts exits 0
  </acceptance_criteria>
  <done>Every admin API route has at least one test. readWahaWebhookBody tested. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: inbound.ts pipeline tests</name>
  <files>src/inbound.test.ts</files>
  <read_first>src/inbound.ts, src/types.ts, src/config-schema.ts</read_first>
  <behavior>
    - Test getDmFilterForAdmin — returns DM filter config for account
    - Test getGroupFilterForAdmin — returns group filter config for account
    - Test handleWahaInbound with DM message from allowed sender — message is processed
    - Test handleWahaInbound with DM message from blocked sender — message is filtered
    - Test handleWahaInbound with group message matching group filter — message is processed
    - Test handleWahaInbound with group message not matching filter — message is filtered
    - Test handleWahaInbound with fromMe message — message is skipped
    - Test handleWahaInbound with duplicate webhook — message is deduplicated
    - Test handleWahaInbound with trigger word detection — correct group is targeted
    - Test handleWahaInbound with /shutup command — intercepted before delivery
    - Test handleWahaInbound with media message — media URL is extracted
    - Test handleWahaInbound with vCard message — vCard is intercepted in deliverWahaReply
  </behavior>
  <action>
    Create src/inbound.test.ts using vitest. Mock all external dependencies:
    - vi.mock('./send.js') — mock sendWahaText, sendWahaImage, etc.
    - vi.mock('./directory.js') — mock getDirectoryDb returning controlled data
    - vi.mock('./runtime.js') — mock getRuntime
    - vi.mock('./accounts.js') — mock resolveAccountSession
    - vi.mock('./shutup.js') — mock checkShutupAuthorization, handleShutupCommand
    - vi.mock('./channel.js') — mock any channel imports
    - vi.mock('./health.js') — mock health check
    - vi.mock('./analytics.js') — mock analytics recording
    - vi.mock('./mentions.js') — mock extractMentionedJids

    Test getDmFilterForAdmin and getGroupFilterForAdmin as pure functions with controlled config objects.

    Test handleWahaInbound by constructing WAHA webhook event payloads (message event shape: { event: 'message', payload: { from, to, body, ... }, session }) and calling the function with mocked OpenClaw SDK deliverInbound.

    Create helper: makeWahaMessage(overrides) that returns a valid WAHA message event payload with sensible defaults.

    Group tests: "getDmFilterForAdmin", "getGroupFilterForAdmin", "handleWahaInbound" (with nested describes for DM filtering, group filtering, dedup, commands, media).
  </action>
  <verify>
    <automated>npx vitest run src/inbound.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "describe\|it(" src/inbound.test.ts returns >= 10 (at least 10 test cases)
    - grep "handleWahaInbound" src/inbound.test.ts confirms pipeline tests
    - grep "getDmFilterForAdmin\|getGroupFilterForAdmin" src/inbound.test.ts confirms filter helper tests
    - npx vitest run src/inbound.test.ts exits 0
  </acceptance_criteria>
  <done>Inbound pipeline tested — DM filter, group filter, dedup, command interception, fromMe skip. All tests pass.</done>
</task>

</tasks>

<verification>
npx vitest run src/monitor.test.ts src/inbound.test.ts --reporter=verbose
All tests pass, zero failures.
</verification>

<success_criteria>
- src/monitor.test.ts exists with 15+ test cases covering all admin API routes
- src/inbound.test.ts exists with 10+ test cases covering pipeline filter/dedup/queue flow
- npx vitest run exits 0 for both files
</success_criteria>

<output>
After completion, create `.planning/phases/31-test-coverage-sprint/31-02-SUMMARY.md`
</output>
