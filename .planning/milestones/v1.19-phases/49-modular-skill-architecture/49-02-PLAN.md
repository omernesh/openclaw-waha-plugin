---
phase: 49-modular-skill-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/status.md
  - skills/presence.md
  - skills/profile.md
  - skills/media.md
  - skills/slash-commands.md
  - SKILL.md
autonomous: true
requirements: [SKL-01, SKL-02, SKL-03]

must_haves:
  truths:
    - "Agent reading status.md can post and delete status/stories with correct parameters"
    - "Agent reading presence.md understands setPresence/setPresenceStatus alias and can manage presence"
    - "Agent reading profile.md can get and update all profile fields"
    - "Agent reading media.md knows file must be direct URL and understands alternative param names"
    - "Agent reading slash-commands.md understands /join /leave /list /shutup syntax and authorization"
    - "SKILL.md is a concise index with category links and no inline action tables"
  artifacts:
    - path: "skills/status.md"
      provides: "Status/stories actions"
      contains: "sendTextStatus"
    - path: "skills/presence.md"
      provides: "Presence actions"
      contains: "setPresenceStatus"
    - path: "skills/profile.md"
      provides: "Profile actions"
      contains: "setProfileName"
    - path: "skills/media.md"
      provides: "Media send + conversion"
      contains: "convertVoice"
    - path: "skills/slash-commands.md"
      provides: "Owner slash commands"
      contains: "/join"
    - path: "SKILL.md"
      provides: "Concise index referencing all 10 category files"
      contains: "skills/groups.md"
  key_links:
    - from: "SKILL.md"
      to: "skills/messaging.md"
      via: "Category link in index table"
      pattern: "skills/messaging.md"
    - from: "SKILL.md"
      to: "skills/groups.md"
      via: "Category link in index table"
      pattern: "skills/groups.md"
---

<objective>
Create the 5 smaller per-category skill files (status, presence, profile, media, slash-commands) and rewrite SKILL.md as a concise index.

Purpose: Completes the modular skill architecture. The index becomes the agent's entry point; category files are the detailed references. No inline action tables remain in SKILL.md.

Output: 5 category markdown files + rewritten SKILL.md index
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-modular-skill-architecture/49-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create status.md, presence.md, profile.md, media.md, slash-commands.md</name>
  <files>skills/status.md, skills/presence.md, skills/profile.md, skills/media.md, skills/slash-commands.md</files>
  <read_first>
    - SKILL.md (current skill file — source content for these sections)
    - src/channel.ts lines 456-480 (status, presence, profile, LID, calls in UTILITY_ACTIONS)
    - .planning/phases/49-modular-skill-architecture/49-RESEARCH.md (action inventory, gotchas)
  </read_first>
  <action>
    Create five category files in skills/ directory using the same template structure (Actions table, Examples, Gotchas, back-reference to SKILL.md).

    **status.md** — Status/Stories (6 actions):
    - Actions: sendTextStatus, sendImageStatus, sendVoiceStatus (ACT-04), sendVideoStatus (ACT-04), deleteStatus, getNewMessageId (ACT-04)
    - Examples: post text status with background color, post image status, delete status
    - Gotchas: getNewMessageId generates a message ID for use in other operations

    **presence.md** — Presence (5 actions):
    - Actions: setPresenceStatus, setPresence (alias), getPresence, subscribePresence, getAllPresence
    - Examples: set online, subscribe to contact presence, get all subscribed presences
    - Gotchas: setPresenceStatus and setPresence are aliases — both work. Document both names.

    **profile.md** — Profile (5 actions):
    - Actions: getProfile, setProfileName, setProfileStatus, setProfilePicture, deleteProfilePicture
    - Examples: get current profile, update name, set profile picture
    - Gotchas: file param for setProfilePicture = direct URL or absolute path

    **media.md** — Media Send + Conversion (5 actions):
    - Actions: sendImage, sendVideo, sendFile, convertVoice (ACT-07), convertVideo (ACT-07)
    - CRITICAL gotcha: `file` must be a direct media URL, NOT a JSON API endpoint. For local files: absolute path. Alternative param names: image/url for sendImage, video/url for sendVideo, url for sendFile.
    - Examples: send image with caption, send document, convert voice
    - Note: audio/ogg auto-rewritten to audio/ogg; codecs=opus for voice bubbles. gifPlayback: true for animated GIFs.

    **slash-commands.md** — Owner Commands (5 commands):
    - Commands: /join, /leave, /list, /shutup, /unshutup, /activation
    - Copy the detailed syntax tables from current SKILL.md (lines 506-573)
    - Authorization: godModeSuperUsers + allowFrom. Pre-LLM processing.
    - Note: These are NOT AI-invoked actions — they are processed before the LLM sees the message.

    Also include LID and Calls actions. These are small enough to fold into an "Other Utilities" section in one of these files. Add to messaging.md or create a brief section at the bottom of profile.md:
    - LID: findPhoneByLid, findLidByPhone, getAllLids
    - Calls: rejectCall
    - Policy: editPolicy

    Decision: Add LID, Calls, and Policy as a "## Other Utilities" section at the bottom of media.md (closest fit — these are misc utilities).
  </action>
  <verify>
    <automated>test -f skills/status.md && test -f skills/presence.md && test -f skills/profile.md && test -f skills/media.md && test -f skills/slash-commands.md && grep -q "sendTextStatus" skills/status.md && grep -q "setPresenceStatus" skills/presence.md && grep -q "setProfileName" skills/profile.md && grep -q "convertVoice" skills/media.md && grep -q "/join" skills/slash-commands.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist in skills/ directory
    - Each file has "## Actions", "## Examples", "## Gotchas" sections
    - skills/status.md contains all 6 status actions including getNewMessageId (ACT-04)
    - skills/presence.md documents setPresenceStatus/setPresence alias
    - skills/profile.md contains all 5 profile actions
    - skills/media.md contains sendImage, sendVideo, sendFile, convertVoice, convertVideo
    - skills/media.md has "direct media URL" gotcha prominently documented
    - skills/media.md has LID + Calls + Policy actions in Other Utilities section
    - skills/slash-commands.md documents /join, /leave, /list, /shutup, /unshutup, /activation
    - Each file has back-reference: "See SKILL.md for overview"
  </acceptance_criteria>
  <done>Five category files created with complete action tables, examples, and gotchas. All remaining UTILITY_ACTIONS covered including LID, Calls, and Policy.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite SKILL.md as concise index</name>
  <files>SKILL.md</files>
  <read_first>
    - SKILL.md (current 574-line file — back up first, then rewrite)
    - .planning/phases/49-modular-skill-architecture/49-RESEARCH.md (index template)
    - src/channel.ts lines 404-484 (verify all actions covered across category files)
  </read_first>
  <action>
    1. Back up current SKILL.md: `cp SKILL.md SKILL.md.bak.v1.18.0`

    2. Rewrite SKILL.md as a concise index (~80-120 lines). Structure per RESEARCH.md template:

    ```markdown
    ---
    name: whatsapp-actions
    description: [keep existing description from current SKILL.md frontmatter]
    version: 6.0.0
    ---

    > **IMPORTANT — Standard Action Names**: [keep existing note]

    # WhatsApp Actions — Skill Index

    [2-3 sentences: plugin auto-resolves names to JIDs, standard vs utility actions, per-category files have full details]

    ## Category Files

    | Category | File | Key Actions |
    |----------|------|-------------|
    | Messaging & Rich Messages | [skills/messaging.md](skills/messaging.md) | send, poll, react, reply, sendEvent, sendLocation, readMessages |
    | Groups | [skills/groups.md](skills/groups.md) | createGroup, addParticipants, joinGroup, getInviteCode |
    | Contacts | [skills/contacts.md](skills/contacts.md) | getContacts, blockContact, sendContactVcard (vCard) |
    | Channels (Newsletters) | [skills/channels.md](skills/channels.md) | followChannel, searchChannelsByText, createChannel |
    | Chat Management | [skills/chats.md](skills/chats.md) | archiveChat, getChatMessages, muteChat, labels |
    | Status/Stories | [skills/status.md](skills/status.md) | sendTextStatus, sendImageStatus, deleteStatus |
    | Presence | [skills/presence.md](skills/presence.md) | setPresence, getPresence, subscribePresence |
    | Profile | [skills/profile.md](skills/profile.md) | getProfile, setProfileName, setProfilePicture |
    | Media | [skills/media.md](skills/media.md) | sendImage, sendVideo, sendFile, LID lookups |
    | Slash Commands | [skills/slash-commands.md](skills/slash-commands.md) | /join, /leave, /list, /shutup |

    ## Quick Start

    [5-6 most common tasks with one-liner examples — send text, send image, search, create poll, read messages, join group]

    ## Auto-Resolution

    [Keep existing auto-resolution section — brief version]

    ## Parameter Formats
    [Keep existing parameter formats section from current SKILL.md]

    ## Error Handling and Recovery
    [Keep existing error table from current SKILL.md]

    ## Rate Limiting
    [Keep existing rate limiting section]

    ## Multi-Session
    [Keep existing multi-session section — brief version with session roles table]

    ## Access Control
    [Keep existing DM/group policy section — brief version]
    ```

    3. CRITICAL: NO inline action tables in the index. All action details live in category files only. The index has Quick Start examples and cross-cutting concerns (errors, rate limiting, multi-session, access control).

    4. Keep the frontmatter `name` and `description` fields — the gateway/agent uses these for skill identification.

    5. Do NOT include "Sammie" anywhere (CLAUDE.md rule). Use "the bot" or "the agent" instead.
  </action>
  <verify>
    <automated>test -f SKILL.md && test -f SKILL.md.bak.v1.18.0 && grep -q "skills/messaging.md" SKILL.md && grep -q "skills/groups.md" SKILL.md && grep -q "skills/contacts.md" SKILL.md && grep -q "skills/channels.md" SKILL.md && grep -q "skills/chats.md" SKILL.md && grep -q "skills/status.md" SKILL.md && grep -q "skills/presence.md" SKILL.md && grep -q "skills/profile.md" SKILL.md && grep -q "skills/media.md" SKILL.md && grep -q "skills/slash-commands.md" SKILL.md && ! grep -q "| \`sendPollVote\`" SKILL.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md.bak.v1.18.0 backup exists
    - SKILL.md has frontmatter with name: whatsapp-actions and version: 6.0.0
    - SKILL.md contains Category Files table linking all 10 category files
    - SKILL.md has Quick Start section with common task examples
    - SKILL.md retains Parameter Formats, Error Handling, Rate Limiting, Multi-Session, Access Control sections
    - SKILL.md does NOT contain inline action tables (no `| sendPollVote |`, no `| archiveChat |`, etc.)
    - SKILL.md is under 150 lines (concise index, not full documentation)
    - No "Sammie" appears in the file
    - All 10 skills/*.md files referenced with relative links
  </acceptance_criteria>
  <done>SKILL.md rewritten as concise index. All action details live in 10 per-category files. Backup preserved. Version bumped to 6.0.0.</done>
</task>

</tasks>

<verification>
Complete modular skill architecture verification:
1. File count: `ls skills/messaging.md skills/groups.md skills/contacts.md skills/channels.md skills/chats.md skills/status.md skills/presence.md skills/profile.md skills/media.md skills/slash-commands.md SKILL.md | wc -l` = 11
2. Index links: `grep -c "skills/.*\.md" SKILL.md` >= 10
3. No inline tables in index: `grep -c "| \`send" SKILL.md` = 0 (Quick Start examples use different format)
4. All category files have required sections: `for f in skills/*.md; do grep -l "## Actions" "$f" && grep -l "## Examples" "$f" && grep -l "## Gotchas" "$f"; done`
5. No "Sammie" anywhere: `grep -ri "sammie" SKILL.md skills/*.md` returns nothing
</verification>

<success_criteria>
- 5 files exist: skills/status.md, skills/presence.md, skills/profile.md, skills/media.md, skills/slash-commands.md
- SKILL.md rewritten as concise index (~80-150 lines) with no inline action tables
- SKILL.md links to all 10 category files
- SKILL.md retains cross-cutting sections (errors, rate limiting, multi-session, access control)
- SKILL.md.bak.v1.18.0 backup exists
- Every action in STANDARD_ACTIONS + UTILITY_ACTIONS appears in at least one category file
- No "Sammie" in any file
</success_criteria>

<output>
After completion, create `.planning/phases/49-modular-skill-architecture/49-02-SUMMARY.md`
</output>
