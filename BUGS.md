# Bug Tracker

| # | Status | Severity | Description | Reported | Fixed |
|---|--------|----------|-------------|----------|-------|
| 1 | Open | Medium | **Dashboard — Access Control**: Shows raw JIDs/LIDs/phone numbers instead of contact names. Should resolve to human-readable names. Add tooltips to section titles explaining what each field means. | 2026-03-15 | |
| 2 | Open | Medium | **Dashboard — Session Info**: Only shows the logan session (port 8050). Should display both omer and logan sessions since both are connected (ports 8050 and 8051). | 2026-03-15 | |
| 3 | Open | Low | **Settings — DM Policy "pairing"**: Tooltip says "require approval code" but this was never tested. Need to investigate how pairing actually works before exposing it as an option. | 2026-03-15 | |
| 4 | Open | Medium | **Settings — Allow From / Group Allow From**: (a) Display contact names not raw JIDs/numbers. (b) Add a search bar to find contacts/groups — support UTF-8 (Hebrew + English), fuzzy search, multiple selection. (c) Allowlist textareas get crowded — allow expanding to see more lines. Important: contacts have both @c.us and @lid JIDs — adding/removing a contact must handle both as a pair. | 2026-03-15 | |
| 5 | Open | Medium | **Settings — Mention Patterns input**: Replace plain textarea with a tag-style input. Type a word, press comma/space to separate, word appears as a bubble with an 'x' to delete. Press enter to add to the list. Same UX for both DM and Group keyword filter patterns. | 2026-03-15 | |
| 6 | Open | Medium | **Settings — God Mode Users (DM + Group)**: (a) Display contact names not raw phone numbers/LIDs. (b) Add search bar to find and add contacts (UTF-8, fuzzy, multi-select). (c) Each name on its own line with 'x'/trash icon to remove. Important: a single person may have both a phone number (972xxx) and a LID (271xxx) — display as one contact, add/remove both JIDs together as a pair. | 2026-03-15 | |
| 7 | **Fixed** | High | **Settings — Save & Restart**: Polling overlay with spinner, waits up to 60s, auto-reloads on success. | 2026-03-15 | 2026-03-15 |
| 8 | **Fixed** | Medium | **Directory — Load More**: SQL-level filtering of @lid/@s.whatsapp.net fixes pagination offset and duplicates. | 2026-03-15 | 2026-03-15 |
| 9 | Open | Medium | **Directory — Contact Settings panel**: (a) Add tooltips to all fields (Mode, Mention Only, Custom Keywords, Can Initiate) explaining what each does. (b) Mode dropdown says "Active" / "Listen Only" — unclear what these mean, tooltips needed. (c) "Mention Only" checkbox needs tooltip. (d) Custom Keywords input should use tag-style input (bubble with 'x' to delete, comma/space to separate) instead of plain text field. | 2026-03-15 | |
| 10 | Open | Medium | **Directory — Groups pagination**: 125 groups load as one long list. Change to paginated table with upper and lower nav bars: `<< < 1 2 3 4 > >>` and a "Display [X] groups" selector. | 2026-03-15 | |
| 11 | Open | Medium | **Directory — Group participants**: (a) Some groups show "0 participants" / fail to load participants. (b) Participants display as raw LIDs not contact names. (c) If someone is already in the global allowlist, the UI doesn't reflect it (buttons should show allowed state). **NOTE:** Don't load participants in real-time from WAHA API — load in background, save to DB, read from DB. Refresh once or twice a day (slow process). | 2026-03-15 | |
| 12 | **Fixed** | High | **Directory — Group Filter Override 502**: AbortController timeout + server-side listEnabledWahaAccounts fallback. | 2026-03-15 | 2026-03-15 |
| 13 | Open | Medium | **Directory — Group Filter Override UX**: (a) No per-group trigger operator option. (b) Keywords input should use tag-style input (bubbles, comma/space to separate) instead of plain text. | 2026-03-15 | |
| 14 | Open | Medium | **Directory — Tab switching & search**: (a) Switching between Contacts/Groups/Newsletters should clear the search bar. (b) Add an 'x' clear button inside the search bar. (c) Rename "Newsletters" tab to "Channels". | 2026-03-15 | |
| 15 | Open | Medium | **Sessions — Session type**: Add a dropdown/pulldown menu to change the session type (role) for each session. Currently read-only. | 2026-03-15 | |
| 16 | Open | Medium | **Directory — Group participant roles**: When viewing a group's participants, allow assigning roles via dropdown: "Bot Admin" (can assign managers and configure bot via DM), "Manager" (can manage bot in this specific group), "Participant" (regular, no special rights). | 2026-03-15 | |
| 17 | Open | Low | **Directory — Bulk edit**: Allow selecting multiple contacts/groups/participants and applying changes in bulk (e.g. allow DM, change role, etc.). | 2026-03-15 | |
| 18 | Open | Medium | **Log tab — Structure**: Make the log display more structured and easier to read. Timestamps should be clearly visible and formatted for each log record. Improve visual separation between log entries. | 2026-03-15 | |
